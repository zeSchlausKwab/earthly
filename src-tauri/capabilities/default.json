{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the Earthly main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "os:allow-platform",
    "local-node-admin"
  ]
}
