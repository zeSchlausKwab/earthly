Cornered switch (not a pill), amber when on. For per-row on/off settings.

```jsx
<Toggle checked={visible} onChange={setVisible} />
```

30×17px, 2px radius. Pair with a 12px label to its left in a 28–32px settings row.
