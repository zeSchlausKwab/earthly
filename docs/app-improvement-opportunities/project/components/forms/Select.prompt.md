26px select trigger; open a Menu on click.

```jsx
<Select value="Dark" onClick={openMenu} />
<Select placeholder="Base map" onClick={openMenu} />
```
For many/long options. 2–4 short options → use SegmentedControl.
