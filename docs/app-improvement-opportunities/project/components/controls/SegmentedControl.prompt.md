Cornered mode/tab switch, one amber active segment. The stance control + panel sub-tabs.

```jsx
<SegmentedControl
  options={["Looking","Focusing","Making"]}
  value={stance}
  onChange={setStance}
/>
```

Keep to 2–4 short labels. Sits in the title bar (stance) or a panel/drawer header (Info/Style/Layers).
