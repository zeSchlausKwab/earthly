Thin track, amber fill, square handle, mono value readout.

```jsx
<Slider value={40} unit="%" onChange={setOpacity} />
```
For opacity / stroke width / overlay strength. Square handle — never a round thumb.
