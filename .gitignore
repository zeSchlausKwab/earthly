# dependencies (bun install)
node_modules
/target
/src-tauri/gen/schemas

# map-chunks
map-chunks

# base files
base-assets

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env.deploy
infra/searxng/.env
ops/vps/searxng/.env
data/events-lmdb/
data/search/
data/og-cache.sqlite*
data/og-images/
data/geocatalog/

# mapnolia (config contains private key, data is blob storage, binary is downloaded)
mapnolia.config.json
mapnolia-data
mapnolia-server

# Legacy deployment artifacts (new releases stage outside the checkout)
deploy.tar.gz

# Android release signing material (generated locally or in CI)
src-tauri/gen/android/keystore.properties
*.jks
*.keystore

# caches
.eslintcache
.cache
*.tsbuildinfo

# Earthly AI/E2E suite artifacts
ai-suite/artifacts
ai-suite/.secrets/
android-suite/artifacts

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# database
legacy-db/*

# claude
.claude
