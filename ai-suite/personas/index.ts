/**
 * @deprecated Signer fixtures are test identities, not experience personas.
 * Import from ../test-identities in new code.
 */
export { testIdentities as personas } from '../test-identities'
export type {
	TestIdentity as EarthlyPersona,
	TestIdentityId as PersonaId,
} from '../test-identities'
