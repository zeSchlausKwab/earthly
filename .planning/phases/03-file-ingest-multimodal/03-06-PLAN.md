---
phase: 03-file-ingest-multimodal
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - src/features/chat/components/FileChipStrip.tsx
  - src/features/chat/components/FileChip.tsx
  - src/features/chat/components/VisionGateControl.tsx
  - src/features/chat/components/fileAttachHandler.ts
  - src/features/chat/components/fileAttachHandler.test.ts
  - src/features/chat/ChatPanel.tsx
  - src/features/chat/ingestSendPath.test.ts
autonomous: false
requirements: [INGEST-01, INGEST-04, INGEST-05, INGEST-07]
must_haves:
  truths:
    - "The user attaches files by a button AND by drag-and-drop, and sees one chip per attached file with parse status (INGEST-01)"
    - "Image files attach as a data URL into the existing image_url content part (INGEST-04), gated by the vision ladder"
    - "Each chip shows the compact parse stat line (rows × cols, detected coordinate columns) expandable for the fuller summary; no always-on data grid (INGEST-05/D-03)"
    - "The vision-gate affordance is three-tier: confirmed → enabled; confirmed-no-vision → hard-disabled + tooltip; unconfirmed → marked uncertain with explicit Send-anyway opt-in (INGEST-07/D-08); the same control governs attached images AND capture_map_snapshot (D-09)"
    - "Parsing runs off the main thread via the ingest client so the UI never freezes (INGEST-02 surfaced; off-thread proven in Plan 02)"
    - "The composed outbound send payload carries {handleId, summary} for an attached dataset and NEVER the raw fullRows array (D-11; success criterion #2 — asserted at the send-path level, not only the store level)"
  artifacts:
    - path: "src/features/chat/components/FileChipStrip.tsx"
      provides: "D-10 NEW strip (Attach-file button + drag-drop + chip list) mounted ALONGSIDE ChatGeometryAttachment"
      min_lines: 40
    - path: "src/features/chat/components/FileChip.tsx"
      provides: "one chip: type icon + filename + stat line + X + Collapsible/Popover expand"
    - path: "src/features/chat/components/fileAttachHandler.ts"
      provides: "extracted pure attach-callback (assertFileWithinCaps → parseFileInWorker → putDataset, in order) — testable below UAT"
      exports: ["handleAttachedFile"]
    - path: "src/features/chat/components/VisionGateControl.tsx"
      provides: "D-08 three-tier image-send affordance driven by detectVisionSupport"
    - path: "src/features/chat/ChatPanel.tsx"
      provides: "mounts FileChipStrip + VisionGateControl beside ChatGeometryAttachment; attachedFiles state; routes attach→parse→ingestStore; composes send with {handleId, summary}"
      contains: "FileChipStrip"
    - path: "src/features/chat/ingestSendPath.test.ts"
      provides: "D-11 send-path invariant: composed outbound message contains handleId+summary but NOT fullRows"
  key_links:
    - from: "src/features/chat/components/FileChipStrip.tsx"
      to: "src/features/chat/components/fileAttachHandler.ts (handleAttachedFile)"
      via: "attach handler delegates to the pure callback (assertFileWithinCaps → parseFileInWorker → putDataset)"
      pattern: "handleAttachedFile|parseFileInWorker|putDataset"
    - from: "src/features/chat/components/VisionGateControl.tsx"
      to: "detectVisionSupport"
      via: "three-tier gating off the VisionSupport result"
      pattern: "detectVisionSupport|VisionSupport"
    - from: "src/features/chat/ChatPanel.tsx"
      to: "FileChipStrip / VisionGateControl"
      via: "mounted beside ChatGeometryAttachment (~681)"
      pattern: "FileChipStrip"
    - from: "src/features/chat/ChatPanel.tsx"
      to: "toModelSummary / {handleId, summary}"
      via: "send composes the dataset attachment as handle+summary, never fullRows (D-11)"
      pattern: "handleId.*summary|toModelSummary"
---

<objective>
Build the chat-input UI: a dedicated file-chip strip (D-10) with a button + drag-and-drop attach path and one chip per file (parse status + expandable stat line), the user-attached-image path (FileReader → `image_url`, INGEST-04), and the three-tier vision-gate affordance (D-08) driven by the Plan 04 ladder and governing both image paths (D-09). All mounted alongside the existing `ChatGeometryAttachment` (not folded in).

Purpose: This is the visible surface of the phase — the two attach paths, the per-file parse summary the user reads (while the model gets only the handle+summary), and the never-silent-send vision gate. It wires the Plan 02 off-thread client, the Plan 03 store/summary/guards, the Plan 04 vision ladder, and the Plan 05 placement/geocode tools into ChatPanel. Honor the UI-SPEC contract verbatim (chip visual language, amber-uncertain idiom, copy strings).

Output: `FileChipStrip.tsx`, `FileChip.tsx`, `fileAttachHandler.ts` (+ test), `VisionGateControl.tsx`, `ChatPanel.tsx` wiring, the `ingestSendPath.test.ts` D-11 send-path invariant, plus a human-verify checkpoint for the visual/interaction acceptance (attach + drop + parse + place + three-tier vision).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-file-ingest-multimodal/03-UI-SPEC.md
@.planning/phases/03-file-ingest-multimodal/03-PATTERNS.md
@.planning/phases/03-file-ingest-multimodal/03-CONTEXT.md
@src/features/chat/ChatGeometryAttachment.tsx
@src/features/chat/ChatPanel.tsx
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- File + component: `src/features/chat/components/FileChipStrip.tsx` — `FileChipStrip({ files, onChange })`
- File + component: `src/features/chat/components/FileChip.tsx` — `FileChip({ file, summary, status, onRemove })`
- File + function: `src/features/chat/components/fileAttachHandler.ts` — `handleAttachedFile(file, deps)` (pure orchestration of assertFileWithinCaps → parseFileInWorker → putDataset)
- File + component: `src/features/chat/components/VisionGateControl.tsx` — `VisionGateControl({ support, onSendAnyway, disabled })`
- File: `src/features/chat/ingestSendPath.test.ts` — D-11 send-path invariant test
- ChatPanel state: `attachedFiles` / ingest-handle list mirroring the `attachedGeometry` useState (146)
- ChatPanel helper: `composeOutboundContent(...)` (or equivalent) producing the dataset attachment as `{handleId, summary}` — exported for `ingestSendPath.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FileChipStrip + FileChip + extracted attach handler (button + drag-drop, off-thread parse, per-file chip + expandable summary)</name>
  <read_first>
    - src/features/chat/ChatGeometryAttachment.tsx (the chip visual language to mirror — trigger button lines 275-288 `Button variant size="sm" h-8 gap-1.5 text-xs` + lucide `h-3.5 w-3.5`; stat row 396-409 `flex flex-wrap items-center gap-2 text-[10px]`; clear control 165-167/365-376 ghost `X`; `{value,onChange}` controlled contract 17-22)
    - .planning/phases/03-file-ingest-multimodal/03-UI-SPEC.md (Interaction Contract Notes — two attach paths, per-chip parse status, expandable summary via Collapsible/Popover, NO data grid; Copywriting Contract — all summary/error strings; Color — status mapping + amber-uncertain; type icons FileSpreadsheet/Braces/FileText/Image)
    - .planning/phases/03-file-ingest-multimodal/03-PATTERNS.md (FileChipStrip/FileChip rows lines 97-101 — analog + add native drag-drop + hidden input)
    - src/features/chat/ingest/ingestClient.ts (parseFileInWorker — Plan 02)
    - src/features/chat/ingest/ingestStore.ts + parseSummary.ts + fileSizeGuards.ts (putDataset, deriveIngestSummary, assertFileWithinCaps — Plan 03)
  </read_first>
  <behavior>
    - Test (attach-order, WARNING 5): `handleAttachedFile(file, deps)` for a non-image file calls `assertFileWithinCaps` FIRST, then `parseFileInWorker(kind, payload)`, then `putDataset` — assert call order via mocked deps (e.g. a recorded call log == `['assertFileWithinCaps','parseFileInWorker','putDataset']`); a file rejected by `assertFileWithinCaps` short-circuits and NEVER calls `parseFileInWorker`/`putDataset`.
    - Test: an image file routes through the `FileReader.readAsDataURL` → `image_url` branch and is NOT sent to `parseFileInWorker`/`putDataset`.
  </behavior>
  <action>
    Create `src/features/chat/components/fileAttachHandler.ts` exporting a pure, testable `handleAttachedFile(file, deps)` where `deps = { assertFileWithinCaps, parseFileInWorker, putDataset, deriveIngestSummary, readImageDataUrl }` (dependency-injected so the test mocks them — no DOM/component required). It: (1) runs `assertFileWithinCaps(file)` and returns the rejection `{ ok:false, reason }` early on over-cap (no parse); (2) for images, calls `readImageDataUrl(file)` and returns an `image_url` attachment; (3) for non-images, runs `parseFileInWorker(kind, payload)` (off-thread, INGEST-02) → `putDataset(parsed)` → `deriveIngestSummary` and returns `{ handleId, summary, status }`. The strict order assertFileWithinCaps → parseFileInWorker → putDataset is the contract the WARNING-5 test pins.
    Create `src/features/chat/components/FileChip.tsx`: one chip rendering a lucide type icon (`FileSpreadsheet` csv/xlsx, `Braces` json/geojson, `FileText` text, `Image` image), the truncated filename (`truncate`), a compact stat line (UI-SPEC parse-summary copy per type), an `X` remove control (ghost `Button`, `h-8`), and a `Collapsible`/`Popover` expansion for the fuller per-type summary (no always-on grid — D-03). Status visuals per UI-SPEC Color table: parsing → `Progress`/spinner + caption; parsed → `text-foreground`; failed → `text-destructive` + the "Couldn't parse {filename}" copy; image-uncertain → amber badge; image-unsupported → `opacity-60` + hard-disabled tooltip.
    Create `src/features/chat/components/FileChipStrip.tsx` with a `{ files, onChange }` controlled contract (mirroring `ChatGeometryAttachment`): an `Attach file` trigger (`Paperclip` lucide, `variant` active when picker open) opening a hidden `<input type="file" multiple>`, AND native `onDragOver`/`onDrop` on the strip region showing the `Drop files to attach` dashed affordance (`bg-muted`). On attach/drop, delegate EACH file to `handleAttachedFile` (the extracted callback above) and render one `FileChip` per file with its returned status. The strip is absent until a file is attached (no empty placeholder, matching the geometry pattern).
  </action>
  <verify>
    <automated>bun test src/features/chat/components/fileAttachHandler.test.ts && bun run build && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `fileAttachHandler.ts` exports `handleAttachedFile`; `fileAttachHandler.test.ts` asserts the assertFileWithinCaps → parseFileInWorker → putDataset call ORDER and the over-cap short-circuit (WARNING 5).
    - `FileChipStrip.tsx` provides BOTH a button (`Paperclip`) and drag-and-drop attach (INGEST-01); one `FileChip` renders per file.
    - Non-image files parse via `parseFileInWorker` (off-thread) and are stored via `putDataset`; the chip shows the `deriveIngestSummary` stat line, expandable via Collapsible/Popover, no data grid (INGEST-05/D-03).
    - Image files encode to a data URL `image_url` part (INGEST-04); over-cap files show the "too large" copy (`assertFileWithinCaps`).
    - Copy strings + chip visual language + type icons match the UI-SPEC; uses only existing `src/components/ui/*` primitives (no @mapcn block).
    - `bun test ...fileAttachHandler.test.ts` green; `bun run build` + `bun run lint` clean.
  </acceptance_criteria>
  <done>Two attach paths work; the attach callback is an extracted, unit-tested pure function proving the three-step order; one chip per file with off-thread parse + expandable summary; images encode for send.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VisionGateControl (three-tier) + ChatPanel mount + send wiring + D-11 send-path invariant</name>
  <read_first>
    - src/features/chat/ChatPanel.tsx (mount row ~681 where `ChatGeometryAttachment` sits; `attachedGeometry` useState 146; request injection 229-240; amber idiom at 549/620)
    - src/features/chat/vision/detectVisionSupport.ts (VisionSupport union — Plan 04) + 03-04-SUMMARY (the contract)
    - src/features/chat/ingest/ingestStore.ts (toModelSummary — Plan 03; the `{handleId, summary}` shape the send composes)
    - .planning/phases/03-file-ingest-multimodal/03-UI-SPEC.md (Vision gate — three states enabled/amber-uncertain+Send-anyway/hard-disabled+tooltip; Copywriting — `Send anyway`, `{model} doesn't support images...`, `This model may not support images. Send anyway?`; Accessibility — disabled reason via Tooltip)
    - .planning/phases/03-file-ingest-multimodal/03-PATTERNS.md (VisionGateControl row lines 147-148 — No-Analog; borrow trigger-button + amber idiom, build three-state logic fresh)
    - .planning/phases/03-file-ingest-multimodal/03-CONTEXT.md (D-08, D-09, D-11)
  </read_first>
  <behavior>
    - Test (D-11 send-path invariant, BLOCKER 3): compose an outbound message via the exported send-composer with a parsed dataset attached (a `ParsedDataset` holding a multi-row `fullRows`); assert the serialized message content CONTAINS the dataset `handleId` AND the `summary` (sampled rows) but does NOT contain the `fullRows` array — deep-scan the serialized payload for any row beyond the sampled set (mirrors the store-level invariant in 03-03 but at the ChatPanel send composition).
    - Test: with `support === 'no-vision'` the composed payload contains NO `image_url` part for an attached image; with `'uncertain'` + no `Send anyway` it also contains none; with `'vision'` (or `'uncertain'` + Send-anyway) the `image_url` part is present.
  </behavior>
  <action>
    Create `src/features/chat/components/VisionGateControl.tsx` taking the `VisionSupport` result for the selected model and rendering three states (D-08): `'vision'` → image-send enabled normally; `'no-vision'` → hard-disabled with a `Tooltip` explaining `{model} doesn't support images, so they can't be sent.`; `'uncertain'` → affordance available with an amber warning badge (reuse the existing `text-amber-600 dark:text-amber-400` idiom) and an explicit `Send anyway` opt-in confirm (`This model may not support images. Send anyway?`). Expose the disabled reason via `Tooltip` for accessibility (not visual-only).
    In `ChatPanel.tsx`: add an `attachedFiles` (+ ingest-handle/image-part) state mirroring the `attachedGeometry` useState; mount `<FileChipStrip>` and `<VisionGateControl>` in the same `flex-wrap` row beside `<ChatGeometryAttachment>` (~681). Resolve `detectVisionSupport(providerConfig, selectedModelId)` for the selected model and pass it to `VisionGateControl`. Extract the send-composition into an exported pure helper (`composeOutboundContent({ text, attachedFiles, visionSupport, sendAnyway })`) so `ingestSendPath.test.ts` can call it without rendering: it includes each attached dataset as `{handleId, summary}` (from `toModelSummary` — NEVER `fullRows`) and includes attached `image_url` parts ONLY when support is `'vision'`, or when `'uncertain'` AND `sendAnyway`; `'no-vision'` blocks the image (never silent send — acceptance criterion #4). This is the SAME gate that governs `capture_map_snapshot` (D-09, wired in Plan 04).
    Create `src/features/chat/ingestSendPath.test.ts` exercising `composeOutboundContent` for the D-11 invariant (handleId+summary present, fullRows absent) and the three vision tiers' image inclusion/exclusion. This is the send-path assertion BLOCKER 3 requires (D-11 tested at composition, not only at the store).
  </action>
  <verify>
    <automated>bun run build && bun test src/features/chat/ingestSendPath.test.ts src/features/chat/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `VisionGateControl` renders all three tiers; `'no-vision'` hard-disables with a Tooltip reason; `'uncertain'` requires the `Send anyway` opt-in; `'vision'` enabled.
    - `ChatPanel` mounts `FileChipStrip` + `VisionGateControl` beside `ChatGeometryAttachment` (source grep); `attachedFiles` state added.
    - `ingestSendPath.test.ts` (BLOCKER 3) asserts the composed send contains `{handleId, summary}` and NOT `fullRows` (D-11 / success criterion #2), and that `image_url` is included only per the gate (never on `'no-vision'`).
    - Copy strings match UI-SPEC; amber idiom reused; `bun run build` + `bun test ingestSendPath.test.ts store.test.ts` green.
  </acceptance_criteria>
  <done>Three-tier vision gate governs both image paths; ChatPanel mounts the strip + gate; the send-path test proves datasets go as handle+summary (no fullRows) and images are never silently sent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — attach + drop + parse summary + place-on-map + three-tier vision</name>
  <read_first>
    - .planning/phases/03-file-ingest-multimodal/03-UI-SPEC.md (full interaction contract — the acceptance behaviors to verify)
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (Validation Architecture — Phase gate: attach + drop + parse + place + vision three-tier)
  </read_first>
  <what-built>
    The file-chip strip (button + drag-drop, one chip per file, parse-status + expandable stat line), the user-attached-image path, the three-tier vision-gate affordance, and the place-on-map flow (via the Plan 05 `place_dataset_features` + `batch_geocode` tools the AI calls), all mounted in `ChatPanel` beside the geometry attachment. Parsing is off-thread (no freeze).
  </what-built>
  <how-to-verify>
    1. Run `bun dev`, open the chat panel.
    2. Click `Attach file`, pick `src/features/chat/ingest/__fixtures__/messy.csv` — confirm a chip appears with rows × columns + detected coordinate column(s), expandable, no freeze.
    3. Drag-and-drop the `sample.geojson` fixture onto the input — confirm a chip with feature count + bbox.
    4. Attach a large file over the size cap — confirm the "too large" copy.
    5. Ask the AI to place the `messy.csv` rows on the map — confirm it calls `place_dataset_features` (Plan 05) and ALL rows appear (not just the sampled ones); rows with only a place-name column geocode via `batch_geocode` (bounded/throttled). This validates the 03-05 tools the send-path references.
    6. Attach an image while a confirmed-vision model is selected — send enabled. Switch to a confirmed-no-vision model (e.g. a non-vision Ollama model) — image-send hard-disabled with the tooltip. Select an uncertain model — confirm the amber badge + `Send anyway` opt-in; verify the image is NOT sent unless `Send anyway` is clicked.
    7. Confirm `capture_map_snapshot` obeys the same gate (D-09).
  </how-to-verify>
  <action>
    Present the running UI for verification of the attach paths, per-file parse summary, off-thread no-freeze behavior, the place-on-map flow (Plan 05 tools), and the three-tier vision gate (including never-silent-send). Capture any visual/copy/interaction deviations from the UI-SPEC for fix before sign-off.
  </action>
  <acceptance_criteria>
    - Both attach paths produce a chip; parse summary correct per type; UI never freezes on a large file.
    - Place-on-map places ALL rows of the dataset (Plan 05 `place_dataset_features`), geocoding place-name-only rows (`batch_geocode`).
    - Three-tier vision gate behaves per D-08 (enabled / hard-disabled+tooltip / uncertain+opt-in); image never silently sent to a non-vision model.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe deviations to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → UI/parse | Untrusted dropped/picked files enter the client |
| attached image → model | Image bytes potentially leaving the client toward the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Denial of Service | huge file dropped onto the strip | mitigate | `assertFileWithinCaps` enforced FIRST in `handleAttachedFile` before parse + the "too large" copy (Task 1, order pinned by `fileAttachHandler.test.ts`); off-thread parse keeps the UI responsive (Plan 02) |
| T-03-20 | Information Disclosure | image silently sent to a non-vision model | mitigate | `VisionGateControl` hard-disables `'no-vision'` and requires `Send anyway` opt-in for `'uncertain'` (Task 2/D-08); `ingestSendPath.test.ts` asserts no `image_url` on `'no-vision'`; never silent send (acceptance criterion #4) |
| T-03-21 | Information Disclosure | UI sending fullRows to the model | mitigate | Send path composes `{handleId, summary}` only (Task 2); `ingestSendPath.test.ts` (BLOCKER 3) asserts the composed payload excludes `fullRows`; fullRows live only in the ingest store (Plan 03 D-11 seam) |
| T-03-22 | Tampering | third-party UI registry block with network/eval | mitigate | No `@mapcn` block consumed this phase (UI-SPEC Registry Safety); only vendored `src/components/ui/*` used — vetting gate not triggered |

All threats mitigated.
</threat_model>

<verification>
- `bun test src/features/chat/components/fileAttachHandler.test.ts` green (attach order + over-cap short-circuit).
- `bun test src/features/chat/ingestSendPath.test.ts` green (D-11 send-path invariant: handle+summary, no fullRows; image gated).
- `bun run build` green (UI + worker chunk); `bun run lint` clean.
- `bun test src/features/chat/store.test.ts` green (send-path gating).
- Human-verify checkpoint passed (attach + drop + parse summary + place-on-map + three-tier vision).
</verification>

<success_criteria>
- Files attach via button AND drag-drop with one chip per file (INGEST-01); images attach as data URLs (INGEST-04).
- Parse summary visible per chip, expandable, no data grid (INGEST-05/D-03); off-thread, no freeze.
- The composed send carries the dataset as `{handleId, summary}` and never `fullRows` (D-11 / success criterion #2) — proven by `ingestSendPath.test.ts`.
- Three-tier vision gate governs both image paths; image never silently sent to a blind model (INGEST-07/D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/03-file-ingest-multimodal/03-06-SUMMARY.md` when done — record the FileChipStrip/VisionGateControl prop contracts, the `handleAttachedFile` + `composeOutboundContent` signatures, the ChatPanel send-path change (datasets as {handleId,summary}; image gated), and the UAT outcome.
</output>
</output>
