---
phase: 03-file-ingest-multimodal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - src/features/chat/ingest/types.ts
  - src/features/chat/ingest/ingest.worker.ts
  - src/features/chat/ingest/parse.test.ts
  - src/features/chat/ingest/__fixtures__/sample.xlsx
  - src/features/chat/ingest/__fixtures__/messy.csv
  - src/features/chat/ingest/__fixtures__/sample.geojson
  - src/features/chat/ingest/__fixtures__/sample.txt
  - test/build-emits-ingest-worker.test.ts
autonomous: false
requirements: [INGEST-02, INGEST-03]
must_haves:
  truths:
    - "ExcelJS parses a real .xlsx to rows inside the Bun-bundled worker (D-02 discretion: parse libs locked exceljs not SheetJS)"
    - "bun run build emits the ingest worker chunk to dist/ (Pitfall 3 build-emission gate)"
    - "papaparse + exceljs install from the npm registry (legitimacy-audited OK; SheetJS forbidden)"
  artifacts:
    - path: "src/features/chat/ingest/ingest.worker.ts"
      provides: "off-thread CSV/xlsx/json/text parse worker mirroring geoJsonParseWorker pattern"
      contains: "self.onmessage"
    - path: "src/features/chat/ingest/types.ts"
      provides: "IngestParseRequest/IngestParseResponse discriminated message shapes"
    - path: "src/features/chat/ingest/parse.test.ts"
      provides: "Wave-0 RED parse-correctness scaffold (INGEST-02/03)"
    - path: "test/build-emits-ingest-worker.test.ts"
      provides: "build-emission gate asserting the worker chunk lands in dist/"
  key_links:
    - from: "src/features/chat/ingest/ingest.worker.ts"
      to: "exceljs / papaparse"
      via: "import inside worker module"
      pattern: "import.*exceljs|import.*papaparse"
---

<objective>
Wave-0 de-risk + scaffold for the ingest pipeline. Install the two new parse libraries (papaparse, exceljs — legitimacy-audited OK), prove ExcelJS parses a real `.xlsx` cleanly inside a Bun-bundled Web Worker (RESEARCH Open Q1 / Assumption A1), and assert `bun run build` emits the ingest-worker chunk to `dist/` (Pitfall 3). Lay the RED test scaffold + fixtures every later plan's parse logic is verified against.

Purpose: The single highest-risk unknown in this phase is whether ExcelJS bundles cleanly into the worker under Bun (it pulls Node `fs`/`stream` paths). This plan resolves it BEFORE the rest of the Excel path is committed. If the spike fails, the fallback (`read-excel-file`, SUS-flagged) is gated behind a human-verify checkpoint here, not discovered mid-build later.

Output: Installed deps, `ingest.worker.ts` (parse branches), `types.ts` (message shapes), fixtures, a RED `parse.test.ts`, and a build-emission gate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-file-ingest-multimodal/03-RESEARCH.md
@.planning/phases/03-file-ingest-multimodal/03-PATTERNS.md
@src/lib/geo/geoJsonParseWorker.ts
@src/features/chat/tools/errors.test.ts
@build.ts
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- File: `src/features/chat/ingest/ingest.worker.ts` (worker module; `self.onmessage` handler)
- File: `src/features/chat/ingest/types.ts`
- Types: `IngestParseRequest`, `IngestParseResponse`, `IngestKind` (`'csv' | 'xlsx' | 'json' | 'geojson' | 'text'`)
- File: `src/features/chat/ingest/parse.test.ts`
- File: `test/build-emits-ingest-worker.test.ts`
- Fixtures: `src/features/chat/ingest/__fixtures__/{sample.xlsx, messy.csv, sample.geojson, sample.txt}`
- npm deps: `papaparse`, `exceljs`, `@types/papaparse`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate + install papaparse + exceljs</name>
  <read_first>
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (## Package Legitimacy Audit table; ## Standard Stack install block)
    - package.json (confirm only @turf/turf present; papaparse/exceljs absent)
  </read_first>
  <what-built>
    The RESEARCH Package Legitimacy Audit already verified `papaparse@^5.5.3` (11.78M/wk, github.com/mholt/PapaParse, no postinstall) and `exceljs@^4.4.0` (9.66M/wk, github.com/exceljs/exceljs, no postinstall) as OK, and FORBIDS SheetJS `xlsx` (npm-frozen 2022 + ReDoS through 0.20.1). Both are `OK`, not `[ASSUMED]`/`[SUS]`, but a package-install legitimacy checkpoint is mandatory and never auto-approvable.
  </what-built>
  <how-to-verify>
    1. Confirm `papaparse` at npmjs.com/package/papaparse (publisher, repo github.com/mholt/PapaParse, recent maintenance).
    2. Confirm `exceljs` at npmjs.com/package/exceljs (repo github.com/exceljs/exceljs, maintained).
    3. Confirm you are NOT installing SheetJS `xlsx` from npm (forbidden — stale + ReDoS).
    4. After approval, run: `bun add papaparse exceljs && bun add -d @types/papaparse`
  </how-to-verify>
  <action>
    Present the two packages for legitimacy confirmation against the RESEARCH audit table. On approval, run `bun add papaparse exceljs` then `bun add -d @types/papaparse`. Do NOT install SheetJS `xlsx`. Do NOT install `read-excel-file` here (that is the spike-failure fallback in Task 2, gated separately).
  </action>
  <acceptance_criteria>
    - `package.json` lists `papaparse` (^5.5.3), `exceljs` (^4.4.0) in dependencies and `@types/papaparse` in devDependencies.
    - `package.json` does NOT contain `xlsx` (SheetJS) or `read-excel-file`.
    - `bun install` completes (lockfile updated).
  </acceptance_criteria>
  <resume-signal>Type "approved" after verifying both packages, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: ExcelJS-in-worker spike — message types + worker parse branches + build-emission gate</name>
  <read_first>
    - src/lib/geo/geoJsonParseWorker.ts (EXACT worker analog — `self.onmessage`, discriminated `{id,success,data?,error?}` response, try/catch never throws out)
    - src/lib/geo/workerJsonParse.ts (the `new Worker(new URL('./x.ts', import.meta.url),{type:'module'})` form Bun bundles zero-config)
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (Pattern 1 lines 218-229; Pitfall 2 ExcelJS browser API `wb.xlsx.load(arrayBuffer)` NOT the Node stream reader; Pitfall 3 build-emission; Code Examples lines 363-385)
    - .planning/phases/03-file-ingest-multimodal/03-PATTERNS.md (ingest.worker.ts row + types.ts row)
    - build.ts (entrypoint enumeration `Bun.Glob("**.html")` at line 166 — worker chunks emit as deps of `new Worker(new URL(...))`)
  </read_first>
  <action>
    Create `src/features/chat/ingest/types.ts` exporting `IngestKind` (`'csv'|'xlsx'|'json'|'geojson'|'text'`), `IngestParseRequest` (`{ id: string; kind: IngestKind; text?: string; buffer?: ArrayBuffer }`), and `IngestParseResponse` (`{ id: string; success: boolean; rows?: Record<string, unknown>[]; schemaFields?: string[]; data?: unknown; error?: string }`) — mirror the discriminated shape of `geoJsonParseWorker.ts`'s `ParseRequest`/`ParseResponse`.
    Create `src/features/chat/ingest/ingest.worker.ts`: a `self.onmessage` handler that branches on `kind`: `csv` → PapaParse `Papa.parse(text, { header: true, dynamicTyping: true, skipEmptyLines: true })` returning `result.data` as rows and `result.meta.fields` as `schemaFields`; `xlsx` → `new ExcelJS.Workbook()` + `await wb.xlsx.load(buffer)` then iterate `wb.worksheets[0].eachRow` building `Record<string,unknown>[]` (header from row 1, per RESEARCH Code Examples); `json`/`geojson` → `JSON.parse(text)` into `data`; `text` → split into lines, return line/char metadata in `data`. Wrap every branch in try/catch and post `{ success: false, error }` — NEVER throw out of the handler (mirror geoJsonParseWorker exactly).
    Create `test/build-emits-ingest-worker.test.ts`: run `bun run build` (or import build.ts's build entry), then assert a chunk whose name/content references the ingest worker exists in `dist/` (grep dist for the worker filename or an ExcelJS/PapaParse marker). This is the Pitfall-3 gate.
  </action>
  <verify>
    <automated>bun run build && bun test test/build-emits-ingest-worker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/chat/ingest/types.ts` exports `IngestKind`, `IngestParseRequest`, `IngestParseResponse`.
    - `src/features/chat/ingest/ingest.worker.ts` imports `exceljs` and `papaparse`, handles all five `kind` branches, and converts every error to `{ success: false, error }` (no throw escapes `self.onmessage`).
    - `bun run build` exits 0 AND emits an ingest-worker chunk to `dist/` (asserted by `test/build-emits-ingest-worker.test.ts`).
    - SPIKE GATE: if `bun run build` fails on ExcelJS Node-shim/`fs`/`stream` errors, STOP and escalate via Task 3 (do not silently fall to sync).
  </acceptance_criteria>
  <done>The worker parses all five kinds, ExcelJS loads xlsx from an ArrayBuffer in-worker, and the build emits the worker chunk. Spike resolved GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Spike-failure fallback gate (only if Task 2 build fails on ExcelJS)</name>
  <read_first>
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (Alternatives Considered — `read-excel-file@9.2.0`, SUS:too-new false-positive; Open Q1 fallback)
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (Package Legitimacy Audit — `read-excel-file` row, `checkpoint:human-verify` before install)
  </read_first>
  <what-built>
    CONDITIONAL — execute ONLY if Task 2's `bun run build` fails because ExcelJS pulls Node-only `fs`/`stream` into the worker bundle. `read-excel-file@9.2.0` (836k/wk, gitlab.com/catamphetamine/read-excel-file) is the lighter browser-first fallback; it was flagged `SUS:too-new` (false positive — recent version on a long-lived package).
  </what-built>
  <how-to-verify>
    1. Confirm `read-excel-file` at npmjs.com/package/read-excel-file (publisher, repo gitlab.com/catamphetamine/read-excel-file, download history).
    2. Confirm the SUS flag is the false-positive `too-new` (long-lived package, recent version bump).
    3. On approval: `bun add read-excel-file`, swap the xlsx branch in `ingest.worker.ts` to `readXlsxFile(buffer)`, re-run the build gate.
  </how-to-verify>
  <action>
    If and only if Task 2's build failed on ExcelJS, present `read-excel-file` for legitimacy confirmation. On approval install it and replace the xlsx parse branch in `ingest.worker.ts`; re-run `bun run build && bun test test/build-emits-ingest-worker.test.ts`. If Task 2 passed GREEN, mark this task NOT NEEDED and skip.
  </action>
  <acceptance_criteria>
    - If executed: `read-excel-file` installed, xlsx branch swapped, build-emission gate green.
    - If skipped: Task 2 build is green with exceljs; record "spike passed, fallback not needed".
  </acceptance_criteria>
  <resume-signal>Type "approved" to install the fallback, or "skip" if Task 2 passed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Parse-correctness RED scaffold + fixtures</name>
  <read_first>
    - src/features/chat/tools/errors.test.ts (colocated `bun test` style — `describe`/`it`/`expect` from `bun:test`)
    - .planning/phases/03-file-ingest-multimodal/03-RESEARCH.md (## Validation Architecture — Wave 0 Gaps; Phase Requirements → Test Map INGEST-02/03 rows)
    - src/features/chat/ingest/ingest.worker.ts (the parse branches under test, from Task 2)
  </read_first>
  <action>
    Create the four fixtures under `src/features/chat/ingest/__fixtures__/`: `messy.csv` (quoted fields, an embedded newline, a `lat`+`lon` column AND a `place` place-name column), `sample.geojson` (a small FeatureCollection), `sample.txt` (a few lines of plain text), and a small real `sample.xlsx` (generate it with exceljs in a one-off `bun` script or commit a tiny binary — header row + 3 data rows). Create `src/features/chat/ingest/parse.test.ts` with `bun:test` covering: CSV → rows + `schemaFields` (quoted/embedded-newline survives), xlsx → rows from the worker parse path (invoke the worker's parse logic directly or via a small synchronous parse helper extracted from the worker), JSON/GeoJSON → parsed object, text → line/char count. These are the RED scaffold the Wave-1 parse client (Plan 02) makes GREEN; assert real behavior, not `MISSING`.
  </action>
  <verify>
    <automated>bun test src/features/chat/ingest/parse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/chat/ingest/__fixtures__/` contains `messy.csv`, `sample.xlsx`, `sample.geojson`, `sample.txt`.
    - `messy.csv` has a quoted field with an embedded newline, a `lat`+`lon` pair, and a `place` column (drives Plan 05 coord-detect + geocode tests).
    - `src/features/chat/ingest/parse.test.ts` runs under `bun test` and asserts CSV/xlsx/json/geojson/text parse correctness (passing against Task 2's worker parse, or RED-pending the Plan 02 client if the worker logic is not yet directly importable — document which).
  </acceptance_criteria>
  <done>Fixtures exist; `parse.test.ts` exercises all five parse kinds and is wired to the worker parse logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → worker | Untrusted file bytes (CSV text / xlsx ArrayBuffer) cross into the parse worker |
| npm registry → build | Third-party parse libraries enter the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | ExcelJS worker parse of malformed/zip-bomb xlsx | mitigate | Worker try/catch converts any parse error to `{success:false}` (Task 2) — never crashes the host; size guard added in Plan 03 before this worker is called |
| T-03-02 | Tampering | npm `papaparse`/`exceljs` install (supply chain) | mitigate | Package legitimacy gate (Task 1, blocking-human) verifies against RESEARCH audit; no postinstall scripts; SheetJS forbidden (ReDoS) |
| T-03-SC | Tampering | npm/bun installs | mitigate | T-03-02 blocking-human checkpoint; `read-excel-file` (SUS) install gated separately (Task 3) and only on spike failure |

All threats this plan owns are dispositioned mitigate; none accepted.
</threat_model>

<verification>
- `bun add papaparse exceljs` + `bun add -d @types/papaparse` recorded in package.json; no SheetJS.
- `bun run build` green AND emits the ingest-worker chunk (Pitfall-3 gate).
- `bun test src/features/chat/ingest/parse.test.ts` green.
- `bun run lint` clean on new files.
</verification>

<success_criteria>
- ExcelJS-in-worker spike resolved (build green + worker chunk emitted), or fallback installed via the Task 3 human-verify gate.
- Parse libs installed and legitimacy-gated; SheetJS NOT used.
- RED parse scaffold + four fixtures exist for Wave-1 to build against.
</success_criteria>

<output>
Create `.planning/phases/03-file-ingest-multimodal/03-01-SUMMARY.md` when done — record the spike outcome (exceljs passed vs read-excel-file fallback), the chosen file-size cap rationale handoff to Plan 03, and the exact dist worker chunk name.
</output>
