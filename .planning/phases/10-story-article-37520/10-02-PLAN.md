---
phase: 10-story-article-37520
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/info-panel/StoryEditorPanel.tsx
  - src/components/StoriesPanel.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03, STORY-04]
must_haves:
  truths:
    - "A user can enter a title, summary, cover image, and a Markdown body for a Story (NIP-23 metadata)"
    - "A user can insert inline nostr:naddr… references and image/video embeds into the body via the @-mention picker (STORY-02 insert half)"
    - "A user can save a Story as a local draft before publishing and re-open it pre-filled"
    - "Editing a published Story opens pre-filled and submits Save changes (same d-tag), not a new publish"
    - "The Stories browse panel lists kind-37520 Stories with a New Story button at the top"
  artifacts:
    - path: "src/components/info-panel/StoryEditorPanel.tsx"
      provides: "create/edit Story authoring panel (Write/Preview tabs, metadata block, draft, in-place edit)"
      exports: ["StoryEditorPanel"]
      min_lines: 120
    - path: "src/components/StoriesPanel.tsx"
      provides: "Stories rail browse panel (StoriesPanelContent) + New Story entry"
      exports: ["StoriesPanelContent"]
      min_lines: 80
  key_links:
    - from: "src/components/info-panel/StoryEditorPanel.tsx"
      to: "src/lib/nostr/story/lifecycle.ts"
      via: "publishStory / editStory"
      pattern: "publishStory|editStory"
    - from: "src/components/info-panel/StoryEditorPanel.tsx"
      to: "src/components/editor/GeoRichTextEditor"
      via: "GeoRichTextEditor body authoring (markdown serialize)"
      pattern: "GeoRichTextEditor"
    - from: "src/components/StoriesPanel.tsx"
      to: "src/lib/hooks/useStories.ts"
      via: "useStories subscription"
      pattern: "useStories"
---

<objective>
Build the Story authoring surface: `StoryEditorPanel` (create + in-place edit, copied
structurally from `GroupEditorPanel`) and `StoriesPanel` (the rail browse panel, copied
from `GeoDatasetsPanelContent`) with the **New Story** create entry at the top. The editor
reuses the TipTap `GeoRichTextEditor` for the Markdown body (with a Write/Preview tab pair),
the `@`-mention picker for geo-ref/media insertion (STORY-02 insert half), the
`BlossomUploaderButton` for the cover image, and the Plan-01 `publishStory`/`editStory`
service for the publish/edit/draft path (STORY-03/04). No new infrastructure — every piece
has a shipped analog.

Purpose: Deliver STORY-01 (title/summary/cover/body), the STORY-02 insert half, the
STORY-04 draft + in-place-edit authoring UX, and the wired Stories browse destination's
panel body, all by structural reuse.
Output: `StoryEditorPanel.tsx`, `StoriesPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-story-article-37520/10-PATTERNS.md
@.planning/phases/10-story-article-37520/10-UI-SPEC.md
@.planning/phases/10-story-article-37520/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StoryEditorPanel — create/edit authoring panel with Write/Preview, metadata, draft, in-place edit (STORY-01/02/03/04)</name>
  <files>src/components/info-panel/StoryEditorPanel.tsx</files>
  <read_first>
    - src/features/groups/GroupEditorPanel.tsx (FULL — the exact structural analog: imports :27-87, handleSave create-vs-modify branch :342-434, EntityPanelShell/Surface/SectionHeader chrome :436+, GeoRichTextEditor body :456+, save-error/isSaving/try-catch-finally pattern, accent submit button rounded-none bg-primary :805-811)
    - src/lib/nostr/story/lifecycle.ts (publishStory/editStory from Plan 01 — the publish path to call instead of inlining ArticleFactory)
    - src/lib/nostr/story/draft.ts (readStoryDraft/writeStoryDraft/clearStoryDraft — the Save draft / pre-fill / clear-on-publish path)
    - src/lib/nostr/article/{helpers,cast}.ts (ArticleContent {title,summary,image,content}; Article cast; isArticle; getArticleContent for pre-fill from initialStory.rawEvent())
    - src/components/editor/GeoRichTextEditor.tsx (GeoRichTextEditor, GeoRichTextEditorRef, GeoFeatureItem — onChange yields serialized Markdown; the @-mention/GeoMentionExtension/MediaExtensions are built in)
    - src/components/editor/RichContentRenderer.tsx:8-19 (the Preview-tab render contract — content + availableFeatures + onMentionVisibilityToggle/onMentionZoomTo)
    - src/components/info-panel/EntityPanelShell.tsx (EntityPanelShell/EntityPanelSurface/EntityPanelSectionHeader props)
    - src/components/blossom/BlossomUploaderButton.tsx (cover image upload affordance signature — reuse as in GroupEditorPanel image field)
    - 10-UI-SPEC.md §3 (metadata block + Write/Preview tabs + @-mention insertion) and §5 (draft + in-place edit) and Copywriting Contract (Publish Story / Save changes / Save draft / Publishing… copy; accent reserved for submit only)
  </read_first>
  <action>
    Create `StoryEditorPanel({ initialStory?: Article, onClose, onSave, availableFeatures })`.
    Copy `GroupEditorPanel` wholesale, substituting Article for Group. State: `title` (Input),
    `summary` (Textarea), `image` (BlossomUploaderButton with a 16:9 aspect-ratio preview),
    `body` (GeoRichTextEditor, onChange → markdown string), plus `saveError`, `isSaving`.
    Pre-fill from `initialStory ? getArticleContent(initialStory.rawEvent())` else from
    `readStoryDraft(initialStory?.dTag ?? 'new-story')`. Wrap the body in a `Tabs` with
    `TabsTrigger` values **Write** (default, the GeoRichTextEditor) and **Preview**
    (a `RichContentRenderer content={body}` exactly as readers see it — DO NOT add raw HTML).
    Validation copy from UI-SPEC: empty title → "A title is required to publish.", empty body →
    "Add some narrative before publishing." Buttons: accent **Publish Story** (create) /
    **Save changes** (edit, when `initialStory` is a published Article) using
    `className="rounded-none bg-primary text-primary-foreground"`; a neutral **Save draft**
    that calls `writeStoryDraft`. handleSave: build `ArticleContent` from the fields, call
    `editStory(initialStory.rawEvent(), content, signer)` when editing (isArticle true) else
    `publishStory(content, signer)` (both from Plan 01 — these do the STORY-03 a-derive +
    STORY-04 lineage internally), then `clearStoryDraft(dTag)`, `castEvent(signed, Article,
    eventStore)`, `onSave(cast)`, `onClose()`. Wrap in try/catch with
    "Couldn't publish — check your connection and try again." and finally setIsSaving(false).
    A **Discard draft** action gated by an alert-dialog ("Discard this draft?" copy) clears
    the draft. NO `dangerouslySetInnerHTML` anywhere.
  </action>
  <behavior>
    - Rendering StoryEditorPanel with no initialStory shows empty Title/Summary/Cover/body and a **Publish Story** submit button
    - Rendering with an initialStory (published Article) pre-fills Title/Summary/body from getArticleContent and the submit reads **Save changes**
    - The body section exposes Write and Preview tabs; Preview renders through RichContentRenderer (no raw HTML node)
    - Attempting publish with empty title sets the error "A title is required to publish." and does not call publishStory
  </behavior>
  <verify>
    <automated>bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "publishStory\|editStory" src/components/info-panel/StoryEditorPanel.tsx` returns ≥ 2 (uses the Plan-01 service, does NOT re-inline ArticleFactory publish)
    - `grep -c "GeoRichTextEditor" src/components/info-panel/StoryEditorPanel.tsx` returns ≥ 1 (reuses the TipTap body editor)
    - `grep -c "dangerouslySetInnerHTML" src/components/info-panel/StoryEditorPanel.tsx` returns 0 (XSS posture — Preview renders only via RichContentRenderer)
    - `grep -c "TabsTrigger\|TabsContent" src/components/info-panel/StoryEditorPanel.tsx` returns ≥ 2 (Write/Preview pair)
    - `grep -c "bg-primary" src/components/info-panel/StoryEditorPanel.tsx` returns ≥ 1 (accent reserved for the submit button per UI-SPEC color contract)
    - The strings "Publish Story", "Save changes", "Save draft", "A title is required to publish." all appear verbatim (UI-SPEC copy)
    - `bun run build` exits 0; `bun run lint src/components/info-panel/StoryEditorPanel.tsx` reports no errors
  </acceptance_criteria>
  <done>StoryEditorPanel creates and in-place-edits Stories via the Plan-01 service, with Write/Preview tabs, metadata block, draft save/discard, and the reserved-accent submit; build green, no raw HTML.</done>
</task>

<task type="auto">
  <name>Task 2: StoriesPanel browse list + New Story entry (D-01/D-02 panel body, STORY-04 status chips)</name>
  <files>src/components/StoriesPanel.tsx</files>
  <read_first>
    - src/components/GeoDatasetsPanel.tsx (the GeoDatasetsPanelContent analog: useFilterState :125, useSortedFilteredItems :148-152, EntitySearchToolbar header :467-477, the create-button block :448-462 showing the `onCreateContext` prop + Button placement, the list-row rendering with star/visibility/overflow)
    - src/lib/hooks/useStories.ts (Plan 01 — the kind-37520 subscription to drive the list)
    - src/lib/nostr/article/cast.ts + helpers.ts (Article cast getters: dTag, pubkey, created_at, article {title,summary,image,content}; isArticle)
    - src/lib/nostr/story/draft.ts (readStoryDraft — to detect/mark a local Draft vs a published Story for the status chip)
    - 10-UI-SPEC.md §1 Stories browse rail (list-row anatomy: cover thumb/placeholder + title 14px semibold + summary one-liner muted + author/date 11px + Draft/Published badge + ⋮ dropdown-menu; skeleton rows), Copywriting Contract (New Story button copy + empty-state copy "No stories yet" / "No stories match"), Color (New Story = accent bg-primary; Draft = Badge variant="outline", Published = Badge variant="secondary")
  </read_first>
  <action>
    Create `StoriesPanelContent({ currentUserPubkey, onOpenStory, onCreateStory, onEditStory,
    onDeleteStory, deletingKey })`. Subscribe via `useStories()`, feed the casts through the
    same `useFilterState` + `useSortedFilteredItems` hooks GeoDatasetsPanelContent uses. Render
    the `EntitySearchToolbar` header. Place an accent **New Story** Button
    (`className="rounded-none bg-primary text-primary-foreground"`) at the TOP of the panel
    (mirrors the contexts create-button block), calling `onCreateStory`. Each list row (a
    `Card`, rounded-none): cover thumbnail (`aspect-ratio` 16:9, neutral placeholder on
    missing/error) + title (text-sm semibold) + summary one-liner (text-[13px] muted-foreground,
    truncated) + author/date meta (text-[11px]) + a status `Badge` (Published →
    variant="secondary"; a story with a local draft → variant="outline" labeled **Draft**) +
    a ⋮ `dropdown-menu` (Open / Edit / Copy link / Delete). Row click → `onOpenStory(story)`.
    Loading shows `skeleton` rows; empty shows the `empty` component with the UI-SPEC headings
    ("No stories yet" / "No stories match"). DO NOT wire the rail destination here — that is
    Plan 03; this plan only delivers the panel body component + its props contract.
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useStories" src/components/StoriesPanel.tsx` returns ≥ 1
    - `grep -c "useFilterState\|useSortedFilteredItems" src/components/StoriesPanel.tsx` returns ≥ 2 (reuses the dataset browse filter/sort hooks)
    - The string "New Story" appears verbatim AND a Button near it carries `bg-primary` (accent create entry per D-02 + color contract)
    - `grep -c "DropdownMenu\|dropdown-menu" src/components/StoriesPanel.tsx` returns ≥ 1 (the ⋮ overflow)
    - The strings "No stories yet" and "No stories match" appear verbatim (empty-state copy)
    - `export function StoriesPanelContent` is present
    - `bun run build` exits 0; `bun run lint src/components/StoriesPanel.tsx` reports no errors
  </acceptance_criteria>
  <done>StoriesPanelContent lists kind-37520 Stories with search/filter, an accent New Story entry, status chips, ⋮ overflow, skeleton/empty states; build green. Rail wiring deferred to Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author keystrokes → Markdown body | The body the author types is the content that will be published and later rendered |
| relay → StoriesPanel list rows | Untrusted kind-37520 events render as list rows (title/summary/cover thumbnail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Elevation of Privilege (XSS) | StoryEditorPanel Preview tab | mitigate | The Preview tab renders the Markdown body ONLY through the sanitized `RichContentRenderer` (no `dangerouslySetInnerHTML`, no raw HTML) — same sanitized path readers see; asserted by grep gate returning 0 dangerouslySetInnerHTML |
| T-10-05 | Elevation of Privilege (XSS) | StoriesPanel list-row cover image / title / summary | mitigate | Title/summary render as React text nodes (auto-escaped); cover image is an `<img src>` (no script execution) with onError → neutral placeholder; no HTML injection sink |
| T-10-06 | Denial of Service | StoriesPanel rendering relay events | mitigate | The list source is `useStories()` which `isArticle`-filters before cast (Plan 01) — malformed/legacy kind-37520 events never reach the row map |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced this plan (all components in src/components/ui/ already installed per UI-SPEC Registry Safety "zero new installs"); no @mapcn block consumed |
</threat_model>

<verification>
- `bun run build` exits 0
- `bun run lint` clean on both new files
- `bun test` shows no new failures vs baseline (663/0)
- No `dangerouslySetInnerHTML` introduced (XSS posture, grep gate)
</verification>

<success_criteria>
- STORY-01: title/summary/cover/Markdown-body authoring works
- STORY-02 (insert half): @-mention picker inserts inline geo/media refs into the body
- STORY-03/04 (UI): publish re-derives `a` (via Plan-01 service); edit preserves d; draft save/discard
- Stories browse panel body ready for the rail destination wiring in Plan 03
</success_criteria>

<output>
Create `.planning/phases/10-story-article-37520/10-02-SUMMARY.md` when done
</output>
