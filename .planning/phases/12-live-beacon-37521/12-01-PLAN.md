---
phase: 12-live-beacon-37521
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/nostr/live-beacon/lifecycle.test.ts
  - src/lib/nostr/live-beacon/visibility.test.ts
  - src/lib/nostr/live-beacon/relay-echo.test.ts
  - src/lib/hooks/useBeacons.test.ts
  - src/features/geo-editor/hooks/useBeaconPublisher.test.ts
  - src/lib/og/fetchBeacon.test.ts
  - src/test/geolocationMock.ts
autonomous: true
requirements: [BEACON-01, BEACON-02, BEACON-03, BEACON-04]
user_setup: []

must_haves:
  truths:
    - "A failing (RED) test asserts updateBeacon derives bbox/g from content.geometry for a PUBLIC beacon and preserves `d` on republish (BEACON-01, D-09)"
    - "A failing test asserts stopBeacon publishes status:'ended' keeping the NIP-40 expiration and the same `d` (BEACON-02, D-04)"
    - "A failing test asserts a PUBLIC beacon emits a t:'live' marker + geo tags while a LINK-ONLY beacon omits both (BEACON-04, D-10)"
    - "A failing test asserts useBeacons filters isLiveBeacon BEFORE cast, dropExpired at the subscription, and derives beaconState live/stale/ended/removed at the threshold boundary (BEACON-03, D-07/D-08)"
    - "A failing test asserts useBeaconPublisher publishes only when moved >= distance floor OR >= heartbeat (single lastPublishedAt guard) and mints a fresh per-session key never written to storage (BEACON-01, D-01/D-02/D-05)"
    - "A failing test asserts fetchBeaconOGData decodes a throwaway-pubkey naddr, fetches latest-by-created_at, and returns null for an expired beacon (BEACON-04, D-11)"
    - "A reusable navigator.geolocation/watchPosition mock fixture exists (none existed before)"
  artifacts:
    - path: "src/lib/nostr/live-beacon/lifecycle.test.ts"
      provides: "BEACON-01/02 RED stubs — publishBeacon/updateBeacon/stopBeacon derive/preserve-d/ended"
      contains: "updateBeacon"
    - path: "src/lib/nostr/live-beacon/visibility.test.ts"
      provides: "BEACON-04 RED stub — public vs link-only discovery marker/geo-tag emission"
      contains: "live"
    - path: "src/lib/nostr/live-beacon/relay-echo.test.ts"
      provides: "BEACON-02 integration stub — relay latest-wins + lazy-GC + client dropExpired against bun relay"
      contains: "dropExpired"
    - path: "src/lib/hooks/useBeacons.test.ts"
      provides: "BEACON-03 RED stub — filter-before-cast, dropExpired, beaconState derivation"
      contains: "beaconState"
    - path: "src/features/geo-editor/hooks/useBeaconPublisher.test.ts"
      provides: "BEACON-01/D-05 RED stub — throttle (distance+time floor), fresh per-session key never persisted"
      contains: "BEACON_HEARTBEAT_MS"
    - path: "src/lib/og/fetchBeacon.test.ts"
      provides: "BEACON-04 RED stub — naddr round-trip + expiry guard"
      contains: "fetchBeaconOGData"
    - path: "src/test/geolocationMock.ts"
      provides: "Reusable navigator.geolocation watchPosition mock fixture"
      contains: "watchPosition"
  key_links:
    - from: "lifecycle.test.ts"
      to: "src/lib/nostr/live-beacon (Plan 02 lifecycle service)"
      via: "imports the not-yet-existing updateBeacon/stopBeacon"
      pattern: "from '@/lib/nostr/live-beacon'"
    - from: "useBeaconPublisher.test.ts"
      to: "src/test/geolocationMock.ts + Plan 03 useBeaconPublisher"
      via: "imports the geolocation mock + the not-yet-existing throttle hook"
      pattern: "useBeaconPublisher"
---

<objective>
Establish the Nyquist Wave-0 RED baseline for Phase 12 (Live Beacon, kind 37521). Create the test stubs that pin every net-new and extended seam's contract BEFORE implementation — the lifecycle derive/preserve-d/ended behavior (BEACON-01/02), the public-vs-link-only discovery-gating (BEACON-04, D-10), the relay latest-wins + lazy-GC + client-drop echo test (BEACON-02), the useBeacons filter-before-cast + dropExpired + beaconState derivation (BEACON-03), the throttled publish loop + fresh-per-session unlinkable throwaway key (BEACON-01, D-05), and the naddr/OG fetch (BEACON-04, D-11) — plus the missing reusable navigator.geolocation/watchPosition mock fixture. These tests MUST be RED now and turn GREEN as Plans 02–05 land.

Purpose: This phase's three genuinely net-new subsystems (the throttled publish loop + throwaway signer, the beaconState live/stale/ended/removed marker, the public/link-only discovery-gating) are exactly where regressions and the privacy footguns hide. Pinning them as failing tests first forces the implementations to satisfy a fixed contract — especially the epoch-seconds clock (Pitfall P-1), filter-before-cast (P-2), and the link-only-not-discoverable invariant (P-6).
Output: 6 new test files + 1 shared geolocation mock fixture — RED against the current tree (the relay-echo test is an integration test that runs against `bun relay`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-beacon-37521/12-CONTEXT.md
@.planning/phases/12-live-beacon-37521/12-RESEARCH.md
@.planning/phases/12-live-beacon-37521/12-PATTERNS.md
@.planning/phases/12-live-beacon-37521/12-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle + visibility + relay-echo RED stubs + geolocation mock fixture</name>
  <read_first>
    - src/lib/nostr/live-beacon/live-beacon.test.ts (the existing scaffold contract — mirror its describe/import shape)
    - src/lib/nostr/live-beacon/factory.ts (create/modify/beacon/expiration/bbox/geohash/hashtags setters — the seams the lifecycle will chain)
    - src/lib/nostr/live-beacon/helpers.ts (LiveBeaconContent — note position? placeholder to be replaced by geometry/status in Plan 02)
    - src/lib/nostr/temporal-sighting/lifecycle.ts (THE clone template — deriveBbox/deriveCentroid try/catch, publishSighting/editSighting, attachStore, mock-friendly publish import)
    - src/lib/nostr/temporal-sighting/temporal-sighting.test.ts (the analog lifecycle-test shape: mock.module('@/lib/nostr', ...) so no live relay is hit, castEvent round-trip)
    - src/lib/nostr/expiry.ts (isExpired/dropExpired — UTC epoch-seconds semantics; never Date.now() ms)
    - src/lib/nostr/tags.ts (setHashtags/setBbox/setGeohash — the t:'live' marker + geo tags the visibility branch toggles)
    - scripts/seed-entities.ts:424-443 (current beacon fixture shape, to be updated in Plan 04)
    - src/components/ui/map.tsx:912-949 (watchPosition success/error/options shape the mock fixture must emulate)
  </read_first>
  <behavior>
    - lifecycle.test.ts: updateBeacon({ content:{ geometry:Point[lon,lat], status:'live' }, expiration, visibility:'public' }, signer) signs a 37521 carrying a `bbox` tag + a `g` tag derived (via turf) from the geometry AND a `t` tag with value 'live' (the D-10 public discovery marker). Round-trip: castEvent(signed, LiveBeacon) → `.status === 'live'` and `.geometry` deep-equals the input Point.
    - lifecycle.test.ts: updateBeacon called twice with the SAME session `d` (modify path) preserves the original `d` tag value (no lineage fork) and bumps `created_at`.
    - lifecycle.test.ts: stopBeacon(session, signer) signs a final 37521 with `content.status === 'ended'`, the SAME `d`, AND keeps the NIP-40 `expiration` tag (BEACON-02 / D-04 — not a silent disappearance).
    - visibility.test.ts: a PUBLIC beacon emits both a `t:['live']` tag AND geo tags (`g`/`bbox`); a LINK-ONLY beacon emits NEITHER `t:'live'` NOR `g`/`bbox` (D-10 / Pitfall P-6). A discovery filter `{ kinds:[37521], '#t':['live'] }` MATCHES the public event and does NOT match the link-only event.
    - relay-echo.test.ts (integration, against `bun relay`): publish a 37521 (same `d`) twice with increasing `created_at`; a re-query `{kinds:[37521],authors:[pubkey],'#d':[d]}` returns exactly ONE event (the newer — latest-wins). Publish one with a short expiration; after expiry, assert the CLIENT `dropExpired(events, unixNow())` hides it regardless of whether the relay still returns it (relay GC lazy/untrusted — SPEC-05).
  </behavior>
  <action>Create src/test/geolocationMock.ts exporting an installable mock that replaces `navigator.geolocation` with a controllable `watchPosition`/`clearWatch`/`getCurrentPosition` (a controller that lets a test `emitFix({longitude,latitude,accuracy})`, `emitError({code})`, and assert clearWatch was called) — match the success/error/options shape at map.tsx:920-948; expose `PERMISSION_DENIED`/`POSITION_UNAVAILABLE`/`TIMEOUT` error codes. Create src/lib/nostr/live-beacon/lifecycle.test.ts importing `updateBeacon`/`stopBeacon` from '@/lib/nostr/live-beacon' (Plan-02 symbols, NOT yet existing) and assert the behavior block — mock the publish call via `mock.module('@/lib/nostr', ...)` exactly as temporal-sighting/temporal-sighting.test.ts does so no live relay is hit; cast via `castEvent(signed, LiveBeacon, undefined as never)`. Create src/lib/nostr/live-beacon/visibility.test.ts asserting the public/link-only tag emission + the `#t:['live']` discovery-filter match/no-match (reference whatever visibility entry Plan 02 exposes — `updateBeacon` with a `visibility:'public'|'link-only'` option). Create src/lib/nostr/live-beacon/relay-echo.test.ts as a `bun relay`-backed integration test (mirror any existing relay integration test shape in the repo if present; otherwise publish via the real `publish` + a generated signer and re-query the relay pool) asserting latest-wins + client dropExpired. These reference Plan-02 symbols that do not exist yet — lifecycle.test.ts + visibility.test.ts MUST be RED. Do NOT stub the implementations.</action>
  <verify>
    <automated>bun test src/lib/nostr/live-beacon/lifecycle.test.ts src/lib/nostr/live-beacon/visibility.test.ts 2>&1 | grep -qE 'fail|error|Cannot find|not a function' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `bun test src/lib/nostr/live-beacon/lifecycle.test.ts` reports failures (RED) attributable to the missing updateBeacon/stopBeacon lifecycle, not syntax errors.
    - source: lifecycle.test.ts references `updateBeacon`, `stopBeacon`, `status: 'ended'`, and an `expiration` assertion; visibility.test.ts references `'#t'` and `'live'` and asserts a link-only event omits both `t` and geo tags.
    - source: src/test/geolocationMock.ts exports a controller with `watchPosition`, `clearWatch`, `emitFix`, `emitError`, and the three geolocation error codes.
    - CLI: `bunx biome check src/lib/nostr/live-beacon/lifecycle.test.ts src/lib/nostr/live-beacon/visibility.test.ts src/lib/nostr/live-beacon/relay-echo.test.ts src/test/geolocationMock.ts` is clean.
  </acceptance_criteria>
  <done>lifecycle.test.ts + visibility.test.ts RED on the missing lifecycle seam; relay-echo.test.ts present (integration); geolocation mock fixture exists; all biome-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useBeacons + useBeaconPublisher + fetchBeacon RED stubs</name>
  <read_first>
    - src/lib/hooks/useSightings.ts (THE clone template — useExpiryClock tick, filter-before-cast, dropExpired in useMemo)
    - src/lib/nostr/live-beacon/cast.ts (LiveBeacon cast — created_at + expiresAt getters; status/geometry getters added in Plan 02)
    - src/lib/nostr/expiry.ts (isExpired/dropExpired epoch-seconds)
    - src/lib/nostr/kinds.ts (LIVE_BEACON_KIND = 37521)
    - src/lib/og/fetchEvent.ts:307-364 (fetchSightingOGData — the OG clone template: decodeNaddr, kind gate, fetchEventFromRelay, isOGEventExpired, contentExpiresAt carry)
    - src/test/geolocationMock.ts (the fixture from Task 1)
    - .planning/phases/12-live-beacon-37521/12-RESEARCH.md § "Beacon state derivation for the map source" + § "Staleness threshold + cadence defaults" (the BEACON_* constants + beaconState branch order)
  </read_first>
  <behavior>
    - useBeacons.test.ts: a derived `beaconState(cast, now)` (the symbol Plan 02/04 exposes) returns 'removed' when isExpired (highest precedence, never rendered), 'ended' when status==='ended' and not expired, 'stale' when `now - created_at >= BEACON_STALE_THRESHOLD_S` (120s) regardless of status (P-3 — past-threshold is STALE even if status claims 'live'), and 'live' otherwise. Assert each branch at a FIXED epoch-seconds `now` (never Date.now()).
    - useBeacons.test.ts: a legacy/forged 37521 (no current modelVersion) is dropped by `isLiveBeacon` BEFORE the cast ctor is reached (filter-before-cast, P-2 — assert the cast ctor is never called on the forged event / the result list excludes it).
    - useBeacons.test.ts: an expired beacon is removed by dropExpired at the subscription read path even with no new event (the ticking-clock re-derivation — assert dropExpired at a fixed `now`).
    - useBeaconPublisher.test.ts: given the geolocation mock, a fix that moved < 25 m AND < 30 s since last publish does NOT publish; a fix that moved >= 25 m OR a heartbeat >= 30 s DOES publish; a coincident fix+interval publishes ONCE (single lastPublishedAt guard, P-4). Constants `BEACON_HEARTBEAT_MS`/`BEACON_DISTANCE_FLOOR_M`/`BEACON_STALE_THRESHOLD_S` are named (not magic numbers) and the staleness threshold equals `(BEACON_HEARTBEAT_MS/1000) * BEACON_STALE_FACTOR`.
    - useBeaconPublisher.test.ts: starting a session mints a FRESH secp256k1 key (two consecutive Starts produce different pubkeys — unlinkable, D-05) and the session key is NEVER written to localStorage/IndexedDB (assert no storage write occurs during a session).
    - fetchBeacon.test.ts: fetchBeaconOGData(naddr, relayUrl) where naddr encodes a THROWAWAY pubkey decodes + fetches `{kinds:[37521],authors:[throwawayPubkey],'#d':[d]}` and returns title/description; returns null for an expired beacon and null for a non-37521 naddr.
  </behavior>
  <action>Create src/lib/hooks/useBeacons.test.ts importing `useBeacons` and the `beaconState` derivation from their Plan-02/04 module paths (not-yet-existing) and asserting the beaconState branch order + filter-before-cast + dropExpired at a fixed `now`. Create src/features/geo-editor/hooks/useBeaconPublisher.test.ts importing `useBeaconPublisher` + the `BEACON_HEARTBEAT_MS`/`BEACON_DISTANCE_FLOOR_M`/`BEACON_STALE_FACTOR`/`BEACON_STALE_THRESHOLD_S` constants (Plan-03 symbols, not-yet-existing); install the src/test/geolocationMock fixture, drive `emitFix`/the heartbeat, and assert the throttle decisions + the fresh-key-per-session + never-persisted invariants. Create src/lib/og/fetchBeacon.test.ts importing `fetchBeaconOGData` (Plan-05 symbol, not-yet-existing) asserting the naddr round-trip (throwaway pubkey), the expiry guard returning null, and the non-37521 rejection — mock `fetchEventFromRelay` the same way fetchEvent tests do if such a test exists; otherwise build the assertion around `decodeNaddr` + an injected fetch. All three files reference symbols that do not exist yet — they MUST be RED. Do NOT implement the hooks.</action>
  <verify>
    <automated>bun test src/lib/hooks/useBeacons.test.ts src/features/geo-editor/hooks/useBeaconPublisher.test.ts src/lib/og/fetchBeacon.test.ts 2>&1 | grep -qE 'fail|error|Cannot find|not a function' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: all three test files are RED, failing on the missing useBeacons/beaconState/useBeaconPublisher/fetchBeaconOGData symbols, not on syntax errors.
    - source: useBeacons.test.ts asserts the beaconState precedence removed > ended > stale > live AND asserts a past-threshold status:'live' beacon resolves to 'stale' (P-3). useBeaconPublisher.test.ts asserts two Starts yield different pubkeys and asserts no storage write. fetchBeacon.test.ts asserts a throwaway-pubkey naddr round-trips and an expired beacon returns null.
    - source: every clock comparison in the tests uses an explicit epoch-seconds `now` — no `Date.now()` in a beacon-clock assertion.
    - CLI: `bunx biome check src/lib/hooks/useBeacons.test.ts src/features/geo-editor/hooks/useBeaconPublisher.test.ts src/lib/og/fetchBeacon.test.ts` is clean.
  </acceptance_criteria>
  <done>useBeacons.test.ts, useBeaconPublisher.test.ts, fetchBeacon.test.ts all RED on their missing seams; biome clean; the epoch-seconds + filter-before-cast + fresh-key + never-persist contracts pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → none | Wave-0 stubs touch only `*.test.ts` files + a test-only `src/test/geolocationMock.ts`; no runtime/production surface, no untrusted input crosses. The one exception is relay-echo.test.ts, which talks to a LOCAL `bun relay` only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-CLOCK | Tampering | Test asserting wrong staleness/expiry units (ms vs epoch seconds) | mitigate | Every beacon-clock assertion in lifecycle/useBeacons/useBeaconPublisher tests compares at an explicit epoch-seconds `now`; the staleness threshold is asserted as a multiple of the heartbeat constant — pins Pitfall P-1 so a ms bug in Plans 02–05 is caught RED. |
| T-12-01-LINKONLY | Information disclosure | Test asserting a link-only beacon is non-discoverable | mitigate | visibility.test.ts asserts a link-only beacon emits NO `t:'live'` and NO `g`/`bbox` and is NOT matched by the `#t:['live']` discovery filter (Pitfall P-6) — pins the D-10 discovery-gating invariant before any UI exists. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; zero new deps for the whole phase (12-RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `bun test src/lib/nostr/live-beacon/lifecycle.test.ts src/lib/nostr/live-beacon/visibility.test.ts src/lib/hooks/useBeacons.test.ts src/features/geo-editor/hooks/useBeaconPublisher.test.ts src/lib/og/fetchBeacon.test.ts` runs; all assertions for not-yet-existing Plan-02..05 seams are RED.
- relay-echo.test.ts is present and runnable against `bun relay` (its assertion is the BEACON-02 honesty check; it may be skipped in CI without a relay but the file must exist).
- `bunx biome check` clean on all seven files.
- No production source modified (test-only + the test fixture).
</verification>

<success_criteria>
- 6 new test files (lifecycle / visibility / relay-echo / useBeacons / useBeaconPublisher / fetchBeacon) created, RED on their respective missing seams.
- src/test/geolocationMock.ts exists and is importable as the shared watchPosition fixture.
- The epoch-seconds clock (P-1), filter-before-cast (P-2), frozen-as-live-is-stale (P-3), single-guard throttle (P-4), and link-only-not-discoverable (P-6) invariants are each pinned by at least one assertion.
- All files biome-clean; `bun run build` unaffected (no production import added).
</success_criteria>

## Artifacts this phase produces

This plan (Wave 0) produces test scaffolding + a test fixture only. The implementation symbols it references (created in later plans) are listed in 12-02/03/04/05 "## Artifacts this phase produces" sections. New files this plan creates:
- `src/lib/nostr/live-beacon/lifecycle.test.ts`
- `src/lib/nostr/live-beacon/visibility.test.ts`
- `src/lib/nostr/live-beacon/relay-echo.test.ts`
- `src/lib/hooks/useBeacons.test.ts`
- `src/features/geo-editor/hooks/useBeaconPublisher.test.ts`
- `src/lib/og/fetchBeacon.test.ts`
- `src/test/geolocationMock.ts`

<output>
Create `.planning/phases/12-live-beacon-37521/12-01-SUMMARY.md` when done.
</output>
