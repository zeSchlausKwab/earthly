---
phase: 12-live-beacon-37521
plan: 04
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - src/features/geo-editor/hooks/useMapLayers.ts
  - src/components/BeaconsPanel.tsx
  - src/components/AppSidebar.tsx
  - scripts/seed-entities.ts
autonomous: true
requirements: [BEACON-01, BEACON-03, BEACON-04]
user_setup: []

must_haves:
  truths:
    - "A distinct beacon marker layer rides useMapLayers with data-driven live/stale/ended paint, dropExpired-before-source-build, freshest-per-{pubkey,d} pick with an id-lexicographic tie-break (BEACON-03, D-07/D-08)"
    - "The live marker is the accent focal point (#fdc700); stale = greyed 70%; ended = hollow grey outline; expired = removed entirely (D-07)"
    - "A Beacons rail tab (Radio icon) lists public beacons via useBeacons with live/stale/ended chip + last-seen age + countdown + Watch-on-map; the user's own active beacon pins to the top (D-12)"
    - "An accent 'Share live location' CTA at the panel top opens the control flow (BEACON-04 entry)"
    - "Seed fixtures publish the new content shape (geometry/status) + cover all four marker states + a link-only fixture for discovery-gating UAT (BEACON-04, D-10)"
  artifacts:
    - path: "src/components/BeaconsPanel.tsx"
      provides: "BeaconsPanelContent browse panel cloned from SightingsPanelContent over useBeacons"
      contains: "useBeacons"
    - path: "src/features/geo-editor/hooks/useMapLayers.ts"
      provides: "Beacon source/layer pair + beaconState data-driven paint + dropExpired-before-source"
      contains: "BEACON_SOURCE_ID"
    - path: "src/components/AppSidebar.tsx"
      provides: "'beacons' rail mode (Radio) + beaconsPanelProps + renderWorkContent case"
      contains: "beacons"
    - path: "scripts/seed-entities.ts"
      provides: "Updated 37521 fixtures (geometry/status, all 4 states + link-only)"
      contains: "geometry"
  key_links:
    - from: "src/features/geo-editor/hooks/useMapLayers.ts"
      to: "src/lib/nostr/live-beacon (beaconState, isLiveBeacon, dropExpired)"
      via: "build the beacon source from dropExpired-filtered casts, paint by beaconState"
      pattern: "beaconState"
    - from: "src/components/BeaconsPanel.tsx"
      to: "src/lib/hooks/useBeacons"
      via: "useBeacons() subscription feeding the browse rows"
      pattern: "useBeacons"
    - from: "src/components/AppSidebar.tsx"
      to: "src/components/BeaconsPanel + RAIL_DESTINATIONS"
      via: "'beacons' WorkViewMode + BeaconsPanelContent render case"
      pattern: "BeaconsPanelContent"
---

<objective>
Ship the live-map render layer and the Beacons browse rail — the index/control home for beacons. Extend `useMapLayers` with a distinct beacon source/layer pair driven by a data-driven `beaconState` paint (live/stale/ended), `dropExpired`-before-source-build with a freshest-per-`{pubkey,d}` pick (id-lexicographic tie-break), re-evaluated on every tick so staleness flips and expiry-removal happen live (D-07/D-08). Clone `SightingsPanelContent` into `BeaconsPanel` over `useBeacons`, add the `beacons` rail destination (Radio icon) + the render case in `AppSidebar`, and update the seed fixtures to the new content shape covering all four marker states + a link-only fixture. The user's own active beacon pins to the top of the panel with inline Stop/Adjust; an accent "Share live location" CTA opens the control flow (wired in Plan 05).

Purpose: This is the visible half of the milestone's one net-new live-render subsystem (the marker) plus the D-12 browse/control surface. Reusing the exact Sighting layer constants and the rail/panel spine keeps it a kind-substituted clone; the only genuinely new visual logic is the `beaconState` data-driven paint and the own-beacon-pinned-to-top affordance. Runs in parallel with Plan 03 (disjoint files).
Output: 1 new panel, 3 extended files (map layers, sidebar, seed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-beacon-37521/12-CONTEXT.md
@.planning/phases/12-live-beacon-37521/12-RESEARCH.md
@.planning/phases/12-live-beacon-37521/12-PATTERNS.md
@.planning/phases/12-live-beacon-37521/12-UI-SPEC.md
@src/components/SightingsPanel.tsx
@src/lib/hooks/useBeacons.ts
@src/lib/nostr/live-beacon/beaconState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beacon map source/layer pair + beaconState data-driven paint</name>
  <read_first>
    - src/features/geo-editor/hooks/useMapLayers.ts:75-97 (the Sighting source/layer IDs + the color hex constants #fdc700/#737373 to mirror)
    - src/features/geo-editor/hooks/useMapLayers.ts (the full Sighting source/layer block — buildSightingSource with dropExpired, the circle+glyph+hit layer triple, the paint expressions; clone this block)
    - src/lib/nostr/live-beacon/beaconState.ts (beaconState(cast, now) + BEACON_STALE_THRESHOLD_S)
    - src/lib/hooks/useBeacons.ts (the cast stream the source consumes — already filter-before-cast + dropExpired + 15s tick)
    - src/lib/nostr/expiry.ts (isExpired/dropExpired — the per-read-path drop the source builder ALSO applies)
    - .planning/phases/12-live-beacon-37521/12-UI-SPEC.md § Net-New 5 (live/stale/ended marker: solid accent / greyed 70% / hollow outline; broadcast glyph; ≥44px hit; offset label) + § Color (the state→treatment table)
  </read_first>
  <action>Extend src/features/geo-editor/hooks/useMapLayers.ts by cloning the Sighting source/layer block: add BEACON_SOURCE_ID, BEACON_HIT_LAYER (export it for useMapInteractions), BEACON_CIRCLE_LAYER, BEACON_GLYPH_LAYER. Reuse the EXACT hex constants (`#fdc700` live = accent focal point, `#737373` grey for stale/ended). Build the beacon GeoJSON source from the useBeacons casts: FIRST `dropExpired` (per-read-path discipline, even though useBeacons already drops — defensive), THEN pick the freshest cast per `{pubkey,d}` by `created_at` (tie-break: greater `event.id` lexicographically per the research §"seq tag" guard), THEN map each to a feature carrying a `beaconState: 'live'|'stale'|'ended'` property computed via `beaconState(cast, now)` (drop any 'removed'). Drive the circle paint data-driven on `beaconState`: live ⇒ solid #fdc700 full opacity + ring (+ a pulse halo, static under prefers-reduced-motion); stale ⇒ #737373 at 70% opacity, no ring; ended ⇒ hollow (#737373 outline, transparent fill, stop glyph). Each marker carries its offset "last seen…/ended…" label per UI-SPEC. The hit layer is an invisible ≥44px padded circle for touch. Re-derive on the tick so live→stale→removed flips live. Use a broadcast/Radio motif glyph distinct from the Sighting Eye.</action>
  <verify>
    <automated>bun run build 2>&1 | grep -qiE 'error|fail' && echo BUILD-FAIL || echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - source: useMapLayers.ts defines BEACON_SOURCE_ID + a circle/glyph/hit layer triple; the paint references a `beaconState` feature property with three branches (live/stale/ended) using #fdc700 and #737373.
    - source: the source builder calls `dropExpired` AND computes `beaconState(cast, now)` AND picks the freshest per {pubkey,d} with an id tie-break; expired/'removed' beacons are excluded from the source.
    - behavior: `bun run build` green; the existing Sighting/dataset layers are unchanged (no regression).
    - CLI: `bunx biome check src/features/geo-editor/hooks/useMapLayers.ts` clean.
  </acceptance_criteria>
  <done>A distinct beacon marker layer rides useMapLayers with the live/stale/ended data-driven paint, per-read-path dropExpired, freshest-per-{pubkey,d} pick; build + biome clean.</done>
</task>

<task type="auto">
  <name>Task 2: BeaconsPanel browse rail + 'beacons' rail destination + render case + seed fixtures</name>
  <read_first>
    - src/components/SightingsPanel.tsx (THE clone template — SightingsPanelContent: useSightings + useFilterState + useSortedFilteredItems + EntitySearchToolbar + the Card rows + GeoSocialActions + the empty/skeleton states + SightingsPanelProps shape)
    - src/lib/hooks/useBeacons.ts (the subscription; default #t:['live'] discovery filter)
    - src/lib/nostr/live-beacon/beaconState.ts (beaconState for the row chip)
    - src/components/AppSidebar.tsx:56-83 (WorkViewMode union, WORK_VIEW_MODES, workNavItems — mirror the 'sightings'/Eye row)
    - src/components/AppSidebar.tsx:655-670 (sightingsPanelProps shape) + :755-765 (renderWorkContent switch + the SightingsPanelContent case)
    - src/components/AppSidebar.tsx:565-578 (the setActiveWorkMode/navigateToView('sightings') create-entry wiring to mirror)
    - scripts/seed-entities.ts:424-443 (the beacon fixture block to update — currently position:pos + .geohash(pos))
    - src/lib/nostr/live-beacon/lifecycle.ts (publishBeacon — OR keep seed on the factory; match the new geometry/status content shape either way)
    - .planning/phases/12-live-beacon-37521/12-UI-SPEC.md § Net-New 1 (BeaconsPanel: own-beacon pinned to top, Share-live-location accent CTA, Watch-on-map, empty-state copy) + § Copywriting Contract (the exact strings)
  </read_first>
  <action>Create src/components/BeaconsPanel.tsx exporting BeaconsPanelContent, cloning SightingsPanelContent: subscribe via useBeacons() (the #t:['live'] discovery surface), feed casts through useFilterState + useSortedFilteredItems + EntitySearchToolbar, render rounded-none Card rows with a live/stale/ended status chip (from beaconState), the last-seen age, the time-box countdown, and a "Watch on map" action (the row is the click target). Pin the user's own active beacon to the TOP as a distinct accent-ringed-when-live card with inline "Stop sharing" (destructive-toned) + "Adjust" actions (the handlers are passed as props — wired in Plan 05). Add the accent "Share live location" button at the panel top (the create entry → opens the control flow via an onShareLocation prop). Use the empty-state copy ("No live beacons" / the body + the Share CTA) and the search-no-match copy from the UI-SPEC. title/label render as ESCAPED React text (no dangerouslySetInnerHTML). Define BeaconsPanelProps mirroring SightingsPanelProps (currentUserPubkey, onShareLocation, onOpenBeacon, onStopBeacon, onAdjustBeacon, onWatchOnMap, selectedKey). Then extend src/components/AppSidebar.tsx: add 'beacons' to the WorkViewMode union + WORK_VIEW_MODES + a workNavItems row `{ mode:'beacons', title:'Beacons', icon: Radio }` (import Radio from lucide-react); add a beaconsPanelProps object (mirror sightingsPanelProps; the beacon handlers come from the Plan-05 controller, threaded as new AppSidebar props with safe `?? (() => {})` defaults so this plan builds standalone); add a `case 'beacons': return <BeaconsPanelContent {...beaconsPanelProps} />` to renderWorkContent. Finally update scripts/seed-entities.ts:424-443 to the new content shape: `LiveBeaconFactory.create({ label, geometry:{type:'Point',coordinates:pos}, status:'live' }).hashtags(['live']).geohash(pos).bbox(deriveBbox-equivalent).expiration(now()+ttl)`; add one `status:'ended'` fixture and one LINK-ONLY fixture (NO geohash, NO hashtags, NO bbox) so all four marker states + the discovery-gating are visually testable.</action>
  <verify>
    <automated>bun run build 2>&1 | grep -qiE 'error|fail' && echo BUILD-FAIL || echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - source: BeaconsPanel.tsx imports useBeacons and renders a status chip from beaconState + the Share-live-location accent CTA + the own-beacon-pinned-top card; BeaconsPanelProps mirrors SightingsPanelProps. No dangerouslySetInnerHTML.
    - source: AppSidebar.tsx WORK_VIEW_MODES includes 'beacons', workNavItems has the Radio row, and renderWorkContent has a `case 'beacons'`.
    - source: seed-entities.ts beacon block uses `geometry: { type: 'Point', coordinates: pos }` + `status` and includes one ended fixture + one link-only fixture (no geohash/hashtags).
    - behavior: `bun run build` green; the rail shows a Beacons tab (manual-verifiable in Plan 05 UAT).
    - CLI: `bunx biome check src/components/BeaconsPanel.tsx src/components/AppSidebar.tsx scripts/seed-entities.ts` clean.
  </acceptance_criteria>
  <done>BeaconsPanel browse rail cloned over useBeacons; 'beacons' rail destination + render case wired; seed fixtures updated to the new shape covering all four states + link-only; build + biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → map source builder | The source builder consumes useBeacons casts (already filter-before-cast + dropExpired); it re-applies dropExpired defensively before painting. |
| relay → BeaconsPanel list | The browse panel reads the #t:['live'] discovery surface only; a link-only beacon is never matched and never listed. |
| beacon label → DOM | label/title render as auto-escaped React text — no raw HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-FROZEN | Spoofing / integrity | A stale/stopped beacon painted as live | mitigate | The marker paint is data-driven on `beaconState(cast, now)` re-evaluated on the 15s tick; past-threshold = greyed STALE, status:'ended' = hollow ENDED, expired = removed from the source entirely (BEACON-03 / D-07). |
| T-12-04-EXPIRED | Information disclosure | An expired beacon still rendered | mitigate | dropExpired at the source builder (per-read-path) AND in useBeacons; the tick re-derives so removal happens without a new event (SPEC-05). |
| T-12-04-LINKLEAK | Information disclosure | A link-only beacon surfacing in the browse list/map discovery layer | mitigate | BeaconsPanel + the discovery map layer read the `#t:['live']` surface; a link-only beacon emits no `t:'live'` so it is never matched (D-10 / P-6). The seed link-only fixture proves it stays off the list. |
| T-12-04-XSS | Tampering | XSS via beacon label in the panel/marker | mitigate | label/title render as auto-escaped React text nodes; no dangerouslySetInnerHTML (Story/Sighting posture). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No installs; lucide-react (Radio icon) + MapLibre + applesauce are pre-existing; no new @mapcn block (12-UI-SPEC Registry Safety: none added). |
</threat_model>

<verification>
- `bun run build` green (client + workers); the existing Sighting/dataset map layers + panels unaffected.
- `bunx biome check` clean on the 4 touched files.
- `bun run seed` against `bun relay` publishes the updated fixtures without error (manual/UAT — covers all four marker states + the link-only beacon staying off the discovery surface).
- grep confirms AppSidebar has a 'beacons' WorkViewMode + render case and BeaconsPanel reads useBeacons.
</verification>

<success_criteria>
- A distinct beacon marker rides useMapLayers with the live(accent)/stale(grey 70%)/ended(hollow) data-driven paint, per-read-path dropExpired, freshest-per-{pubkey,d} pick + id tie-break; expired beacons removed from the source.
- A Beacons rail tab (Radio) lists public beacons with live/stale/ended chip + last-seen + countdown + Watch-on-map; the user's own active beacon pins to the top; the accent Share-live-location CTA opens the control flow.
- Seed fixtures publish the new geometry/status content shape covering all four marker states + a link-only beacon for discovery-gating UAT.
- Build + biome clean; no regression to the shipped Sighting/dataset surfaces.
</success_criteria>

## Artifacts this phase produces

New symbols this plan creates (excluded from drift verification):
- `BeaconsPanelContent`, `BeaconsPanelProps` (`src/components/BeaconsPanel.tsx`)
- `BEACON_SOURCE_ID`, `BEACON_HIT_LAYER`, `BEACON_CIRCLE_LAYER`, `BEACON_GLYPH_LAYER` + the beacon source builder (`src/features/geo-editor/hooks/useMapLayers.ts`)
- the `'beacons'` WorkViewMode + `beaconsPanelProps` + the renderWorkContent case (`src/components/AppSidebar.tsx`)

<output>
Create `.planning/phases/12-live-beacon-37521/12-04-SUMMARY.md` when done.
</output>
