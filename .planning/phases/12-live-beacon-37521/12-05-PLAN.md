---
phase: 12-live-beacon-37521
plan: 05
type: execute
wave: 3
depends_on: [12-03, 12-04]
files_modified:
  - src/components/info-panel/BeaconControlPanel.tsx
  - src/components/info-panel/BeaconViewPanel.tsx
  - src/components/RunningBeaconBanner.tsx
  - src/features/geo-editor/hooks/useBeaconController.ts
  - src/features/geo-editor/GeoEditorView.tsx
  - src/components/AppSidebar.tsx
  - src/features/geo-editor/hooks/useRouting.ts
  - src/lib/og/fetchEvent.ts
  - src/lib/og/cache.ts
  - src/lib/og/template.ts
  - src/lib/og/index.ts
  - src/index.ts
autonomous: false
requirements: [BEACON-01, BEACON-02, BEACON-03, BEACON-04]
user_setup: []

must_haves:
  truths:
    - "A Start-beacon control panel collects time-box presets (D-03), visibility (D-10) with the non-dismissible 'unlisted not private' caveat, identity (D-05), the no-delete consent (D-06), and Starts the publisher (BEACON-01/02)"
    - "A persistent 'you are live' running banner shows LIVE/searching + countdown + a one-tap Stop while a beacon is active (BEACON-02, mobile-critical)"
    - "A beacon view/detail panel shows label + live/stale/ended status + last-seen (primary) + countdown (secondary) + Copy-share-link; the link carries the throwaway pubkey for anonymous beacons (BEACON-03/04, D-11)"
    - "Stop is a destructive-toned alert-dialog with the no-delete recap; there is NO Delete action (D-04/D-06)"
    - "A thin /beacon/:naddr route + an account-free OG card open a beacon for a logged-out viewer with honest staleness (BEACON-04, D-11)"
    - "Comment/react on beacons is DEFERRED to Phase 13 (XCUT-01) per research Open Q1 — not mounted here"
  artifacts:
    - path: "src/components/info-panel/BeaconControlPanel.tsx"
      provides: "Start-beacon authoring control (time-box/visibility/identity/consent), no pin-drop"
      contains: "Start beacon"
    - path: "src/components/RunningBeaconBanner.tsx"
      provides: "Always-on 'you are live' banner + Stop (no Sighting twin)"
      contains: "Stop sharing"
    - path: "src/components/info-panel/BeaconViewPanel.tsx"
      provides: "Beacon read/detail view + Copy-share-link with throwaway-pubkey naddr"
      contains: "Copy share link"
    - path: "src/features/geo-editor/hooks/useBeaconController.ts"
      provides: "Beacon Start/Stop/Adjust/inspect lifecycle binding the publisher to the UI"
      contains: "useBeaconController"
    - path: "src/lib/og/fetchEvent.ts"
      provides: "fetchBeaconOGData (clone of fetchSightingOGData)"
      contains: "fetchBeaconOGData"
    - path: "src/features/geo-editor/hooks/useRouting.ts"
      provides: "/beacon/:naddr thin per-kind route (Phase 13 generalizes)"
      contains: "beacon"
  key_links:
    - from: "src/features/geo-editor/hooks/useBeaconController.ts"
      to: "src/features/geo-editor/hooks/useBeaconPublisher.ts"
      via: "Start/Stop/Adjust drive the publisher session"
      pattern: "useBeaconPublisher"
    - from: "src/index.ts"
      to: "src/lib/og/fetchEvent.ts fetchBeaconOGData"
      via: "handleBeaconRoute → fetchCachedBeaconEventOGData → generateBeaconOGHtml"
      pattern: "/beacon/:naddr"
    - from: "src/components/info-panel/BeaconViewPanel.tsx"
      to: "nip19.naddrEncode (throwaway pubkey)"
      via: "Copy-share-link builds 37521:<throwawayPubkey>:<d>"
      pattern: "naddrEncode"
---

<objective>
Ship the Live Beacon authoring + reading + share UX — the kind-substituted Phase-11 Sighting spine plus the one net-new always-on running banner. Build the Start-beacon control panel (time-box presets D-03, visibility D-10 with the non-dismissible "unlisted not private" caveat, identity D-05, no-delete consent D-06; NO pin-drop — position comes from GPS), the `RunningBeaconBanner` (persistent "you are live" + one-tap Stop), the `BeaconViewPanel` (label + live/stale/ended status + last-seen + countdown + Copy-share-link carrying the throwaway pubkey), the `useBeaconController` hook binding the Plan-03 publisher to the UI (Start/Stop/Adjust/inspect), the GeoEditorView + AppSidebar wiring, the thin `/beacon/:naddr` route, and the account-free OG card. Comment/react on beacons is DEFERRED to Phase 13 per research Open Q1.

Purpose: This closes BEACON-01..04 end-to-end — a user can Start a time-boxed beacon, see the always-on live banner, Stop it to a clean ended state, and share a link a logged-out viewer can open with honest staleness. The control panel's visibility/identity/consent chrome is the milestone's highest privacy surface; the running banner and the thin route are the only genuinely new UX. Everything else clones the Sighting view/editor/route/OG slots.
Output: 3 new components, 1 new controller hook, extensions to GeoEditorView/AppSidebar/useRouting + the 5-file OG card surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-beacon-37521/12-CONTEXT.md
@.planning/phases/12-live-beacon-37521/12-RESEARCH.md
@.planning/phases/12-live-beacon-37521/12-PATTERNS.md
@.planning/phases/12-live-beacon-37521/12-UI-SPEC.md
@src/components/info-panel/SightingEditorPanel.tsx
@src/components/info-panel/SightingViewPanel.tsx
@src/features/geo-editor/hooks/useSightingEditor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BeaconControlPanel + BeaconViewPanel + RunningBeaconBanner components</name>
  <read_first>
    - src/components/info-panel/SightingEditorPanel.tsx (THE control-panel clone template — the ExpiryPreset/DEFAULT_EXPIRY_PRESET state machinery at :177-178, resolveExpiration, the publish call site :240-287, the form section structure; DROP the geometry/pin-drop guard :244-247 and the placedGeometry/onDrawArea props)
    - src/components/info-panel/SightingViewPanel.tsx (THE view-panel clone template — EntityPanelShell/EntityPanelSurface chrome, the isExpired detail-read gate, the status chip, the owner action row; STRIP the observation-range + the CommentsPanel mount — comment/react is deferred to Phase 13)
    - src/lib/nostr/live-beacon/lifecycle.ts (publishBeacon/updateBeacon/stopBeacon — the control panel hands content+options to the controller, not the factory)
    - src/lib/nostr/live-beacon/beaconState.ts (beaconState for the view status chip + the banner)
    - src/components/ui/{radio-group,card,alert,alert-dialog,button,badge,switch}.tsx (the existing primitives — DO NOT add new ones per UI-SPEC Registry Safety)
    - src/lib/og/fetchEvent.ts:60-72 (decodeNaddr) + how naddr is built elsewhere (nip19.naddrEncode { kind, pubkey, identifier })
    - .planning/phases/12-live-beacon-37521/12-UI-SPEC.md § Copywriting Contract (EVERY exact string: "Share your live location", "Start beacon", "Stop sharing", the time-box presets + "1 hour" default, the visibility helpers + the non-dismissible honesty caveat verbatim, the identity helpers, the no-delete consent at Start + the My-account stronger variant, the Stop alert-dialog copy, last-seen/countdown formats) + § Net-New 2 (control panel) + § Net-New 3 (running banner) + § Net-New 4 (view panel) + § Color (Start=accent, Stop=destructive, the touch-target sizes h-12/≥44px)
  </read_first>
  <action>Create src/components/info-panel/BeaconControlPanel.tsx by cloning SightingEditorPanel's structure: a single-column form with space-y-6 between four blocks — TIME BOX (a radio-group 2x2 preset grid 15min/1hour/4hours/8hours with "1 hour" pre-selected + a Custom… input, reusing the Sighting ExpiryPreset/resolveExpiration machinery; chip ≥44px mobile, selected = accent ring), VISIBILITY (a radio-group of Public/Link-only cards, NO sticky default — asked every Start; selecting Link-only reveals the non-dismissible honesty caveat inline beneath it, verbatim from the UI-SPEC), IDENTITY (a radio-group Anonymous default / My-account opt-in; choosing My-account swaps the consent to the stronger copy), and CONSENT+START (the no-delete consent copy directly above an accent h-12 full-width-on-mobile "Start beacon" button — pressing Start IS the consent, no checkbox). DROP the pin-drop entirely (no placedGeometry/onDrawArea props, no "Drop a pin" guard) — position comes from GPS. A permission gate disables Start with the permission copy when geolocation is not granted. The panel calls an onStart({ content:{label}, expiration, visibility, identity }) prop (the controller wires it to the publisher). Edit/Adjust mode pre-fills from an existing session. Create src/components/RunningBeaconBanner.tsx (NO analog): a persistent .glass-panel banner — mobile bottom-anchored full-width safe-area-inset, desktop a corner pill — showing the LIVE status word (--primary) + "ends in {countdown}" + an unmistakable ≥44px destructive-toned "Stop sharing" button; a "searching…" sub-line when the publisher reports POSITION_UNAVAILABLE/permission sub-state (never disappears mid-session). The live dot is static under prefers-reduced-motion. Driven by the publisher status passed as props. Create src/components/info-panel/BeaconViewPanel.tsx by cloning SightingViewPanel: EntityPanelShell + a tone surface with the label (20px), the live/stale/ended status chip (from beaconState), the last-seen age (primary, from created_at), the time-box countdown (secondary, from expiration), and a "Copy share link" button (toast "Link copied — anyone with it can watch"). The share naddr MUST carry the THROWAWAY pubkey: nip19.naddrEncode({ kind: LIVE_BEACON_KIND, pubkey: beacon.pubkey, identifier: beacon.dTag }). Owner viewing their own live beacon also sees inline Stop/Adjust. Gate the detail read on isExpired (show the "This beacon has ended." terminal copy when expired). Do NOT mount CommentsPanel/GeoSocialActions (deferred to Phase 13 — leave a `// comment/react deferred to Phase 13 / XCUT-01` comment). All strings auto-escaped React text — no dangerouslySetInnerHTML. There is NO Delete action — Stop (alert-dialog, destructive, with the no-delete recap) is the only teardown.</action>
  <verify>
    <automated>bun run build 2>&1 | grep -qiE 'error|fail' && echo BUILD-FAIL || echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - source: BeaconControlPanel.tsx has the four form blocks (time-box/visibility/identity/consent), the verbatim "unlisted, not private" caveat string, the "Start beacon" accent button, and NO placedGeometry/pin-drop prop. RunningBeaconBanner.tsx has the LIVE word + countdown + a destructive "Stop sharing" + a searching sub-state. BeaconViewPanel.tsx builds the share naddr with `naddrEncode` carrying the beacon pubkey and gates on isExpired.
    - source: no `dangerouslySetInnerHTML` and no `CommentsPanel`/`GeoSocialActions` import in BeaconViewPanel.tsx (comment/react deferred); no new `@/components/ui/*` primitive added.
    - source: BeaconViewPanel.tsx has NO Delete action; Stop is an alert-dialog with the no-delete recap copy.
    - behavior: `bun run build` green.
    - CLI: `bunx biome check src/components/info-panel/BeaconControlPanel.tsx src/components/info-panel/BeaconViewPanel.tsx src/components/RunningBeaconBanner.tsx` clean.
  </acceptance_criteria>
  <done>The Start control panel (no pin-drop), the always-on running banner, and the read/detail view (Copy-share-link with throwaway pubkey, no Delete, comment/react deferred) all build + biome clean against the verbatim UI-SPEC copy.</done>
</task>

<task type="auto">
  <name>Task 2: useBeaconController + GeoEditorView + AppSidebar wiring</name>
  <read_first>
    - src/features/geo-editor/hooks/useSightingEditor.ts (THE controller clone template — the create/edit/inspect/close lifecycle, prepareNonGeometryWorkspace, navigateToView('sightings'), the hook-local viewSighting state, the lastInspected highlight key; DROP all pin-drop/armPlacement/handleGeometryPlaced — beacon has no pin-drop)
    - src/features/geo-editor/hooks/useBeaconPublisher.ts (Plan 03 — startBeacon/stopBeacon/session/status the controller drives)
    - src/features/geo-editor/GeoEditorView.tsx:1602-1718 (the Sighting hook wiring block) + :1960-1980 (the Sighting prop-threading into the info-panel) + :2280-2300 (the second mount site)
    - src/components/AppSidebar.tsx:655-670 (sightingsPanelProps) + :565-578 (the create-entry wiring) + the beaconsPanelProps stub added in Plan 04 (thread the real controller handlers here)
    - src/features/geo-editor/store/viewModeSlice.ts (viewStory/viewDataset/viewContext slots + applyRouteState — note Sighting view is hook-local, mirror that for beacon)
    - .planning/phases/12-live-beacon-37521/12-UI-SPEC.md § Net-New 1/2/3 (Share-live-location entry, control panel open, the banner mount over the map)
  </read_first>
  <action>Create src/features/geo-editor/hooks/useBeaconController.ts cloning useSightingEditor MINUS the pin-drop: hold hook-local `beaconControlMode: 'none'|'create'|'adjust'`, `viewBeacon`, `lastInspectedBeaconKey`, `focusCommentId`. Compose useBeaconPublisher (Plan 03) for the live session. handleShareLocation opens the control panel (beaconControlMode='create') and navigateToView('beacons'). handleStartBeacon(options) calls publisher.startBeacon and switches to the live state. handleStopBeacon shows the Stop alert-dialog then calls publisher.stopBeacon. handleAdjustBeacon opens the control pre-filled (LiveBeaconFactory.modify preserves d via updateBeacon). handleInspectBeacon(beacon, commentId?) sets viewBeacon + navigateToView('beacons'). handleCloseBeaconControl resets. Expose the publisher status/session so GeoEditorView can mount the RunningBeaconBanner. Wire into GeoEditorView.tsx mirroring the Sighting block: instantiate useBeaconController, thread its handlers + state into the info-panel mount (a BeaconControlPanel branch when beaconControlMode!=='none', a BeaconViewPanel branch when viewBeacon, gated like the Sighting editor/view branches), and mount <RunningBeaconBanner> over the map whenever a publisher session is live (the always-on chrome, regardless of which panel is open). In AppSidebar.tsx, replace the Plan-04 beaconsPanelProps default-stub handlers with the real controller handlers (onShareLocation=handleShareLocation, onOpenBeacon=handleInspectBeacon, onStopBeacon=handleStopBeacon, onAdjustBeacon=handleAdjustBeacon, onWatchOnMap=onZoomToBeacon) threaded from GeoEditorView through AppSidebar's props; mirror the sightings create-entry wiring so the rail New action opens the control panel.</action>
  <verify>
    <automated>bun run build 2>&1 | grep -qiE 'error|fail' && echo BUILD-FAIL || echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - source: useBeaconController.ts composes useBeaconPublisher and exposes handleShareLocation/handleStartBeacon/handleStopBeacon/handleAdjustBeacon/handleInspectBeacon + the live session/status; it has NO armPlacement/handleGeometryPlaced (no pin-drop).
    - source: GeoEditorView.tsx mounts RunningBeaconBanner when a session is live and renders BeaconControlPanel/BeaconViewPanel in the info-panel gated on beaconControlMode/viewBeacon; AppSidebar.tsx beaconsPanelProps uses the real controller handlers (not the Plan-04 () => {} stubs).
    - behavior: `bun run build` green; the existing Sighting/Story/dataset panels + wiring unaffected.
    - CLI: `bunx biome check src/features/geo-editor/hooks/useBeaconController.ts src/features/geo-editor/GeoEditorView.tsx src/components/AppSidebar.tsx` clean.
  </acceptance_criteria>
  <done>The controller binds the publisher to the control/view panels + the running banner; GeoEditorView mounts the banner always-on and the panels in the info-panel slot; the Beacons rail handlers are live; build + biome clean.</done>
</task>

<task type="auto">
  <name>Task 3: /beacon/:naddr thin route + account-free OG card</name>
  <read_first>
    - src/features/geo-editor/hooks/useRouting.ts:36,54,127-138 (the focusType union, isFocusType, the /sighting block with the `// Phase 13 / XCUT-02 generalizes` comment — clone it thin)
    - src/lib/og/fetchEvent.ts:307-364 (fetchSightingOGData — THE clone template: decodeNaddr, kind gate, fetchEventFromRelay {kinds,authors,#d}, isOGEventExpired guard, contentExpiresAt carry)
    - src/lib/og/cache.ts:14,21,187-219,358-363 (OGCacheType union, OGPayloadByType, the expiry-aware cache record, fetchCachedSightingEventOGData — clone for beacon)
    - src/lib/og/template.ts:143-178 (generateStoryOGHtml/generateSightingOGHtml — clone for beacon; the audited generateOGHtml escaping)
    - src/lib/og/index.ts (the export surface to extend)
    - src/index.ts:180-217 (handleSightingRoute — clone) + :366-367 (the route registration) + :280-292 (the OG-image type branch)
    - src/lib/og/fetchBeacon.test.ts (the Plan-01 RED contract this turns GREEN)
    - .planning/phases/12-live-beacon-37521/12-UI-SPEC.md § Net-New 6 (the account-free share view + the honest "Live location — may have ended" OG copy)
  </read_first>
  <action>Extend src/lib/og/fetchEvent.ts with `fetchBeaconOGData(naddr, relayUrl)` cloning fetchSightingOGData: gate on `decoded.kind === LIVE_BEACON_KIND`, fetch latest-by-created_at via fetchEventFromRelay {kinds:[37521],authors:[decoded.pubkey],'#d':[decoded.identifier]} (works for the throwaway pubkey), return null for an expired beacon (isOGEventExpired), parse the label, carry contentExpiresAt; export a BeaconOGData type. Add 'beacon' to OGCacheType + OGPayloadByType + the expiry branch + a fetchCachedBeaconEventOGData in src/lib/og/cache.ts. Add generateBeaconOGHtml to src/lib/og/template.ts with honest copy ("Live location — may have ended") and the /#/beacons/beacon/:naddr in-app URL + the /og/image/beacon/:naddr image URL. Re-export the new symbols from src/lib/og/index.ts. In src/index.ts add handleBeaconRoute (clone handleSightingRoute — crawler→generateBeaconOGHtml, user→redirect to /#/beacons/beacon/:naddr, warmOGCache('beacon',...)), register '/beacon/:naddr' + '/beacon/:naddr/comment/:commentId', and add the 'beacon' branch to the OG-image route. In src/features/geo-editor/hooks/useRouting.ts add `| 'beacon'` to the focusType union (:36), add `value === 'beacon'` to isFocusType (:54), and add a thin `first === 'beacon'` segment block mirroring the /sighting block (sidebarView:'beacons', the optional /comment suffix) with the `// Phase 13 / XCUT-02 generalizes` comment (P-5 — keep it a thin per-kind clone).</action>
  <verify>
    <automated>bun test src/lib/og/fetchBeacon.test.ts 2>&1 | grep -qE '(^| )0 fail| 0 fail|pass' && echo GREEN-OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `bun test src/lib/og/fetchBeacon.test.ts` passes (0 fail) — the naddr round-trip (throwaway pubkey), the expired→null guard, and the non-37521 rejection all green.
    - source: fetchEvent.ts has `fetchBeaconOGData` gating on LIVE_BEACON_KIND; cache.ts OGCacheType includes 'beacon'; template.ts has generateBeaconOGHtml with the "may have ended" honest copy; src/index.ts registers '/beacon/:naddr'; useRouting.ts has the thin beacon block + the Phase-13 comment.
    - source: the /beacon block does NOT introduce a generic entity router (thin per-kind clone, P-5).
    - behavior: `bun run build` green (client + server).
    - CLI: `bunx biome check src/lib/og/fetchEvent.ts src/lib/og/cache.ts src/lib/og/template.ts src/lib/og/index.ts src/index.ts src/features/geo-editor/hooks/useRouting.ts` clean.
  </acceptance_criteria>
  <done>A thin /beacon/:naddr route + a full account-free OG card (fetch/cache/template/index/server) open a beacon for a logged-out viewer with honest staleness; Plan-01 fetchBeacon test GREEN; build + biome clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The end-to-end Live Beacon flow: Start a time-boxed beacon (anonymous throwaway key by default) → the live dot appears + moves on the map as you move → the always-on "you are live" banner shows the countdown → the marker greys to STALE if updates stop → Stop leaves a clean ENDED marker → the beacon auto-expires and is removed → a /beacon/:naddr link opens for a logged-out viewer with honest staleness + an OG card. Plus the public-vs-link-only discovery-gating and the no-delete/honesty consent copy.</what-built>
  <how-to-verify>
    1. Run `bun relay` + `bun run seed` + `bun dev`. Confirm the seeded beacons render: a LIVE accent dot, a STALE grey dot, an ENDED hollow marker; the link-only seed beacon does NOT appear in the Beacons rail list or the discovery map layer (only via its naddr).
    2. Open the Beacons rail (Radio icon). Press "Share live location". Confirm the control panel: time-box presets with "1 hour" pre-selected, visibility (select Link-only → the non-dismissible "unlisted, not private" caveat appears inline), identity (Anonymous pre-selected), the no-delete consent above an accent "Start beacon" (h-12 on mobile).
    3. Start a beacon. Confirm: the always-on running banner appears (LIVE + "ends in …" + Stop), your own active beacon pins to the TOP of the Beacons rail, and a live accent dot appears on the map at your location. Move (or simulate movement >25 m) and confirm the dot re-paints.
    4. Stop publishing (banner Stop). Confirm the alert-dialog with the no-delete recap, then a clean ENDED marker (not a silent disappearance). Confirm there is NO Delete action anywhere.
    5. Switch identity to "My account" on a new Start → confirm the consent copy weights stronger.
    6. Copy the share link from the beacon view panel. Open it in a fresh private window with NO account → confirm the beacon opens (account-free) showing label + status + last-seen + countdown; if expired, the "This beacon has ended." terminal copy.
    7. Deny location at Start → confirm Start is disabled with the permission copy.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. (Per human_verify_mode:end-of-phase, this may be deferred to the consolidated end-of-phase UAT — the automated gates above must be green regardless.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user choice → publish identity/visibility | The control panel collects identity (anonymous default) + visibility (asked each time) + the informed no-delete consent before any position leaves the device. |
| share link → logged-out viewer | /beacon/:naddr opens account-free (guest scope); the OG crawler path runs server-side and must never leak an expired beacon's content. |
| throwaway pubkey → naddr | The share naddr carries the throwaway pubkey (the beacon is not under the user's profile); the OG fetch resolves by {kind,pubkey,#d}. |
| beacon label → DOM + OG HTML | label renders as auto-escaped React text in-app and through the audited generateOGHtml escaping in the OG card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-DEANON | Information disclosure | De-anonymization via own-pubkey publish | mitigate | Identity defaults to Anonymous (throwaway key); My-account is an explicit opt-in that swaps the consent to the stronger-weighted copy at Start (D-05/D-06). |
| T-12-05-LINKHONESTY | Information disclosure | "Link-only" assumed cryptographically private | mitigate | Selecting Link-only reveals a NON-DISMISSIBLE inline caveat: "unlisted, not private … published unencrypted … throwaway identity … don't treat it as secret" (verbatim UI-SPEC). Discovery-gating is honestly disclosed as client-side only (D-10). |
| T-12-05-NODELETE | Information disclosure | User unaware the last point stays public | mitigate | The no-delete consent shows at Start (pressing Start IS the consent) and a recap in the Stop alert-dialog; there is NO Delete action — Stop is the only consent-gated teardown (D-04/D-06). |
| T-12-05-FROZEN | Spoofing / integrity | A stopped/stale beacon shown as current in the view/banner | mitigate | The view + banner derive live/stale/ended via beaconState off created_at; the banner shows a searching sub-state on fix-unavailable rather than freezing as live; the detail gates on isExpired (BEACON-03 / P-3). |
| T-12-05-OGLEAK | Information disclosure | OG card leaking an expired beacon's content | mitigate | fetchBeaconOGData returns null for an expired beacon (isOGEventExpired) + carries contentExpiresAt so the cache hard-misses; the crawler then renders the generic fallback — the label is never leaked (clone of the audited Sighting OG posture). |
| T-12-05-XSS | Tampering | XSS via beacon label in panels / OG HTML | mitigate | label renders as auto-escaped React text in-app and through the audited generateOGHtml escaping server-side; no dangerouslySetInnerHTML. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No installs; all primitives (radix ui, lucide-react, nip19) pre-existing; no new @mapcn block (12-UI-SPEC Registry Safety). |
</threat_model>

<verification>
- `bun test src/lib/og/fetchBeacon.test.ts` GREEN; `bun test` full suite green (no regression).
- `bun run build` green (client + server + workers); `bunx biome check` clean on all 12 touched files.
- grep confirms BeaconViewPanel does NOT import CommentsPanel/GeoSocialActions (comment/react deferred to Phase 13) and has NO Delete action.
- grep confirms the /beacon route block + the OG card carry the throwaway pubkey and the "may have ended" honest copy.
- The blocking human-verify checkpoint covers the live-GPS, account-free-link, permission-denied, and discovery-gating behaviors that cannot be unit-tested (per 12-VALIDATION Manual-Only table); may be deferred to the consolidated end-of-phase UAT.
</verification>

<success_criteria>
- A user can Start a time-boxed beacon (anonymous throwaway key default, My-account opt-in) with the visibility choice + the non-dismissible honesty caveat + the no-delete consent; the live dot updates on the map (BEACON-01).
- The beacon auto-expires (NIP-40) and Stop leaves a clean ENDED state with the no-delete recap; there is no Delete action (BEACON-02).
- The view/banner/marker show honest live/stale/ended staleness; a stale/stopped beacon is never shown as current (BEACON-03).
- A public beacon is discoverable; a link-only beacon opens only via its throwaway-pubkey naddr, account-free, with an OG card (BEACON-04).
- Comment/react on beacons is explicitly deferred to Phase 13.
- Plan-01 fetchBeacon test GREEN; full suite + build + biome clean.
</success_criteria>

## Artifacts this phase produces

New symbols this plan creates (excluded from drift verification):
- `BeaconControlPanel`, `BeaconViewPanel`, `RunningBeaconBanner` (components)
- `useBeaconController` (`src/features/geo-editor/hooks/useBeaconController.ts`)
- `fetchBeaconOGData`, `BeaconOGData`, `fetchCachedBeaconEventOGData`, `generateBeaconOGHtml`, the `'beacon'` OGCacheType, `handleBeaconRoute`
- the `'beacon'` focusType + the `/beacon/:naddr` route block (`src/features/geo-editor/hooks/useRouting.ts`)
- new `beacons` handlers threaded through `GeoEditorView.tsx` + `AppSidebar.tsx`

<output>
Create `.planning/phases/12-live-beacon-37521/12-05-SUMMARY.md` when done.
</output>
