---
phase: 13-cross-cutting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/social/hooks/useGeoComments.ts
  - src/components/info-panel/BeaconViewPanel.tsx
  - src/features/social/hooks/useGeoComments.beacon.test.ts
autonomous: true
requirements: [XCUT-01]
tags: [comments, beacon, nostr, nip-22]

must_haves:
  truths:
    - "A user can comment on a Live Beacon (kind 37521) — the last unwired kind reaches full comment parity with Story/Group/Sighting (XCUT-01, D-06)"
    - "The useGeoComments target union and react() param union both accept LiveBeacon"
    - "BeaconViewPanel mounts <CommentsPanel target={beacon} /> and the Phase-12 deferral note is gone"
  artifacts:
    - path: "src/features/social/hooks/useGeoComments.ts"
      provides: "LiveBeacon added to both the target union (UseGeoCommentsOptions.target) and the react() param union"
      contains: "LiveBeacon"
    - path: "src/components/info-panel/BeaconViewPanel.tsx"
      provides: "CommentsPanel mount on the beacon 37521 coordinate + comment/mention props on BeaconViewPanelProps"
      contains: "CommentsPanel"
    - path: "src/features/social/hooks/useGeoComments.beacon.test.ts"
      provides: "automated proof the widened target union roots a comment at 37521:pubkey:dTag (D-11)"
  key_links:
    - from: "src/components/info-panel/BeaconViewPanel.tsx"
      to: "src/features/social/comments/CommentsPanel.tsx"
      via: "CommentsPanel target={beacon} mount"
      pattern: "CommentsPanel[\\s\\S]*target=\\{beacon\\}"
    - from: "src/features/social/hooks/useGeoComments.ts"
      to: "37521:pubkey:dTag comment address"
      via: "target.kind/pubkey/dTag → #A filter (already kind-generic)"
      pattern: "\\$\\{targetKind\\}:\\$\\{targetPubkey\\}:\\$\\{targetDTag\\}"
---

<objective>
Wire Live Beacon (kind 37521) to full comment parity — the last kind still
unwired for comments (XCUT-01, D-06). Group (37518), Story (37520), and Sighting
(37522) are already comment-wired; the comment stack (`geo-comment/factory.ts`,
`useGeoComments.ts` filter) has been kind-generic since Phase 8. This plan is a
type-union widening plus a panel mount — no comment machinery changes.

Purpose: Close the XCUT-01 comment-root-widening gap so all four v1.2 entity kinds
are commentable, satisfying ROADMAP Phase-13 Success Criterion 1.
Output: Beacons accept comments; `BeaconViewPanel` shows a Discussion section
identical to `StoryViewPanel`/`SightingViewPanel`; the Phase-12 deferral note is
removed.
</objective>

<artifacts_this_phase_produces>
Symbols this plan CREATES (excluded from source-drift verification):
- New prop names on `BeaconViewPanelProps`: `focusCommentId`, `onCommentGeometryVisibility`, `onZoomToBounds`, `availableFeatures`, `onMentionVisibilityToggle`, `onMentionZoomTo` (copied verbatim from `StoryViewPanelProps` L44-59 / `SightingViewPanelProps`).
- New test file `src/features/social/hooks/useGeoComments.beacon.test.ts`.
Note: `LiveBeacon` is an EXISTING type (`@/lib/nostr/live-beacon`, Phase 12); it is added to two existing unions, not created here.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-cutting/13-CONTEXT.md
@.planning/phases/13-cross-cutting/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen useGeoComments unions to accept LiveBeacon + RED test</name>
  <read_first>
    - src/features/social/hooks/useGeoComments.ts (the two unions to widen: L27-30 target, L40-42 react() param; the kind-generic filter L55-63 stays byte-for-byte; type imports at L15-18)
    - src/lib/nostr/live-beacon/index.ts (confirm `LiveBeacon` is exported and exposes `.kind`/`.pubkey`/`.dTag` — the fields the L55-63 filter reads)
    - src/lib/nostr/geo-comment/sightingComment.test.ts (analog test shape: how a 37522 comment root is asserted; mirror for 37521)
  </read_first>
  <behavior>
    - Test: constructing UseGeoCommentsOptions with a LiveBeacon-shaped target ({kind:37521, pubkey, dTag}) yields the `#A` filter address `37521:<pubkey>:<dTag>` (proves the widened union flows through the already-generic L55-63 filter).
    - Test: a target with a missing dTag yields a null filter (defensive path unchanged).
  </behavior>
  <action>
    Add `import type { LiveBeacon } from '@/lib/nostr/live-beacon'` to the type-import block (near L15-18). Widen `UseGeoCommentsOptions.target` (L28) from `GeoDataset | MapContext | Article | TemporalSighting | null` to include `| LiveBeacon`. Widen the `react()` param union (L41) identically to include `| LiveBeacon`. Make NO other change to useGeoComments — the L55-63 filter already reads `target.kind`/`target.pubkey`/`target.dTag` and builds `${targetKind}:${targetPubkey}:${targetDTag}` for the `#A` filter, which LiveBeacon satisfies. Write the RED test at src/features/social/hooks/useGeoComments.beacon.test.ts asserting the 37521 root address derivation and the null-on-missing-dTag path; import the LiveBeacon cast the same way sightingComment.test.ts imports its cast.
  </action>
  <verify>
    <automated>bun test src/features/social/hooks/useGeoComments.beacon.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LiveBeacon" src/features/social/hooks/useGeoComments.ts` returns a value >= 3 (import + target union + react union).
    - `bun test src/features/social/hooks/useGeoComments.beacon.test.ts` exits 0 (both behaviors GREEN).
    - `bun run build` succeeds.
  </acceptance_criteria>
  <done>useGeoComments accepts LiveBeacon in both unions; the beacon comment-root test is GREEN; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Mount CommentsPanel in BeaconViewPanel + remove the deferral note</name>
  <read_first>
    - src/components/info-panel/BeaconViewPanel.tsx (the deferral note in the header comment L7-8 to remove; the deferral note at L273-274 to replace; BeaconViewPanelProps L56-70 to extend; the existing "Copy share link" Button block L262-270 for the surrounding structure)
    - src/components/info-panel/StoryViewPanel.tsx:217-238 (the EXACT CommentsPanel mount to clone — key/target/onCommentGeojsonVisibilityChange/onZoomToCommentGeojson/availableFeatures/onMentionVisibilityToggle/onMentionZoomTo/focusCommentId) and StoryViewPanelProps L44-59 (the exact prop names/types to copy)
    - src/components/info-panel/SightingViewPanel.tsx:240-256 (second reference for the same mount shape on a temporal-entity panel)
  </read_first>
  <action>
    Add the comment/mention props to `BeaconViewPanelProps`, copying names and types verbatim from `StoryViewPanelProps`: `focusCommentId?: string`, `onCommentGeometryVisibility?`, `onZoomToBounds?`, `availableFeatures?: GeoFeatureItem[]`, `onMentionVisibilityToggle?`, `onMentionZoomTo?`. Import `CommentsPanel` from `@/features/social/comments` and `GeoFeatureItem`/`GeoComment` types as StoryViewPanel does. Replace the deferral note block at L273-274 with an `<EntityPanelSurface tone="discussion">` containing an `<EntityPanelSectionHeader eyebrow="Discussion" title="Comments" />` and the `<CommentsPanel key={beacon.id ?? beacon.dTag ?? 'no-beacon'} target={beacon} ... />` mount, mirroring StoryViewPanel L222-238 exactly. Delete the "comment/react deferred to Phase 13 / XCUT-01" sentences from the file header comment (L7-8) and the inline note (L273-274). Destructure the new props in the component signature. Do NOT change the expiry gate at L125-127 (an expired beacon still short-circuits to the terminal copy BEFORE the comments render — no comment surface on an ended beacon, preserving Phase-12 honesty posture).
  </action>
  <verify>
    <automated>bun run build && bunx biome check src/components/info-panel/BeaconViewPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deferred to Phase 13\|DEFERRED to Phase 13" src/components/info-panel/BeaconViewPanel.tsx` returns 0 (deferral note gone).
    - `grep -cE "CommentsPanel[[:space:]]" src/components/info-panel/BeaconViewPanel.tsx` returns >= 1 AND `grep -c "target={beacon}" src/components/info-panel/BeaconViewPanel.tsx` returns >= 1 (mount present).
    - `grep -c "focusCommentId" src/components/info-panel/BeaconViewPanel.tsx` returns >= 1.
    - `bun run build` succeeds; `bunx biome check` on the file passes.
  </acceptance_criteria>
  <done>BeaconViewPanel renders a Discussion/Comments section against the beacon 37521 coordinate identical to StoryViewPanel; the deferral note is removed; build + biome green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → comment render | comment events (37517) authored by untrusted relays render beneath a beacon |
| beacon.pubkey → comment address | the throwaway-key beacon pubkey becomes part of the `37521:pubkey:d` root address |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-XSS | Tampering | comment text/mention render under BeaconViewPanel | mitigate | Reuse the EXISTING CommentsPanel render path unchanged — it already renders comment text as escaped React text nodes with zero `dangerouslySetInnerHTML` (Story/Sighting parity). This plan adds no new render surface, so no new injection vector. Acceptance: `grep -c dangerouslySetInnerHTML src/components/info-panel/BeaconViewPanel.tsx == 0`. |
| T-13-01-MISATTACH | Spoofing | reused own-pubkey beacon `d` carries old comments | accept | D-07 known edge: throwaway-key-per-session (Phase 12 D-05) makes the `37521:pubkey:d` address effectively session-unique, so cross-session misattach only occurs in the own-pubkey opt-in case. Documented as a known edge; NO de-dup/scoping built this phase (per CONTEXT.md Deferred Ideas). Build nothing; record in SUMMARY. |
| T-13-01-GPS | Information Disclosure | beacon GPS-privacy posture (Phase 12 CR-01/CR-02) | mitigate | Do NOT regress the location-leak protections: the comment mount reads only `beacon.id`/`beacon.dTag`/`beacon.pubkey` for the CommentsPanel key + target — it does NOT read or emit `beacon.geometry`/position into any comment payload or address. Acceptance: the CommentsPanel mount references no beacon position field (`grep -c "beacon.geometry\|beacon.position" src/components/info-panel/BeaconViewPanel.tsx` unchanged from pre-edit; the mount adds only key/target). |
</threat_model>

<verification>
- `bun test src/features/social/hooks/useGeoComments.beacon.test.ts` exits 0.
- `bun run build` succeeds.
- `bunx biome check src/components/info-panel/BeaconViewPanel.tsx src/features/social/hooks/useGeoComments.ts` passes.
- Full suite regression: `bun test` shows no new failures vs the Phase-12 baseline (755/0 + any still-RED beacon fixtures unchanged).
- `grep -c dangerouslySetInnerHTML src/components/info-panel/BeaconViewPanel.tsx` returns 0.
</verification>

<success_criteria>
- LiveBeacon appears in both useGeoComments unions; the widened-union test is GREEN.
- BeaconViewPanel mounts CommentsPanel on the 37521 coordinate; the deferral note (header + inline) is deleted.
- Beacon reaches comment parity with Story/Group/Sighting (D-06); ROADMAP SC-1 advanced for the beacon kind.
- Build + biome + targeted test all green; no regression.
</success_criteria>

<output>
Create `.planning/phases/13-cross-cutting/13-01-SUMMARY.md` when done. Record the
D-07 own-pubkey d-reuse known edge as an explicit residual (no de-dup built).
</output>
