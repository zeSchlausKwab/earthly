---
phase: 13-cross-cutting
plan: 07
type: execute
wave: 2
gap_closure: true
depends_on: ["13-05", "13-06"]
files_modified: []
autonomous: false
requirements: [XCUT-01, XCUT-02]
must_haves:
  truths:
    - "UAT test 5: the 'Add to map stack' button renders on the beacon AND sighting view panels AND on their rail rows (the missing-button symptom was a stale HMR bundle — already CONFIRMED gone after the earlier restart)."
    - "UAT test 6: MapStackPanel shows top-pinned 'Sightings' and 'Live beacons' aggregate rows; toggling a row's visibility off removes that whole layer from the map, toggling back on restores it."
    - "UAT test 7: a fresh cold-start Browse (no shared/deep link) seeds BOTH aggregate layers (Sightings + Live beacons) visible-by-default; they are removable/toggleable."
    - "UAT test 9: a pinned individual sighting/beacon entry auto-removes from the Map Stack once its entity expires — no leftover 'ended'/tombstone row."
  artifacts: []
  key_links: []
---

<objective>
Re-run UAT tests 5, 6, 7, and 9 against a CLEAN dev-server runtime. These four
gaps are diagnosed in `13-UAT.md` as `NOT A CODE DEFECT — stale HMR runtime`, not
source bugs — so this plan makes NO source change. It is a single human-action
verification checkpoint: restart the dev server, hard-reload, re-run the four
tests.

Root cause (UAT tests 5/6/7, authoritative). The `bun --hot src/index.ts` dev
server (PID 95035) started 13:00:39 — ~2.5h BEFORE the Plan-03/04 commits
(`45936be`/`a219548`/`c8d6df4`/`77071df` landed 15:31-15:59). `bun --hot` could
NOT hot-apply a structural change of that magnitude: a new module-scope export
(`deriveVisibleEntitiesFromStack`), a DELETED `extraMapBeacons` `useState` (changes
`GeoEditorView`'s hook count/order → React fast-refresh bails), added
`useRef`/`useEffect`/`useMemo`, props threaded through 4 files, and a switched
`useMapLayers` call. So the LIVE bundle kept the pre-Phase-13 `GeoEditorView` (old
always-on subscription render, no add-to-stack props, no aggregate seeding). The
COMMITTED source and the `dist/` build (16:20) are correct and fully wired
end-to-end (add-to-stack chain intact `GeoEditorView` → `AppSidebar` →
`GeoEditorInfoPanel` → view panels + rails; cold-start seed + expiry sweep present),
proven by read AND by 13/13 `stackLayers`/`layerEntries` tests passing. Test 5's
own retest ALREADY CONFIRMED the button renders after a restart — establishing the
stale-runtime diagnosis for the whole cluster. Each gap's `missing:` field is
literally "Restart dev server + hard reload, re-run UAT 5/6/7/9."

Test 9 was `blocked` at UAT time (could not create a pinned individual entry to age
out because the add button was absent in the stale runtime); with a clean runtime
(and the Plan 13-06 phantom-entry fix landed as a `depends_on`), an individual
entry can be pinned and observed to auto-remove on expiry.

Purpose: confirm the Plan-03/04 Map Stack UI + lifecycle surface actually works in
a running app once the stale bundle is gone — closing the four gaps by
verification, not by fabricated code changes.
Output: a re-run UAT result for tests 5/6/7/9; if all pass, the cluster is closed.
IMPORTANT: if ANY symptom persists after a CLEAN restart, it becomes a REAL bug to
diagnose separately (this plan does not pre-suppose a fix for that case).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-cutting/13-UAT.md
@.planning/debug/mapstack-ui-surface-absent.md
@.planning/phases/13-cross-cutting/13-03-SUMMARY.md
@.planning/phases/13-cross-cutting/13-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NONE — this is a verification-only plan (`files_modified: []`). No new symbols, no
source files, no cache/guard/subscription. The only "artifact" is a re-run UAT
result recorded in the SUMMARY: pass/fail per test 5/6/7/9 against a clean runtime.
It exists to prove the ALREADY-SHIPPED Plan-03/04 symbols
(`deriveVisibleEntitiesFromStack`, `addBeaconToMapStack`/`addSightingToMapStack`,
the cold-start `aggregateLayersSeededRef` seed, the expiry-sweep effect, the
`bucketMapStackEntries`/`orderedMapStackEntries` top-pin helpers) are live in the
running app once the stale HMR bundle is replaced.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Restart the dev server clean, hard-reload, re-run UAT 5/6/7/9</name>
  <what-built>
    NO new code. This checkpoint verifies the Plan-03/04 Map Stack UI + lifecycle
    surface (already committed + built, `stackLayers`/`layerEntries` tests green)
    now behaves correctly in a running app once the stale `bun --hot` bundle
    (PID 95035, started before the Plan-03/04 commits) is replaced by a fresh
    server + hard reload. The Plan 13-06 phantom-entry fix (add-to-stack for an
    out-of-discovery beacon) is a `depends_on` and is expected to be live in the
    same fresh runtime, which unblocks test 9's "pin an individual entry then let
    it expire" flow.
  </what-built>
  <read_first>
    - .planning/phases/13-cross-cutting/13-UAT.md gaps for tests 5, 6, 7 (all root_cause: "NOT A CODE DEFECT — stale HMR runtime"; `missing:` = "Restart dev server + hard reload, re-run UAT 5/6/7/9") and test 9 (`blocked`, `blocked_by: other` — "Cannot create a pinned individual stack entry to age out ... Re-test after the Test 5/6/7 Map Stack UI fixes land")
    - .planning/debug/mapstack-ui-surface-absent.md (the stale-runtime diagnosis: committed source correct, live bundle divergent)
    - .planning/phases/13-cross-cutting/13-04-SUMMARY.md ("Add-to-stack" affordance wiring, cold-start seed site + `aggregateLayersSeededRef` guard, expiry-sweep effect location — the behaviors to confirm)
    - CLAUDE.md (Bun runtime — `bun dev` runs `./scripts/dev-clean.sh`; `bun --hot src/index.ts` is the HMR server; hard reload = the browser step)
  </read_first>
  <action>
    Bring the running app to a CLEAN Phase-13 runtime, then re-run the four UAT
    tests. Steps (the executor automates the restart; the HUMAN performs the browser
    verification):
    (1) Identify and kill the stale dev-server process: find the running
        `bun --hot src/index.ts` / `bun dev` process (the UAT names PID 95035 as the
        stale one; the actual PID may differ now) and terminate it (e.g.
        `pkill -f "bun --hot src/index.ts"` or the project's `bun relay:kill`-style
        stop for the app server). Confirm no stale `bun --hot` process remains.
    (2) Start a fresh dev server: `bun dev` (runs `./scripts/dev-clean.sh`). Wait
        for it to report ready.
    (3) HUMAN: hard-reload the browser (Cmd/Ctrl+Shift+R) on the app so the fresh
        bundle loads (no service-worker/HTTP cache).
    (4) HUMAN: re-run UAT tests 5, 6, 7, 9 exactly as specified in `13-UAT.md`, and
        report pass/issue per test (see acceptance criteria for the expected
        behavior of each).
    Do NOT write any source fix in this plan. If a test PASSES, it closes that gap.
    If a test still FAILS after the clean restart + hard reload, STOP and record it
    as a NEWLY-CONFIRMED real bug (not a stale-runtime artifact) with the observed
    symptom — it then warrants its own debug/plan, separate from this verification.
  </action>
  <acceptance_criteria>
    - No stale `bun --hot src/index.ts` process remains before the fresh start (the restart actually happened; the running bundle is post-Plan-03/04).
    - Test 5: the "Add to map stack" button RENDERS on the beacon view panel AND the sighting view panel AND on their rail rows (beacon/sighting rail rows show an add affordance, not only locate + inspect). (Already partially confirmed post-restart for the beacon panel — confirm the sighting panel + both rails too.)
    - Test 6: MapStackPanel shows a top-pinned "Sightings" row and a top-pinned "Live beacons" row (above dataset/context entries); toggling a row's visibility OFF removes that whole layer from the map, toggling ON restores it.
    - Test 7: a fresh Browse cold-start (no shared/deep link, no `?ms=`) already contains BOTH aggregate layers ("Sightings" + "Live beacons"), visible/on by default, so sightings and live beacons show with no manual step; both are removable/toggleable.
    - Test 9: pin an individual sighting/beacon entry to the stack (via the now-visible add-to-stack button + the Plan 13-06 fix) and confirm it AUTO-REMOVES from the Map Stack once its entity expires (NIP-40 `expiration`) — no leftover "ended"/tombstone row lingers.
    - The re-run results (pass/issue per test) are recorded, updating the four gap statuses in the phase UAT tracking. Any persistent failure is flagged as a new real bug, NOT silently marked pass.
  </acceptance_criteria>
  <resume-signal>Type "5/6/7/9 pass" (or report which test still fails after the clean restart, with the observed symptom).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed by this plan. It changes NO source, opens NO new subscription or
network surface, writes NO data, and touches NO trust boundary — it restarts a
local dev process and performs manual browser verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07-01 | (n/a) | verification-only plan | accept | `files_modified: []`. No code change, no new dependency, no new subscription, no new data flow, no new UI surface. The only action is killing/restarting a local dev process and a human re-running existing UAT steps. Minimal threat surface — nothing new to attack. The behaviors being verified were already threat-modeled under Plan 13-03 (T-13-03-*) and Plan 13-04 (T-13-04-EXPIRELEAK/GPSREGRESS/COLDSTART/XSS); this plan introduces none. |
| T-13-07-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. `bun dev` runs the existing `./scripts/dev-clean.sh`; no new dependency is added by this plan. No slopcheck checkpoint needed. |

Explicit statement (per the security gate): this is a verification-only plan with
minimal threat surface — no code, dependency, subscription, or data-flow change.
</threat_model>

<verification>
- Manual UAT re-run of tests 5, 6, 7, 9 against a freshly-restarted dev server +
  hard-reloaded browser (the human-verify checkpoint).
- No automated source assertion applies (no code changed). The already-green
  `bun test src/features/geo-editor/GeoEditorView.stackLayers.test.ts` (9/9) and
  `bun test src/components/MapStackPanel.layerEntries.test.ts` (4/4) remain the
  static proof that the committed source is correct; this plan proves the RUNTIME
  matches the source once the stale bundle is gone.
</verification>

<success_criteria>
- UAT tests 5, 6, 7, and 9 all PASS on re-run against the clean runtime — the
  stale-HMR-runtime cluster is closed by verification.
- If any test still fails after the clean restart + hard reload, it is recorded as
  a newly-confirmed real bug (with symptom) for separate diagnosis — the plan does
  NOT mark it pass and does NOT fabricate a fix.
</success_criteria>

<output>
Create `.planning/phases/13-cross-cutting/13-07-SUMMARY.md` when done, recording
the re-run pass/issue result for each of tests 5/6/7/9 and (if any) the
newly-confirmed real bug to route to a separate plan.
</output>
</content>
