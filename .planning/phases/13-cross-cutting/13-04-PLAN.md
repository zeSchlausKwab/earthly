---
phase: 13-cross-cutting
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - src/features/geo-editor/GeoEditorView.tsx
  - src/components/info-panel/BeaconViewPanel.tsx
  - src/components/info-panel/SightingViewPanel.tsx
  - src/components/BeaconsPanel.tsx
  - src/components/SightingsPanel.tsx
  - src/components/MapStackPanel.tsx
  - src/components/MapStackPanel.layerEntries.test.ts
autonomous: true
requirements: [XCUT-01, XCUT-02]
tags: [map-stack, ui, aggregate-layer, cold-start, expiry, beacon, sighting]

must_haves:
  truths:
    - "An 'Add to map stack' affordance exists on BeaconViewPanel/SightingViewPanel and on the Beacons/Sightings rail rows, mirroring the dataset onAddDatasetToMap affordance (SPEC §3.4)"
    - "Aggregate 'Sightings' / 'Live beacons' layer entries render in MapStackPanel, toggle the whole subscription-driven layer, and pin to the TOP above dataset/context entries (D-05, SPEC §3.4)"
    - "Cold-start Browse auto-adds both aggregate layer entries (source:'browse-default', visible:true, entityKey:'all') so today's always-on behavior is preserved but now removable/toggleable, Clear-aware (SPEC §3.3)"
    - "A pinned individual sighting/beacon whose NIP-40 expiration passes has its Map Stack entry AUTO-REMOVED (D-02, dropExpired parity — no tombstone rows)"
  artifacts:
    - path: "src/components/MapStackPanel.tsx"
      provides: "sightingLayerEntries/beaconLayerEntries + individual sighting/beacon rows rendered above dataset/context; entityType label/icon cases for the 4 new types"
      contains: "sighting-layer"
    - path: "src/components/info-panel/BeaconViewPanel.tsx"
      provides: "onAddToMapStack prop + Add-to-map-stack button beside Copy share link"
      contains: "onAddToMapStack"
    - path: "src/components/info-panel/SightingViewPanel.tsx"
      provides: "onAddToMapStack prop + Add-to-map-stack button"
      contains: "onAddToMapStack"
    - path: "src/components/MapStackPanel.layerEntries.test.ts"
      provides: "automated proof: aggregate layers sort to top; expiry sweep removes an expired individual entry (D-02, D-05)"
  key_links:
    - from: "cold-start browse seeding"
      to: "addSightingToMapStack/addBeaconToMapStack layer entries"
      via: "browse-default aggregate layer entries (entityKey:'all')"
      pattern: "browse-default"
    - from: "expiry sweep effect"
      to: "removeMapStackEntry(id)"
      via: "isExpired over the resolved individual entity → remove its stack entry"
      pattern: "removeMapStackEntry"
    - from: "BeaconViewPanel/SightingViewPanel Add-to-map-stack button"
      to: "addBeaconToMapStack/addSightingToMapStack"
      via: "onAddToMapStack prop wired from GeoEditorView"
      pattern: "onAddToMapStack"
---

<objective>
The UI + lifecycle surface of the Map Stack unification: the "Add to map stack"
affordances (SPEC §3.4), the aggregate layer entries rendered/toggled in
MapStackPanel pinned to top (D-05), cold-start browse-default seeding of both
aggregate layers (SPEC §3.3), and the pinned-entry expiry auto-remove sweep (D-02).
Plan 03 built the model + render gate + handlers; this plan exposes them to the
user and makes the defaults + expiry honest.

Purpose: Give users the SAME visibility controls for sightings/beacons that
datasets/contexts already have, preserve today's "it just works" default while
making the layers removable, and keep "on stack = visible" honest by auto-removing
expired individual entries. Advances the folded-in unification (D-01) and ROADMAP
SC-2. Completes the phase body started in Plan 03.
Output: Add-to-stack buttons; toggleable/top-pinned aggregate layer rows;
cold-start defaults; expiry auto-remove.
</objective>

<artifacts_this_phase_produces>
Symbols this plan CREATES (excluded from source-drift verification):
- New `onAddToMapStack?` prop on `BeaconViewPanelProps`, `SightingViewPanelProps`, `BeaconsPanelProps`, and the Sightings rail row props.
- New MapStackPanel memos `sightingLayerEntries`/`beaconLayerEntries` (mirror `datasetEntries`) + entityType label/icon cases for `sighting`/`beacon`/`sighting-layer`/`beacon-layer`.
- New cold-start seeding of aggregate layer entries + a pinned-entry expiry sweep effect in GeoEditorView.
- New test file `src/components/MapStackPanel.layerEntries.test.ts`.
Note: `addSightingToMapStack`/`addBeaconToMapStack` are created in Plan 03 (this plan wires them to UI). `toggleMapStackEntryVisible`/`removeMapStackEntry`/`setMapStackEntryIsolated` are existing slice methods. `isExpired`/`dropExpired` are Phase-8 helpers.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-cutting/13-CONTEXT.md
@.planning/phases/13-cross-cutting/13-PATTERNS.md
@.planning/design/map-stack-entity-layers-SPEC.md
@.planning/phases/13-cross-cutting/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render aggregate + individual entries in MapStackPanel (top-pinned) + RED test</name>
  <read_first>
    - src/components/MapStackPanel.tsx:194-249 (the row component's entityType branches for label/icon — L245-249 the dataset/context/draft icon switch, L282 the label) to add sighting/beacon/*-layer cases; L525-527 the props (contextEntries/datasetEntries/otherEntries); L627-655 the render order (contextEntries → datasetEntries → otherEntries); L727-747 the entityType-filtered memos (draftEntries L727, contextEntries L730, datasetEntries L734, otherEntries L738) to add sightingLayerEntries/beaconLayerEntries; L752-772 the isolatedEntry handling to confirm the new types flow through toggle/isolate/remove
    - .planning/design/map-stack-entity-layers-SPEC.md §3.1 (entityKey:'all' for aggregate), §3.4 (aggregate toggled from the Map Stack panel), open-Q4 resolved as D-05 (aggregate layers pin to TOP)
    - src/features/geo-editor/store/mapStackSlice.ts:47-56 (removeMapStackEntry), :70-80 (toggleMapStackEntryVisible), :82-98 (setMapStackEntryIsolated) — the existing row actions the new types must reuse
  </read_first>
  <behavior>
    - Test: given a mixed stack (dataset entry, context entry, sighting-layer entry, beacon-layer entry), the panel's render order places sighting-layer + beacon-layer entries ABOVE dataset/context entries (D-05 top-pin). Test the pure ordering/bucketing memo, not the DOM.
    - Test: an individual `sighting`/`beacon` entry buckets into a rendered group (not silently dropped) and carries a label/icon (entityType case exists — no 'unknown' fallthrough).
  </behavior>
  <action>
    In MapStackPanel.tsx: add `sightingLayerEntries` and `beaconLayerEntries` memos mirroring `datasetEntries` (L734-737) filtering `entityType === 'sighting-layer'` / `'beacon-layer'`. Add `sightingEntries`/`beaconEntries` for individual pins (or let them stay in `otherEntries` — the L738-747 bucket already catches them; per PATTERNS the aggregate ones need explicit top-pin, individuals can ride otherEntries). Render the aggregate layer groups ABOVE contextEntries/datasetEntries in the render body (before L627), per D-05. Add `entityType === 'sighting' | 'beacon' | 'sighting-layer' | 'beacon-layer'` cases to the row label (L282) and icon (L245-249) switches so no new type falls through to a blank/unknown label (aggregate: "Sightings" / "Live beacons"; individual: the entity label). Pass the new bucket arrays through the same props threading contextEntries/datasetEntries/otherEntries use (L525-527, L553-555, L627-655). Per-entry toggle/isolate/remove already work via the existing toggleMapStackEntryVisible/setMapStackEntryIsolated/removeMapStackEntry wiring — verify the new types are not special-cased out. To make ordering testable, extract the bucket-and-order logic into a pure helper if it is inline; the test imports it. Write the RED test at src/components/MapStackPanel.layerEntries.test.ts for the top-pin ordering + individual-entry bucketing behaviors.
  </action>
  <verify>
    <automated>bun test src/components/MapStackPanel.layerEntries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sighting-layer\|beacon-layer" src/components/MapStackPanel.tsx` returns >= 4 (2 bucket memos + label + icon cases).
    - `grep -c "sightingLayerEntries\|beaconLayerEntries" src/components/MapStackPanel.tsx` returns >= 2.
    - `bun test src/components/MapStackPanel.layerEntries.test.ts` exits 0 (top-pin ordering + individual bucketing GREEN).
    - `bun run build` succeeds; `bunx biome check src/components/MapStackPanel.tsx` passes.
  </acceptance_criteria>
  <done>Aggregate layer entries render top-pinned; individual sighting/beacon entries render with proper label/icon; toggle/isolate/remove reuse existing wiring; test GREEN; build + biome pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add-to-stack affordances (view panels + rails) + cold-start defaults + expiry auto-remove</name>
  <read_first>
    - src/components/info-panel/BeaconViewPanel.tsx:262-270 (the "Copy share link" Button — the exact button shape to clone for Add-to-map-stack) + BeaconViewPanelProps
    - src/components/info-panel/SightingViewPanel.tsx:56-100 (SightingViewPanelProps + the action region ~L159-166 onZoomTo button) for the same affordance
    - src/components/BeaconsPanel.tsx (BeaconsPanelProps L57, onOpenBeacon; the row onOpen L103-140) + src/components/SightingsPanel.tsx (SightingsPanelProps L57, onOpenSighting; SightingRowProps L86-96, onOpen/onZoomTo L108-109; the row action region L195-211) — add an onAddToMapStack rail affordance mirroring how the datasets rail wires onAddDatasetToMap
    - src/features/geo-editor/GeoEditorView.tsx: the addSightingToMapStack/addBeaconToMapStack handlers from Plan 03; the browse-default dataset seeding site (seedRecentDatasets L668-676 + the cold-start trigger) — the analog for seeding aggregate layer entries; the BeaconViewPanel/SightingViewPanel mount sites (~L2205 focusCommentId region, ~L2170 sighting; ~L2311, ~L2521, ~L2547) and the BeaconsPanel/SightingsPanel mount sites (~L2173 onInspectSighting, ~L2190/L2538 onInspectBeacon/onOpenBeacon, ~L2311) to pass the new onAddToMapStack props
    - src/lib/nostr/expiry.ts (isExpired/dropExpired) + src/lib/hooks/useSightings.ts + useBeacons.ts (the subscription sets to resolve individual entries against for the expiry sweep)
    - .planning/design/map-stack-entity-layers-SPEC.md §3.3 (cold-start browse-default layer entries, Clear-aware) + open-Q3 resolved as D-02 (auto-remove on expiry)
  </read_first>
  <action>
    VIEW PANELS: add `onAddToMapStack?: (entity) => void` to BeaconViewPanelProps and SightingViewPanelProps; render an "Add to map stack" `<Button variant="outline">` cloning the BeaconViewPanel Copy-share-link button shape (L262-270), gated on the prop being present. Wire from GeoEditorView: pass `onAddToMapStack={addBeaconToMapStack}` / `addSightingToMapStack` to the respective panel mounts.
    RAILS: add an `onAddToMapStack` handler prop to BeaconsPanel and SightingsPanel row props; render a small add affordance on the rail row mirroring the datasets rail's onAddDatasetToMap; wire from GeoEditorView to addBeaconToMapStack/addSightingToMapStack (source 'manual' default → toasts + non-isolated visible entry).
    COLD-START DEFAULTS: at the browse cold-start (mirror the seedRecentDatasets / browse-default seeding site L668-676), auto-add ONE `sighting-layer` and ONE `beacon-layer` entry with `source:'browse-default'`, `visible:true`, `entityKey:'all'`, `isolated:false` — only if not already present (idempotent, and Clear-aware because browse-default entries clear normally per mapStackSlice.clearMapStack L169-185). This preserves today's always-on sightings/beacons while making them toggleable/removable.
    EXPIRY AUTO-REMOVE (D-02): add a sweep effect that, on the existing sighting/beacon expiry tick, iterates the stack's individual `sighting`/`beacon` entries, resolves each against the subscription (or treats as expired if no longer resolvable), and calls `removeMapStackEntry(id)` for any whose resolved entity `isExpired` (or has dropped from the dropExpired'd subscription). Aggregate `*-layer` entries are NOT swept (they gate the whole subscription which self-drops expired via buildSource). No tombstone rows.
  </action>
  <verify>
    <automated>bun run build && bunx biome check src/components/info-panel/BeaconViewPanel.tsx src/components/info-panel/SightingViewPanel.tsx src/components/BeaconsPanel.tsx src/components/SightingsPanel.tsx src/features/geo-editor/GeoEditorView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c onAddToMapStack src/components/info-panel/BeaconViewPanel.tsx` >= 1 AND src/components/info-panel/SightingViewPanel.tsx >= 1 AND src/components/BeaconsPanel.tsx >= 1 AND src/components/SightingsPanel.tsx >= 1.
    - `grep -c "onAddToMapStack={addBeaconToMapStack}\|onAddToMapStack={addSightingToMapStack}" src/features/geo-editor/GeoEditorView.tsx` >= 2 (both panels wired).
    - `grep -c "'sighting-layer'\|'beacon-layer'" src/features/geo-editor/GeoEditorView.tsx` >= 2 (cold-start seeds both aggregate entries).
    - `grep -c removeMapStackEntry src/features/geo-editor/GeoEditorView.tsx` >= 1 (expiry sweep calls it).
    - `bun run build` succeeds; `bunx biome check` on all five files passes.
  </acceptance_criteria>
  <done>Add-to-stack buttons on both view panels + both rails wired to the Plan-03 handlers; cold-start seeds both aggregate layers (Clear-aware, idempotent); expired individual entries auto-remove; build + biome green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → addMapStackEntry | manual add-to-stack creates a normal visible non-isolated entry |
| expiry tick → removeMapStackEntry | the sweep removes entries for expired entities |
| beacon geometry → map render (Phase 12 CR-01/CR-02) | rail/panel affordances must not surface link-only beacon position beyond intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-EXPIRELEAK | Information Disclosure | expired beacon/sighting lingering on stack | mitigate | D-02 auto-remove: the sweep removes an individual entry once its entity is expired/unresolvable, so a stale beacon is never shown as current (Phase-12 honesty posture). Aggregate layers self-drop via buildSource dropExpired. Acceptance: the expiry behavior is exercised (removeMapStackEntry called for expired entries); no tombstone rows persist. |
| T-13-04-GPSREGRESS | Information Disclosure | link-only beacon GPS-privacy (CR-01/CR-02) | mitigate | Cold-start seeds only the AGGREGATE `beacon-layer` (entityKey:'all'), which renders the `#t:['live']` DISCOVERY set — link-only beacons are absent from discovery (Phase 12 P-6) and are NOT surfaced by the aggregate layer or the rail. A link-only beacon still renders only via an explicit deep-link/add-to-stack action (Plan 03). Acceptance: cold-start adds only the aggregate discovery layer; no code path feeds a link-only beacon into the browse rail. |
| T-13-04-COLDSTART | Tampering | idempotent cold-start seeding | mitigate | The seed is guarded to add each aggregate entry only if absent (idempotent) and only on cold-start browse, and browse-default entries clear normally on Clear — a repeated cold-start cannot pile up duplicate layers or resurrect cleared ones (source distinguishes browse-default per types.ts L89-93). Acceptance: the seed checks for an existing entityKey:'all' entry of each type before adding. |
| T-13-04-XSS | Tampering | new entity labels rendered in MapStackPanel rows | mitigate | Entity labels (beacon/sighting title) render as escaped React text nodes in the existing row component — no dangerouslySetInnerHTML introduced. Acceptance: `grep -c dangerouslySetInnerHTML src/components/MapStackPanel.tsx == 0`. |
</threat_model>

<verification>
- `bun test src/components/MapStackPanel.layerEntries.test.ts` exits 0.
- `bun run build` succeeds.
- `bunx biome check` passes on all six touched source files.
- `grep -c dangerouslySetInnerHTML src/components/MapStackPanel.tsx` returns 0.
- Full suite regression: `bun test` shows no new failures vs baseline.
- Manual gate: cold-start seeds exactly one sighting-layer + one beacon-layer entry (idempotent); a manually-added sighting/beacon is a non-isolated visible entry; toggling an aggregate layer OFF removes its entities from the map (via the Plan-03 selector).
</verification>

<success_criteria>
- Add-to-map-stack affordance on both view panels + both rails, wired to the Plan-03 handlers (SPEC §3.4).
- Aggregate layer entries render top-pinned, toggle the whole layer (D-05); cold-start preserves today's default while making them removable/toggleable + Clear-aware (SPEC §3.3).
- Pinned individual entries auto-remove on expiry (D-02); no tombstone rows.
- Build + biome + targeted test green; no regression; GPS-privacy posture preserved.
</success_criteria>

<output>
Create `.planning/phases/13-cross-cutting/13-04-SUMMARY.md` when done. Note the
cold-start seed site + guard, the expiry-sweep effect location, and confirm the
full 4-kind matrix + new stack behaviors (D-11) are ready for end-of-phase UAT.
</output>
