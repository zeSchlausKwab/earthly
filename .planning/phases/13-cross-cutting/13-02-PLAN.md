---
phase: 13-cross-cutting
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/features/geo-editor/hooks/useRouting.ts
  - src/features/geo-editor/hooks/useRouting.dispatch.test.ts
  - src/features/geo-editor/hooks/useBeaconController.ts
  - src/features/geo-editor/GeoEditorView.tsx
autonomous: true
requirements: [XCUT-02]
tags: [routing, naddr, deep-link, nip-19, beacon]

must_haves:
  truths:
    - "The five byte-identical per-kind route parsers are collapsed into ONE prefix→{focusType,sidebarView} lookup + one generic dispatcher body (D-08)"
    - "Every existing URL shape resolves byte-for-byte identically after the refactor (D-09): /geoevent/:naddr, /mapcontext/:naddr, /story/:naddr, /sighting/:naddr, /beacon/:naddr, /context/:naddr/... and the /comment/:id suffix"
    - "handleInspectBeacon threads route.commentId → beaconFocusCommentId → the BeaconViewPanel focusCommentId prop (added in Plan 01), closing the beacon comment-deep-link gap so all five kinds have parity (D-10)"
  artifacts:
    - path: "src/features/geo-editor/hooks/useRouting.ts"
      provides: "SHARE_ROUTES prefix→{focusType,sidebarView} lookup table + generic dispatch body replacing the 5 cloned blocks (L108-160)"
      contains: "SHARE_ROUTES"
    - path: "src/features/geo-editor/hooks/useBeaconController.ts"
      provides: "handleInspectBeacon(beacon, commentId?) + beaconFocusCommentId state, mirroring useSightingEditor"
      contains: "beaconFocusCommentId"
    - path: "src/features/geo-editor/hooks/useRouting.dispatch.test.ts"
      provides: "byte-for-byte parse assertions for all 5 share prefixes + comment suffix + scoped-context branch + malformed-naddr non-crash (D-09, D-11)"
  key_links:
    - from: "useRouting.parsePathSegments"
      to: "SHARE_ROUTES[first]"
      via: "single lookup + generic RouteState build"
      pattern: "SHARE_ROUTES\\[first\\]"
    - from: "GeoEditorView beacon dispatch site (~L1968)"
      to: "handleInspectBeacon(beacon, route.commentId)"
      via: "commentId thread mirroring the Sighting site ~L1954"
      pattern: "handleInspectBeacon\\(beacon, route\\.commentId\\)"
    - from: "useBeaconController.handleInspectBeacon"
      to: "BeaconViewPanel focusCommentId prop"
      via: "beaconFocusCommentId hook state → GeoEditorView mount prop (prop declared in Plan 01)"
      pattern: "focusCommentId=\\{beaconFocusCommentId\\}"
---

<objective>
Generalize entity routing (XCUT-02, D-08/D-09/D-10) WITHOUT changing any URL. The
"old single-context route shape" the roadmap describes replacing is already gone —
all five kinds already have clean-path deep-link routes. The debt is that
`useRouting.ts:108-160` holds FIVE byte-identical per-kind parser blocks (Pitfall
P-5). Collapse them into one `prefix→{focusType, sidebarView}` lookup table feeding
one generic dispatch body, and close the known beacon `commentId` gap (D-10) by
mirroring the Sighting hook's `focusCommentId` threading in the beacon controller.

Purpose: Pay down the "cloned per kind" routing debt so the next kind is trivial,
and guarantee comment-deep-link parity across all five kinds — satisfying ROADMAP
Phase-13 Success Criterion 2, with URL shapes preserved byte-for-byte (D-09).
Output: One route dispatcher; a beacon that honors `/beacon/:naddr/comment/:id`
deep links like Story/Sighting do.

DEPENDENCY NOTE: depends_on 13-01 because Task 2 passes `focusCommentId=
{beaconFocusCommentId}` into the BeaconViewPanel mount, and the `focusCommentId`
prop on `BeaconViewPanelProps` is DECLARED by Plan 01 (the comment mount). Plan 02
does NOT edit BeaconViewPanel.tsx — it only supplies the prop value from
GeoEditorView. This is why 02 is Wave 2, not Wave 1 (no same-wave file overlap with
Plan 01 on BeaconViewPanel.tsx).
</objective>

<artifacts_this_phase_produces>
Symbols this plan CREATES (excluded from source-drift verification):
- `SHARE_ROUTES` — the `Record<string, {focusType, sidebarView}>` lookup table in useRouting.ts.
- `beaconFocusCommentId` — new returned key on useBeaconController (mirrors `sightingFocusCommentId`).
- New `commentId?` param on `handleInspectBeacon`.
- New test file `src/features/geo-editor/hooks/useRouting.dispatch.test.ts`.
Note: the `focusCommentId` prop on BeaconViewPanelProps is created in Plan 01 (this plan supplies its value). RouteState/SidebarViewMode are existing types.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-cutting/13-CONTEXT.md
@.planning/phases/13-cross-cutting/13-PATTERNS.md
@.planning/phases/13-cross-cutting/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse the 5 per-kind parsers into a SHARE_ROUTES dispatcher + byte-for-byte RED test</name>
  <read_first>
    - src/features/geo-editor/hooks/useRouting.ts (the 5 cloned blocks L108-160 to collapse; RouteState L31-46; isSidebarViewMode L51-53 + isFocusType L55-65; the /user branch L89-106 the dispatcher slots after; the UNTOUCHED /context scoped branch L168-206; the generic sidebar tail L208-220; parseLocation hash fallback L231-245; upgradeLegacyHashRoute L259-266)
    - src/index.ts (confirm the OG share path is SERVER-SIDE route handlers L82-203 that redirect to the SAME client URL shapes — the client parser must keep recognizing /geoevent/, /mapcontext/, /story/, /sighting/, /beacon/ prefixes so redirects still land; NO src/index.ts change needed and it is NOT in files_modified)
  </read_first>
  <behavior>
    - Test: parsePathSegments(['geoevent', NADDR]) === {focusType:'geoevent', naddr:NADDR, commentId:undefined, sidebarView:'datasets'} — and identically for mapcontext→contexts, story→stories, sighting→sightings, beacon→beacons.
    - Test: the /comment/:id suffix parses — parsePathSegments(['beacon', NADDR, 'comment', CID]).commentId === CID; parsePathSegments(['beacon', NADDR, 'comment']).commentId === undefined (missing id).
    - Test: the scoped /context/:naddr/:view branch is UNCHANGED — parsePathSegments(['context', CTX_NADDR, 'datasets']) still returns a context-scoped RouteState (contextNaddr set), NOT a share-form match.
    - Test (D-11 malformed input): parsePathSegments(['beacon', 'not-a-valid-naddr']) does NOT throw and returns focusType 'beacon' with naddr passed through opaque (decode happens downstream; the parser is pubkey/kind-agnostic and must never crash on a bad naddr string).
  </behavior>
  <action>
    Define a module-level `const SHARE_ROUTES: Record<string, { focusType: RouteState['focusType']; sidebarView: SidebarViewMode }>` mapping `geoevent`→`{datasets}`, `mapcontext`→`{contexts}`, `story`→`{stories}`, `sighting`→`{sightings}`, `beacon`→`{beacons}`. In parsePathSegments, AFTER the /user branch (L106) and BEFORE the /context branch (L168), add one generic body: `const share = SHARE_ROUTES[first]; if (share && segments[1]) return { focusType: share.focusType, naddr: segments[1], commentId: segments[2] === 'comment' && segments[3] ? segments[3] : undefined, sidebarView: share.sidebarView }`. DELETE the five `if (first === '<kind>' && segments[1])` blocks (L110-160). Touch NOTHING else: the /context scoped branch (L168-206), isSidebarViewMode tail (L208-220), parseLocation (L231-245), upgradeLegacyHashRoute (L259-266), and the naddr encoders (L482-529, already generic `nip19.naddrEncode` — the kind travels inside the naddr) all stay byte-for-byte. Write the RED test at src/features/geo-editor/hooks/useRouting.dispatch.test.ts covering the four behaviors above (export or import parsePathSegments; if it is module-private, export it for test or exercise it through the public parse entrypoint).
  </action>
  <verify>
    <automated>bun test src/features/geo-editor/hooks/useRouting.dispatch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SHARE_ROUTES" src/features/geo-editor/hooks/useRouting.ts` returns >= 2 (definition + use).
    - `grep -c "first === 'geoevent'\|first === 'mapcontext'\|first === 'story'\|first === 'sighting'\|first === 'beacon'" src/features/geo-editor/hooks/useRouting.ts` returns 0 (the 5 cloned blocks are gone; the /context and /user string-equality checks remain and are NOT counted by this pattern).
    - `bun test src/features/geo-editor/hooks/useRouting.dispatch.test.ts` exits 0 (all four behaviors GREEN, proving byte-for-byte parity + non-crash on malformed naddr).
    - `bun run build` succeeds.
  </acceptance_criteria>
  <done>The five parsers are one lookup + one body; every URL shape parses identically; malformed naddr does not crash; test GREEN; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Close the beacon commentId gap — mirror the Sighting focusCommentId thread (D-10)</name>
  <read_first>
    - src/features/geo-editor/hooks/useSightingEditor.ts:67-69 (focusCommentId hook state), :102-122 (handleInspectSighting(sighting, commentId?) → setFocusCommentId(commentId), with the "navigateToView wipes the URL /comment/:id segment" rationale), :218-225 (returned `sightingFocusCommentId: focusCommentId`)
    - src/features/geo-editor/hooks/useBeaconController.ts:167-207 (handleInspectBeacon(beacon) with NO commentId param today), :216-256 (the return object to extend with beaconFocusCommentId)
    - src/features/geo-editor/GeoEditorView.tsx: the beacon dispatch site ~L1964-1970 (handleInspectBeacon(beacon), missing route.commentId) vs the Sighting site ~L1950-1956 (handleInspectSighting(sighting, route.commentId)); the useBeaconController destructure site + the BeaconViewPanel mount sites (~L2205, ~L2547 — where focusCommentId is already passed for Story/Sighting) to see how sightingFocusCommentId is wired to the panel prop
    - src/components/info-panel/BeaconViewPanel.tsx (READ ONLY — confirm Plan 01 added the `focusCommentId` prop to BeaconViewPanelProps; this task supplies its value from GeoEditorView, it does NOT edit this file)
  </read_first>
  <action>
    In useBeaconController: add a `focusCommentId` state via `useState<string | undefined>(undefined)` (mirror useSightingEditor L69). Add a `commentId?: string` param to `handleInspectBeacon` (L167) and call `setFocusCommentId(commentId)` inside it (mirror useSightingEditor L114) — hold it in hook state because navigateTo/navigateToView wipes the URL `/comment/:id` segment (same rationale as the Sighting comment at L112-113). Return `beaconFocusCommentId: focusCommentId` in the return object (L216-256), mirroring `sightingFocusCommentId`. In GeoEditorView: change the beacon dispatch site (~L1968) from `handleInspectBeacon(beacon)` to `handleInspectBeacon(beacon, route.commentId)` (matching the Sighting site ~L1954); destructure `beaconFocusCommentId` from useBeaconController; pass `focusCommentId={beaconFocusCommentId}` to every BeaconViewPanel mount (the ~L2205 and ~L2547 region — wherever BeaconViewPanel is rendered), mirroring how `focusCommentId={focusCommentId}`/`sightingFocusCommentId={sightingFocusCommentId}` are already passed. Do NOT edit BeaconViewPanel.tsx (Plan 01 owns its props); do NOT change the /beacon/:naddr URL shape or the naddr resolution at L1964-1966 (the account-free {authors,#d} fallback stays).
  </action>
  <verify>
    <automated>bun run build && bunx biome check src/features/geo-editor/hooks/useBeaconController.ts src/features/geo-editor/GeoEditorView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "beaconFocusCommentId" src/features/geo-editor/hooks/useBeaconController.ts` returns >= 1 AND in src/features/geo-editor/GeoEditorView.tsx returns >= 2 (destructure + panel prop pass).
    - `grep -c "handleInspectBeacon(beacon, route.commentId)" src/features/geo-editor/GeoEditorView.tsx` returns >= 1 (dispatch threads commentId).
    - `grep -c "commentId" src/features/geo-editor/hooks/useBeaconController.ts` returns >= 1 (param present on handleInspectBeacon).
    - `git diff --stat src/components/info-panel/BeaconViewPanel.tsx` shows NO change in THIS plan's commit (Plan 01 owns that file).
    - `bun run build` succeeds; `bunx biome check` on the two changed source files passes.
  </acceptance_criteria>
  <done>handleInspectBeacon accepts + stores commentId; GeoEditorView threads route.commentId at the beacon dispatch and passes beaconFocusCommentId to the BeaconViewPanel focusCommentId prop; a /beacon/:naddr/comment/:id deep link focuses the comment; build + biome green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (untrusted) → parsePathSegments | any path/hash segment (incl. attacker-crafted naddr) enters the dispatcher |
| naddr string → nip19.decode (downstream) | the raw naddr from the URL is decoded later; the parser must pass it opaque without crashing |
| route.commentId → CommentsPanel focus | a URL-supplied comment d-tag scrolls/highlights a comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-MALNADDR | Denial of Service | parsePathSegments / dispatcher | mitigate | The dispatcher treats `segments[1]` (naddr) as an OPAQUE string — it does NOT decode it, so a malformed naddr cannot throw in the parser (decode happens downstream in existing try/catch'd encoders like `decodeContextCoordinateFromNaddr` L67-76 which already returns undefined on failure). Acceptance: the RED test's malformed-naddr case (Task 1) asserts no throw. Do not add naddr validation to the parser (would change behavior vs. the 5 clones which also passed it opaque). |
| T-13-02-MISROUTE | Tampering | SHARE_ROUTES lookup | mitigate | The lookup is a closed `Record` keyed only by the five known prefixes; an unknown `first` falls through to the existing /context and sidebar-tail branches exactly as before — no arbitrary focusType/sidebarView can be injected via the URL because the mapping is table-driven, not derived from the segment. Acceptance: Task-1 test proves an unknown prefix does NOT match SHARE_ROUTES. |
| T-13-02-URLBREAK | Repudiation | shared beacon/story links + OG cards | mitigate | D-09: URL shapes stay byte-for-byte, so already-shared links and the server-side OG redirects (src/index.ts, unchanged) keep resolving. Acceptance: Task-1 byte-for-byte parse assertions for all five prefixes + the /comment suffix are GREEN; src/index.ts is NOT in files_modified. |
| T-13-02-GPS | Information Disclosure | beacon deep-link resolution (Phase 12 CR-01/CR-02) | mitigate | The commentId thread carries only a comment d-tag; it does NOT alter how the beacon position resolves or renders. The existing account-free {authors,#d} beacon resolution (L1964-1966) is untouched. Acceptance: no change to the beacon naddr-resolution block; `grep` confirms L1964-1966 resolution logic unchanged (only the handleInspectBeacon CALL gains a second arg). |
</threat_model>

<verification>
- `bun test src/features/geo-editor/hooks/useRouting.dispatch.test.ts` exits 0 (all five prefixes + comment suffix + scoped-context + malformed-naddr).
- `bun run build` succeeds.
- `bunx biome check src/features/geo-editor/hooks/useRouting.ts src/features/geo-editor/hooks/useBeaconController.ts src/features/geo-editor/GeoEditorView.tsx` passes.
- Full suite regression: `bun test` shows no new failures vs baseline.
- Manual grep gate: the 5 cloned parser blocks are gone (`SHARE_ROUTES` used instead); the /context scoped branch, hash fallback, and upgradeLegacyHashRoute are all still present (`grep -c "upgradeLegacyHashRoute\|decodeContextCoordinateFromNaddr" src/features/geo-editor/hooks/useRouting.ts` >= 2).
</verification>

<success_criteria>
- One SHARE_ROUTES lookup + one dispatch body replaces the five per-kind parsers (D-08).
- Every URL shape resolves byte-for-byte identically; malformed naddr does not crash (D-09, D-11).
- Beacon honors /beacon/:naddr/comment/:id deep links via beaconFocusCommentId, reaching parity with all five kinds (D-10).
- Build + biome + targeted test green; no regression; OG share path (server-side) untouched and still resolving.
</success_criteria>

<output>
Create `.planning/phases/13-cross-cutting/13-02-SUMMARY.md` when done. Note that
the OG server redirects (src/index.ts) were intentionally NOT modified — the
client parser preserving all prefixes is what keeps them resolving. Confirm
BeaconViewPanel.tsx was NOT edited (Plan 01 owns it).
</output>
