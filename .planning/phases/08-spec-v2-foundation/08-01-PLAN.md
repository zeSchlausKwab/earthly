---
phase: 08-spec-v2-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/nostr/tags.test.ts
  - src/lib/nostr/modelVersion.test.ts
  - src/lib/nostr/expiry.test.ts
  - src/lib/validation/schemaWorker.test.ts
  - src/lib/nostr/article/article.test.ts
  - src/lib/nostr/live-beacon/live-beacon.test.ts
  - src/lib/nostr/temporal-sighting/temporal-sighting.test.ts
autonomous: true
requirements: [SPEC-02, SPEC-03, SPEC-04, SPEC-05, TAX-01]
must_haves:
  truths:
    - "The six Nyquist Wave-0 test files exist and import the exact symbol names the foundation seams will export"
    - "Running the suite produces RED (import/reference failures), not silent pass — the contracts are pinned before implementation"
  artifacts:
    - path: "src/lib/nostr/tags.test.ts"
      provides: "SPEC-02 tag round-trip + shared-helper equality + TAX-01 L/l pairing/vocab/disjointness assertions"
      min_lines: 40
    - path: "src/lib/nostr/modelVersion.test.ts"
      provides: "SPEC-03 guard truth table {new, legacy, malformed} + no-throw assertions"
      min_lines: 25
    - path: "src/lib/nostr/expiry.test.ts"
      provides: "SPEC-05 isExpired/dropExpired against a fixed UTC clock"
      min_lines: 25
    - path: "src/lib/validation/schemaWorker.test.ts"
      provides: "SPEC-04 ReDoS/$ref/oversized fail-closed within timeout + compile-once-per-hash + $data-off via sync fallback"
      min_lines: 40
    - path: "src/lib/nostr/article/article.test.ts"
      provides: "SPEC-02 per-kind guard + create() d+modelVersion + cast getters for 37520"
      min_lines: 20
    - path: "src/lib/nostr/live-beacon/live-beacon.test.ts"
      provides: "SPEC-02 per-kind guard + create() + cast getters + expiresAt for 37521"
      min_lines: 20
    - path: "src/lib/nostr/temporal-sighting/temporal-sighting.test.ts"
      provides: "SPEC-02 per-kind guard + create() + cast getters for 37522"
      min_lines: 20
  key_links:
    - from: "src/lib/nostr/tags.test.ts"
      to: "src/lib/nostr/tags.ts (setLabels/getLabels/FEATURE_CATEGORY_VOCAB/EARTHLY_LABEL_NAMESPACE/getBbox/getHashtags/getContextRefs/getReferencedAddresses)"
      via: "import"
      pattern: "from ['\"].*tags['\"]"
    - from: "src/lib/validation/schemaWorker.test.ts"
      to: "src/lib/validation/schemaWorker.ts (validateSchema synchronous fallback)"
      via: "import"
      pattern: "from ['\"].*schemaWorker['\"]"
---

<objective>
Create the six Nyquist Wave-0 test stub files (`bun:test`) that pin the exact export contracts of every Phase 8 foundation seam BEFORE any implementation lands. These tests reference symbol names by their final identifiers, so they fail RED until Plans 02–04 implement them — establishing the Nyquist sampling baseline (Dimension 8).

Purpose: VALIDATION.md requires these six files to exist so per-requirement test coverage is measurable from the first task. Pinning the import contracts here forces every downstream plan to honor the agreed symbol names.
Output: 7 `*.test.ts` files (the schema-worker, modelVersion, expiry, tags suites + three per-kind suites) under `src/lib/nostr/` and `src/lib/validation/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-v2-foundation/08-VALIDATION.md
@.planning/phases/08-spec-v2-foundation/08-RESEARCH.md

# Analogs for test shape and the symbol contracts under test
@src/lib/nostr/map-context/helpers.ts
@src/lib/nostr/map-context/factory.ts
@src/features/chat/sandbox/transport/quickjsWorker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the four shared-seam test contracts (tags, modelVersion, expiry, schemaWorker)</name>
  <files>src/lib/nostr/tags.test.ts, src/lib/nostr/modelVersion.test.ts, src/lib/nostr/expiry.test.ts, src/lib/validation/schemaWorker.test.ts</files>
  <read_first>
    - `.planning/phases/08-spec-v2-foundation/08-VALIDATION.md` (Per-Task Verification Map — the exact behaviors each file asserts) and `08-RESEARCH.md` § Validation Architecture → Test Map rows SPEC-02/03/04/05 + TAX-01.
    - `src/lib/nostr/map-context/helpers.ts:75-85` (the defensive `JSON.parse` no-throw discipline SPEC-03 asserts) and `:94-138` (the bbox/t/c/a getter shapes `tags.ts` will own).
    - `src/features/chat/sandbox/transport/quickjsWorker.ts:40` (`WATCHDOG_SLACK_MS=500`), `:131-134` (the `typeof Worker === 'undefined'` synchronous fallback path the SPEC-04 tests exercise), `:149-156` (the watchdog timeout-kill the timing assertion bounds).
  </read_first>
  <action>
    Create four `bun:test` files importing from the not-yet-created modules by their final identifiers:
    - `src/lib/nostr/tags.test.ts`: import `getBbox`, `getGeohash`, `getHashtags`, `getLabels`, `getContextRefs`, `getReferencedAddresses`, `setBbox`, `setHashtags`, `setLabels`, `setContextRefs`, `setReferencedAddresses`, `FEATURE_CATEGORY_VOCAB`, `EARTHLY_LABEL_NAMESPACE` from `@/lib/nostr/tags`. Assert (SPEC-02) a synthetic event round-trips bbox/`t`/`c`/`a` identically, and that `getHashtags` over a `geo-event`-shaped and `map-context`-shaped event return equal arrays via the shared helper. Assert (TAX-01) `setLabels(tags, ['natural','route'])` yields exactly one `['L','earthly']` plus one `['l',v,'earthly']` per value; `setLabels(tags, [])` strips all `L`/`l`; `getLabels` reads back only `earthly`-namespaced `l` values; round-trip is stable; a `t` value equal to an `l` value is flagged/rejected (disjointness).
    - `src/lib/nostr/modelVersion.test.ts`: import `MODEL_VERSION`, `hasCurrentModelVersion` from `@/lib/nostr/modelVersion`. Assert the truth table over {new-model event (content carries `modelVersion === MODEL_VERSION`) ⇒ `true`, legacy event (no `modelVersion`) ⇒ `false`, malformed-JSON content ⇒ `false`} and that NONE of these throw (wrap each in a no-throw assertion). Assert a `filter(hasCurrentModelVersion)` over a mixed array excludes the legacy/malformed entries.
    - `src/lib/nostr/expiry.test.ts`: import `isExpired`, `dropExpired` from `@/lib/nostr/expiry`. Using a fixed UTC epoch-seconds clock, assert `isExpired` is `true` for `expiration < now`, `false` for a future expiration and for an event with no `expiration` tag; `dropExpired([...])` removes only the expired entries.
    - `src/lib/validation/schemaWorker.test.ts`: import `validateSchema` from `@/lib/validation/schemaWorker`. Driving the synchronous fallback (no live `Worker` under `bun test`), assert: (a) a ReDoS `pattern` schema (`^(a+)+$` over a long input), a `$ref`/`$dynamicRef` schema, and an oversized/deep schema each resolve to a fail-closed "could not validate" verdict within the timeout+slack wall-clock budget (bound the assertion by ≤100ms in-engine + 500ms slack per `quickjsWorker.ts:40`); (b) `$ref` is rejected before compile; (c) a valid schema compiled twice with the same `schemaHash` reuses the cached validator (assert compile invoked once via a spy/counter the worker module exposes); (d) `$data` keyword is off.
    NO fenced implementation in plan prose — write the assertions directly in the test files. These files MUST fail RED at this point (the imported modules do not yet exist); that is the intended Nyquist baseline.
  </action>
  <acceptance_criteria>
    - All four files exist at the paths above and parse as valid TypeScript test modules.
    - `bun test src/lib/nostr/tags.test.ts src/lib/nostr/modelVersion.test.ts src/lib/nostr/expiry.test.ts src/lib/validation/schemaWorker.test.ts` runs and reports FAILURES due to missing modules/exports (RED) — not zero tests, not silent pass.
    - Each file imports the exact symbol names listed in the action (these become the binding contract for Plans 02/03).
    - No file imports from a sibling implementation that does not appear in this plan's symbol list.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/tags.test.ts src/lib/nostr/modelVersion.test.ts src/lib/nostr/expiry.test.ts src/lib/validation/schemaWorker.test.ts 2>&1 | grep -Eq 'fail|error|Cannot find|not a function' && echo RED-OK</automated>
  </verify>
  <done>The four shared-seam test contracts exist and fail RED on missing-symbol references, pinning the export names for Plans 02 and 03.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the three per-kind scaffold test contracts (article, live-beacon, temporal-sighting)</name>
  <files>src/lib/nostr/article/article.test.ts, src/lib/nostr/live-beacon/live-beacon.test.ts, src/lib/nostr/temporal-sighting/temporal-sighting.test.ts</files>
  <read_first>
    - `src/lib/nostr/map-context/helpers.ts:67` (the `is<Entity>()` guard shape), `:18` (`KnownEvent<typeof KIND>` event type).
    - `src/lib/nostr/map-context/factory.ts:30-47` (`create()` writes content + generates `d` only if absent; `modify()` preserves `d`).
    - `src/lib/nostr/map-context/cast.ts:23-27` (constructor throws via guard) + `:46-90` (typed getters incl. `dTag`).
    - `08-RESEARCH.md` Pattern 1 (per-kind class names) + § Code Examples factory scaffold; `08-CONTEXT.md` D-01 (37520 Article / 37521 Beacon / 37522 Sighting).
  </read_first>
  <action>
    Create three `bun:test` files, each importing from its kind's (not-yet-created) barrel:
    - `src/lib/nostr/article/article.test.ts` ← `@/lib/nostr/article`: assert `isArticle()` accepts a well-formed 37520 event (has `d` tag + `modelVersion` content) and rejects a wrong-kind event; `ArticleFactory.create()` produces a template with a `d` tag and content carrying `modelVersion`; constructing the `Article` cast over a valid event exposes `dTag` and round-trips tags via the shared `tags.ts` getters.
    - `src/lib/nostr/live-beacon/live-beacon.test.ts` ← `@/lib/nostr/live-beacon`: same guard/create/cast assertions for kind 37521, plus assert the cast exposes `expiresAt` (NIP-40 `getExpirationTimestamp`).
    - `src/lib/nostr/temporal-sighting/temporal-sighting.test.ts` ← `@/lib/nostr/temporal-sighting`: same guard/create/cast assertions for kind 37522.
    Use class/function names per RESEARCH Pattern 1: `is<Entity>`, `<Entity>Factory`, `<Entity>` cast (`Article`/`LiveBeacon`/`TemporalSighting`). These files MUST fail RED (modules absent) — the intended baseline.
  </action>
  <acceptance_criteria>
    - All three files exist at the paths above and import the kind barrels by the exact symbol names (`isArticle`/`ArticleFactory`/`Article`, etc.).
    - `bun test src/lib/nostr/article src/lib/nostr/live-beacon src/lib/nostr/temporal-sighting` runs and reports FAILURES due to missing modules (RED).
    - Each kind's test asserts: guard accept/reject, `create()` emits `d`+`modelVersion`, cast exposes `dTag` (and `expiresAt` for live-beacon).
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/article src/lib/nostr/live-beacon src/lib/nostr/temporal-sighting 2>&1 | grep -Eq 'fail|error|Cannot find' && echo RED-OK</automated>
  </verify>
  <done>The three per-kind scaffold test contracts exist and fail RED, pinning the class/guard/factory symbol names for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan creates only test files; no runtime input crosses any boundary. The seams these tests pin DO introduce boundaries (covered in Plans 02–04 threat models). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-1 | Tampering | Test asserting a weaker contract than the seam requires (false-green) | mitigate | Tests reference the exact final symbol names and assert no-throw + fail-closed semantics; the RED-now requirement proves the assertions actually execute against absent modules rather than vacuously passing. |
</threat_model>

<verification>
- `bun test src/lib/nostr/ src/lib/validation/` runs without a framework-config error (Bun auto-discovers).
- All seven `*.test.ts` files report RED (missing-module references) — confirming the contracts are live, not vacuous.
- `bun run lint` is clean on the new test files (biome).
</verification>

<success_criteria>
The six VALIDATION.md Wave-0 test files (counting the three per-kind files as the single "per-kind" row) exist, import the foundation seams by their final symbol names, and fail RED — establishing the Nyquist sampling baseline that Plans 02–04 turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-spec-v2-foundation/08-01-SUMMARY.md` when done
</output>
