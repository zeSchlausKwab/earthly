---
phase: 08-spec-v2-foundation
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/lib/nostr/kinds.ts
  - src/lib/nostr/tags.ts
  - src/lib/nostr/modelVersion.ts
  - src/lib/nostr/expiry.ts
  - src/lib/nostr/geo-event/helpers.ts
  - src/lib/nostr/map-context/helpers.ts
  - src/lib/nostr/index.ts
autonomous: true
requirements: [SPEC-02, SPEC-03, SPEC-05, TAX-01]
must_haves:
  truths:
    - "kinds.ts exposes ARTICLE_KIND=37520, LIVE_BEACON_KIND=37521, TEMPORAL_SIGHTING_KIND=37522 (D-01 contiguous block)"
    - "A single tags.ts owns bbox/g/t/L/l/c/a read+write; geo-event/helpers.ts and map-context/helpers.ts consume it with no copy-pasted getter bodies"
    - "A user can apply NIP-32 L/l controlled-vocabulary labels with correct earthly namespace pairing; freeform t hashtags remain and must not double-encode an l value (disjointness)"
    - "A new-model event is recognized via the in-content modelVersion discriminator; an absent/unrecognized modelVersion or malformed content makes the guard return false WITHOUT throwing"
    - "The client can filter NIP-40-expired events on read via isExpired/dropExpired regardless of relay GC"
  artifacts:
    - path: "src/lib/nostr/kinds.ts"
      provides: "ARTICLE_KIND / LIVE_BEACON_KIND / TEMPORAL_SIGHTING_KIND constants"
      contains: "ARTICLE_KIND = 37520"
    - path: "src/lib/nostr/tags.ts"
      provides: "shared bbox/g/t/L/l/c/a read+write helpers + FEATURE_CATEGORY_VOCAB + EARTHLY_LABEL_NAMESPACE + disjointness guard"
      exports: ["getBbox", "setBbox", "getGeohash", "setGeohash", "getHashtags", "setHashtags", "getLabels", "setLabels", "getContextRefs", "setContextRefs", "getReferencedAddresses", "setReferencedAddresses", "FEATURE_CATEGORY_VOCAB", "EARTHLY_LABEL_NAMESPACE"]
      min_lines: 80
    - path: "src/lib/nostr/modelVersion.ts"
      provides: "MODEL_VERSION constant + hasCurrentModelVersion no-throw guard"
      exports: ["MODEL_VERSION", "hasCurrentModelVersion"]
    - path: "src/lib/nostr/expiry.ts"
      provides: "isExpired wrapper over applesauce-core/helpers/expiration + dropExpired"
      exports: ["isExpired", "dropExpired"]
  key_links:
    - from: "src/lib/nostr/map-context/helpers.ts"
      to: "src/lib/nostr/tags.ts"
      via: "import + delegate"
      pattern: "from ['\"].*tags['\"]"
    - from: "src/lib/nostr/geo-event/helpers.ts"
      to: "src/lib/nostr/tags.ts"
      via: "import + delegate"
      pattern: "from ['\"].*tags['\"]"
    - from: "src/lib/nostr/expiry.ts"
      to: "applesauce-core/helpers/expiration"
      via: "wraps isExpired"
      pattern: "from ['\"]applesauce-core/helpers/expiration['\"]"
---

<objective>
Stand up the five low-risk shared seams every new entity kind inherits: the three new kind constants (D-01), the extracted `tags.ts` (the only new abstraction) with both shipped kinds migrated to consume it, the NIP-32 `L`/`l` paired-emit helper + starter vocab (TAX-01), the in-content `modelVersion` discriminator with a no-throw guard (SPEC-03), and the NIP-40 `isExpired`/`dropExpired` wrapper (SPEC-05).

Purpose: These are the cross-cutting primitives Plans 04 (scaffolds) and Phases 9–13 build on. Extracting `tags.ts` here (and migrating `geo-event` + `map-context` onto it) eliminates the copy-paste before four more kinds multiply it.
Output: `kinds.ts` (+3 constants), new `tags.ts`/`modelVersion.ts`/`expiry.ts`, migrated `geo-event/helpers.ts` + `map-context/helpers.ts`, barrel re-exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-v2-foundation/08-CONTEXT.md
@.planning/phases/08-spec-v2-foundation/08-PATTERNS.md

# Extraction sources + the constant block to extend
@src/lib/nostr/kinds.ts
@src/lib/nostr/map-context/helpers.ts
@src/lib/nostr/geo-event/helpers.ts
@src/lib/nostr/map-context/factory.ts
@src/lib/worldGeohash.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the three kind constants and extract the shared tags.ts (migrate both shipped consumers)</name>
  <files>src/lib/nostr/kinds.ts, src/lib/nostr/tags.ts, src/lib/nostr/geo-event/helpers.ts, src/lib/nostr/map-context/helpers.ts, src/lib/nostr/index.ts</files>
  <read_first>
    - `src/lib/nostr/kinds.ts:8-21` (existing doc-comment + `export const` style for the 37515/37517/37518/37519/34444 block — mirror it).
    - `src/lib/nostr/map-context/helpers.ts:60-65` (per-tag `Symbol.for` cache keys), `:94-138` (the bbox/`t`/`c`/`a` getters to generalize), and the `getOrComputeCachedValue` discipline at `:76,95,113`.
    - `src/lib/nostr/geo-event/helpers.ts:65-73` (`getBoundingBox`), `:75-77` (`getGeohash`), `:98-104` (`getHashtags`), `:114-120` (`getContextReferences`) — the second copy-paste source proving the bodies are near-identical.
    - `src/lib/nostr/map-context/factory.ts:66-121` (the filter-out-then-append setter shape: `bbox`/`hashtags`/`contextReferences`/`referencedAddresses`).
    - `src/lib/worldGeohash.ts:18` (`lonLatToWorldGeohash(precision, lon, lat)` for the `g` writer).
    - `08-PATTERNS.md` § `tags.ts` (read getter shape, write setter shape, NIP-32 block) and § Open Question O-2 (centralize read getters now; centralize write setters only if byte-identical, else leave per-factory — honor the tight-diff constraint).
  </read_first>
  <behavior>
    - getBbox/setBbox round-trips a `[w,s,e,n]` tuple identically; malformed/absent bbox ⇒ undefined.
    - getHashtags over a geo-event-shaped and a map-context-shaped event return equal arrays via the single shared helper (proves no drift).
    - getGeohash reads the `g` tag; setGeohash writes a precision-5–7 hash via lonLatToWorldGeohash.
    - getContextRefs/getReferencedAddresses read `c`/`a`; setters filter-out-then-append.
  </behavior>
  <action>
    (1) In `src/lib/nostr/kinds.ts`, append the contiguous block per D-01, mirroring the existing doc-comment style: `ARTICLE_KIND = 37520`, `LIVE_BEACON_KIND = 37521`, `TEMPORAL_SIGHTING_KIND = 37522`. Do NOT alter 37515/37517/37518/37519/34444 or the status kinds.
    (2) Create `src/lib/nostr/tags.ts` exporting generic pure read getters `getBbox`/`getGeohash`/`getHashtags`/`getContextRefs`/`getReferencedAddresses` (each over `NostrEvent.tags`, preserving the `getOrComputeCachedValue` memo with ONE shared `Symbol.for(...)` per tag name — not per entity kind) and pure `string[][] -> string[][]` write transformers `setBbox`/`setGeohash`/`setHashtags`/`setContextRefs`/`setReferencedAddresses` (filter-out-then-append). `setGeohash` derives the hash from a centroid via `lonLatToWorldGeohash`. Re-use the `GeoBoundingBox` type from `@/lib/nostr/geo-event`.
    (3) Migrate `src/lib/nostr/map-context/helpers.ts` and `src/lib/nostr/geo-event/helpers.ts` to delegate their bbox/`g`/`t`/`c`/`a` READ getters to `tags.ts` — keep each module's existing exported function names as thin re-export/delegate wrappers so no call site breaks (tight diff). Centralize the WRITE setters into `tags.ts` only where the two factories are byte-identical; otherwise leave the factory setters in place and have them call the `tags.ts` transformer. Do NOT touch content-shape getters (`getMapContextContent`, `getFeatureCollection`) — those stay per-kind.
    (4) Re-export `tags.ts` from `src/lib/nostr/index.ts`.
  </action>
  <acceptance_criteria>
    - `grep -nE 'ARTICLE_KIND = 37520|LIVE_BEACON_KIND = 37521|TEMPORAL_SIGHTING_KIND = 37522' src/lib/nostr/kinds.ts` returns all three.
    - `src/lib/nostr/tags.ts` exports the getters + setters named in frontmatter; `grep -v '^\s*//' src/lib/nostr/map-context/helpers.ts | grep -c 'getTagValue(event, .bbox.)'` is `0` AND the map-context bbox getter delegates to `tags.ts` (the copy-pasted body is gone from the consumer).
    - `bun test src/lib/nostr/tags.test.ts` passes the SPEC-02 round-trip + shared-helper-equality assertions (the geo-event vs map-context `getHashtags` equality test is GREEN).
    - `bun test` full suite stays green (no existing call site broke from the migration).
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/tags.test.ts && grep -nE 'ARTICLE_KIND = 37520|LIVE_BEACON_KIND = 37521|TEMPORAL_SIGHTING_KIND = 37522' src/lib/nostr/kinds.ts</automated>
  </verify>
  <done>The three kind constants exist (D-01), `tags.ts` owns the shared bbox/g/t/c/a read+write, and both shipped kinds consume it with the copy-pasted getter bodies removed; tags.test.ts SPEC-02 rows are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NIP-32 L/l paired-emit helper + starter vocab (TAX-01), modelVersion discriminator (SPEC-03), and isExpired/dropExpired wrapper (SPEC-05)</name>
  <files>src/lib/nostr/tags.ts, src/lib/nostr/modelVersion.ts, src/lib/nostr/expiry.ts, src/lib/nostr/index.ts</files>
  <read_first>
    - `08-PATTERNS.md` § `tags.ts` NIP-32 block (`setLabels`/`getLabels` shape; disjointness rule) and § `modelVersion.ts` (the `hasCurrentModelVersion` no-throw guard) and § `expiry.ts` (the one-line wrapper).
    - `src/lib/nostr/map-context/helpers.ts:75-85` (the defensive `JSON.parse` try/catch returning a default — the no-throw discipline `hasCurrentModelVersion` must preserve).
    - `node_modules/applesauce-core/dist/helpers/expiration.d.ts` (confirm `isExpired` / `getExpirationTimestamp` signatures) — referenced via `applesauce-core/helpers/expiration`.
    - `08-CONTEXT.md` D-05/D-06/D-07 (paired emit; flat `earthly` namespace literal; vocab `natural`/`infrastructure`/`amenity`/`route`/`boundary`) and D-03 (absence/mismatch ⇒ skip, no throw).
    - `08-RESEARCH.md` Assumptions A1 (`MODEL_VERSION` value is discretion — pick one stable string, e.g. `'earthly/2'`, and document it) and A3 (folding vs separate files is organizational).
  </read_first>
  <behavior>
    - setLabels(tags, ['natural','route']) ⇒ exactly one ['L','earthly'] + one ['l',v,'earthly'] per value; setLabels(tags, []) strips all L/l; getLabels reads back only earthly-namespaced l values; round-trip stable.
    - A t value equal to an l value is flagged/rejected by the disjointness guard.
    - hasCurrentModelVersion: new-model ⇒ true; legacy (no modelVersion) ⇒ false; malformed JSON ⇒ false; none throw.
    - isExpired ⇒ true when expiration<now, false for future/no-expiry; dropExpired removes only expired.
  </behavior>
  <action>
    (1) In `src/lib/nostr/tags.ts` add the NIP-32 taxonomy seam (TAX-01): `export const EARTHLY_LABEL_NAMESPACE = 'earthly'` (D-06), `export const FEATURE_CATEGORY_VOCAB = ['natural','infrastructure','amenity','route','boundary'] as const` (D-07), `setLabels(tags, values)` emitting exactly one `['L','earthly']` plus one `['l', value, 'earthly']` per value (replacing any existing `L`/`l` first), and `getLabels(event)` reading back only `earthly`-namespaced `l` values. Add a disjointness guard so a `t` value identical to an `l` value is flagged/rejected (do not silently double-encode). `setHashtags` must reject/strip any value already governed by `L`/`l`.
    (2) Create `src/lib/nostr/modelVersion.ts` (SPEC-03): `export const MODEL_VERSION = 'earthly/2'` (document the chosen literal) and `hasCurrentModelVersion(event)` which `JSON.parse`s `event.content` inside try/catch and returns `content?.modelVersion === MODEL_VERSION`, returning `false` (never throwing) on parse failure or absence. Re-export from `src/lib/nostr/index.ts`.
    (3) Create `src/lib/nostr/expiry.ts` (SPEC-05): `isExpired(event)` wrapping `applesauce-core/helpers/expiration::isExpired` (compare UTC epoch seconds only) and `dropExpired(events)` filtering out expired. Re-export from `src/lib/nostr/index.ts`.
  </action>
  <acceptance_criteria>
    - `bun test src/lib/nostr/tags.test.ts` passes the TAX-01 pairing/vocab/disjointness assertions (GREEN).
    - `bun test src/lib/nostr/modelVersion.test.ts` passes the SPEC-03 truth-table + no-throw assertions (GREEN).
    - `bun test src/lib/nostr/expiry.test.ts` passes the SPEC-05 fixed-clock assertions (GREEN).
    - `grep -q "EARTHLY_LABEL_NAMESPACE = 'earthly'" src/lib/nostr/tags.ts` and `grep -q "MODEL_VERSION = 'earthly/2'" src/lib/nostr/modelVersion.ts` (or the documented chosen literal).
    - `bun run lint` clean.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/tags.test.ts src/lib/nostr/modelVersion.test.ts src/lib/nostr/expiry.test.ts</automated>
  </verify>
  <done>TAX-01 (paired L/l + vocab + disjointness), SPEC-03 (modelVersion no-throw guard), and SPEC-05 (isExpired/dropExpired) seams exist and their Wave-0 tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay/cache → guard | A legacy or foreign-relay 37518 event (untrusted, possibly malformed `content`) is parsed by `hasCurrentModelVersion`; it must never crash the read path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering / Denial of Service (crash) | `hasCurrentModelVersion` parsing a legacy/malformed-content 37518 event during a list `filter`/`map` | mitigate | Defensive `JSON.parse` in try/catch returns `false` (never throws), mirroring `map-context/helpers.ts:75-85`; legacy/malformed events fall out of every render set (SPEC-03, D-03). Asserted by `modelVersion.test.ts` no-throw rows. |
| T-08-TAX | Tampering | NIP-32 `l` emitted without its paired `L` (lands in `ugc`) or a value double-encoded as both `t` and `l` | mitigate | `setLabels` emits the `['L','earthly']`+`['l',v,'earthly']` pair atomically from one call; disjointness guard rejects a `t` equal to an `l` (TAX-01). |
| T-08-EXP | (advisory) | NIP-40 expiry treated as relay-enforced, leaving expired events on the map | accept→mitigate | `isExpired`/`dropExpired` give a single client-side read filter; relay GC is never trusted (SPEC-05). No `high` residual — this seam is the mitigation, consumed by Phases 11/12. |
</threat_model>

<verification>
- `bun test src/lib/nostr/tags.test.ts src/lib/nostr/modelVersion.test.ts src/lib/nostr/expiry.test.ts` all GREEN.
- `bun test` full suite green (migration broke no existing consumer).
- `bun run build` green.
- `bun run lint` clean.
- No copy-pasted bbox/`t`/`c`/`a` getter body remains in `map-context/helpers.ts` or `geo-event/helpers.ts` (they delegate to `tags.ts`).
</verification>

<success_criteria>
The kind constants (D-01), shared `tags.ts` (consumed by both shipped kinds), NIP-32 taxonomy seam (TAX-01), `modelVersion` no-throw discriminator (SPEC-03), and NIP-40 `isExpired`/`dropExpired` wrapper (SPEC-05) all exist and turn their Wave-0 tests GREEN, with the full suite + build + biome green.
</success_criteria>

<output>
Create `.planning/phases/08-spec-v2-foundation/08-02-SUMMARY.md` when done
</output>
