---
phase: 08-spec-v2-foundation
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - SPEC.md
  - src/lib/nostr/spec.doc.test.ts
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "SPEC.md v2 documents the split entity model (Story 37520, slimmed Group 37518, Live Beacon 37521, Temporal Sighting 37522) with final kind-number assignments, replacing the overloaded kind-37518 context"
    - "SPEC.md v2 documents the modelVersion discriminator + clean-break (absence/mismatch ⇒ legacy/inert/skipped)"
    - "SPEC.md v2 documents the three-way L/l · t · c split, the earthly namespace, and FEATURE_CATEGORY_VOCAB"
    - "SPEC.md v2 documents the schema governance dialect (draft-2020-12, no $data, no external $ref, size/depth caps) and NIP-40 expiry advisory + client-always-filters"
    - "A doc-assertion test proves the required kind numbers, the modelVersion clause, and the three-way-split string are present in SPEC.md"
  artifacts:
    - path: "SPEC.md"
      provides: "v2 canonical spec covering all kinds + tag vocabulary + modelVersion + schema dialect + NIP-40 (in-place rewrite)"
      contains: "37520"
    - path: "src/lib/nostr/spec.doc.test.ts"
      provides: "SPEC-01 doc-assertion: required kind numbers + modelVersion clause + L/l·t·c split present"
      min_lines: 20
  key_links:
    - from: "src/lib/nostr/spec.doc.test.ts"
      to: "SPEC.md"
      via: "reads the doc and asserts required strings present"
      pattern: "SPEC\\.md"
---

<objective>
Rewrite `SPEC.md` IN PLACE to v2 (clean break; v1 stays in git history) documenting the whole split entity model and every Phase 8 seam now that the code exists. The spec is written LAST because it describes what the code became: the kind block (37520/37521/37522 + slimmed 37518), the `modelVersion` clean-break, the three-way `L`/`l`·`t`·`c` taxonomy split, the schema governance dialect, and the NIP-40 expiry contract. Also create the `spec.doc.test.ts` Wave-0 doc-assertion that pins required strings.

Purpose: SPEC-01 — the canonical spec must express each real use case as a distinct first-class entity instead of one overloaded "context", with final kind-number assignments.
Output: `SPEC.md` (v2, in place) + `src/lib/nostr/spec.doc.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-v2-foundation/08-CONTEXT.md
@.planning/phases/08-spec-v2-foundation/08-RESEARCH.md

# The v1 spec being rewritten + the now-shipped seams it documents
@SPEC.md
@src/lib/nostr/kinds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SPEC-01 doc-assertion test (spec.doc.test.ts)</name>
  <files>src/lib/nostr/spec.doc.test.ts</files>
  <read_first>
    - `08-VALIDATION.md` SPEC-01 row (the doc-assertion: a section per kind 37515/37518/37520/37521/37522, the kind numbers, the `modelVersion` clause, the `L/l·t·c` split present).
    - `08-RESEARCH.md` § Code Examples → "SPEC.md v2 — recommended section map" (the 10-section order + which strings must appear).
    - `SPEC.md` (current 421-line v1 — the rewrite target; confirm it is plain-prose with `N Title` headers and `Tag Example Notes` tables).
  </read_first>
  <action>
    Create `src/lib/nostr/spec.doc.test.ts` (`bun:test`) that reads `SPEC.md` from disk (via `Bun.file('SPEC.md').text()`) and asserts presence of: each kind number string `37515`, `37517`, `37518`, `37520`, `37521`, `37522`; a `modelVersion` clause (the discriminator + clean-break wording); the three-way split markers (`L`/`l`, `t`, `c` described as disjoint axes) and the `earthly` namespace literal; the `FEATURE_CATEGORY_VOCAB` member values (`natural`/`infrastructure`/`amenity`/`route`/`boundary`); and the schema-dialect clause (`draft-2020-12`, `$data` off / no external `$ref`) and NIP-40 advisory wording. Assert by substring/heading presence — keep it robust to prose phrasing (match on stable tokens like kind numbers and `modelVersion`, not exact sentences). This test will be RED until Task 2 rewrites SPEC.md.
  </action>
  <acceptance_criteria>
    - `src/lib/nostr/spec.doc.test.ts` exists and reads `SPEC.md` via `Bun.file`.
    - Before Task 2, `bun test src/lib/nostr/spec.doc.test.ts` is RED (the v1 spec lacks 37520/37521/37522 + the `modelVersion` clause).
    - The test asserts every kind number + the `modelVersion` clause + the three-way-split + dialect/NIP-40 tokens.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/spec.doc.test.ts 2>&1 | grep -Eq 'fail|expect' && echo RED-OK</automated>
  </verify>
  <done>The SPEC-01 doc-assertion exists and fails RED against the v1 spec, pinning the required-strings contract for the rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SPEC.md to v2 in place</name>
  <files>SPEC.md</files>
  <read_first>
    - `SPEC.md` (full v1 — preserve unchanged kind 37515 / 37516 collection / 34444 map-layer sections; note 37517 K/k widening is coming Phase 13).
    - `08-RESEARCH.md` § Code Examples "SPEC.md v2 — recommended section map" (the authoritative 10-section order and per-section content).
    - `08-CONTEXT.md` D-01 (kind assignments), D-02 (37522 reserved; representation confirmed Phase 11 — document as assigned-and-recommended dedicated kind with the note), D-03/D-04 (modelVersion clean break, silent drop), D-05/D-06/D-07 (NIP-32 paired emit, flat `earthly` namespace tradeoff note, FEATURE_CATEGORY_VOCAB), and the discretion defaults (schema dialect caps; NIP-40 advisory).
    - The shipped code to reference with `file_path:line_number` per CLAUDE.md: `src/lib/nostr/kinds.ts` (kind constants), `src/lib/nostr/tags.ts` (taxonomy), `src/lib/nostr/modelVersion.ts` (discriminator), `src/lib/validation/schema.worker.ts` (dialect), `src/lib/nostr/expiry.ts` (NIP-40).
  </read_first>
  <action>
    Rewrite `SPEC.md` IN PLACE (clean break — no parallel/versioned file) following the 10-section map from RESEARCH § Code Examples, in this order:
    1. GeoJSON Data Event (kind 37515) — unchanged semantics, keep.
    2. Geo Comment (kind 37517) — note K/k widening coming Phase 13.
    3. Group / Topic (kind 37518, SLIMMED) — `modelVersion` discriminator; `governance` ladder placeholder (open|schema|closed, defined Phase 9); `c`-attach + pinned-`a` two-lane; legacy-37518 clean break.
    4. Story / Article (kind 37520) — NIP-23 metadata; inline naddr→`a` mirror (Phase 10).
    5. Live Beacon (kind 37521) — replaceable + NIP-40 expiration (lifecycle Phase 12).
    6. Temporal Sighting (kind 37522) — NIP-52 start/end; NIP-40 expiry; D-02 representation note (number assigned, representation confirmed Phase 11).
    7. Tag vocabulary — bbox·g·t·L/l·c·a usage; the three-way `L/l`·`t`·`c` disjoint split; flat `earthly` namespace (D-06) WITH the deliberate collision-resistance tradeoff note vs reverse-DNS; FEATURE_CATEGORY_VOCAB members (D-07).
    8. modelVersion discriminator + clean break — absence/mismatch ⇒ legacy/inert/skipped (D-03/D-04), the chosen `MODEL_VERSION` literal.
    9. Schema governance dialect — draft-2020-12 pinned; no `$data`; no external `$ref`; size/depth caps; schema-hash (enforced Phase 9).
    10. Expiration (NIP-40) — advisory; client always filters on read (SPEC-05).
    Keep kinds 37516 (collection) and 34444 (map layer) sections if present (unchanged). Use `file_path:line_number` references to the shipped seams. Do NOT introduce migration/back-compat rendering language — the clean break is the documented stance.
  </action>
  <acceptance_criteria>
    - `bun test src/lib/nostr/spec.doc.test.ts` GREEN (all required kind numbers + `modelVersion` clause + three-way split + dialect/NIP-40 tokens present).
    - `grep -Eq '37520|37521|37522' SPEC.md` and `grep -q 'modelVersion' SPEC.md` and `grep -q 'earthly' SPEC.md`.
    - The overloaded kind-37518 "context" framing is replaced by the slimmed-Group + split-entity framing (no remaining description of 37518 as a multi-role discriminated union).
    - `bun test` full suite green; `bun run lint` clean.
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/spec.doc.test.ts && grep -Eq '37520|37521|37522' SPEC.md && grep -q 'modelVersion' SPEC.md</automated>
  </verify>
  <done>SPEC.md v2 documents the full split entity model with final kind numbers, the modelVersion clean break, the three-way taxonomy split, the schema dialect, and NIP-40 — and the SPEC-01 doc-assertion is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan rewrites documentation and adds a doc-assertion test; no runtime input crosses a boundary. The spec DOCUMENTS the boundaries the code plans (02/03/04) mitigate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-DOC | Tampering (drift) | SPEC.md documenting a contract that diverges from the shipped code (stale spec) | mitigate | The doc-assertion test pins kind numbers + `modelVersion` + the split markers against the doc; the rewrite cites shipped `file_path:line_number` anchors so the spec tracks the code. |
</threat_model>

<verification>
- `bun test src/lib/nostr/spec.doc.test.ts` GREEN.
- `bun test` full suite green.
- `bun run build` green; `bun run lint` clean.
- SPEC.md covers all 10 sections with final kind numbers and the clean-break stance (no migration language).
</verification>

<success_criteria>
SPEC.md v2 (in-place rewrite) documents the split entity model — Story 37520, slimmed Group 37518, Live Beacon 37521, Temporal Sighting 37522 — with final kind-number assignments, the `modelVersion` clean break, the three-way `L`/`l`·`t`·`c` split, the schema governance dialect, and the NIP-40 advisory, with the SPEC-01 doc-assertion GREEN (SPEC-01).
</success_criteria>

<output>
Create `.planning/phases/08-spec-v2-foundation/08-05-SUMMARY.md` when done
</output>
