---
phase: 08-spec-v2-foundation
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/lib/nostr/article/helpers.ts
  - src/lib/nostr/article/cast.ts
  - src/lib/nostr/article/factory.ts
  - src/lib/nostr/article/index.ts
  - src/lib/nostr/live-beacon/helpers.ts
  - src/lib/nostr/live-beacon/cast.ts
  - src/lib/nostr/live-beacon/factory.ts
  - src/lib/nostr/live-beacon/index.ts
  - src/lib/nostr/temporal-sighting/helpers.ts
  - src/lib/nostr/temporal-sighting/cast.ts
  - src/lib/nostr/temporal-sighting/factory.ts
  - src/lib/nostr/temporal-sighting/index.ts
  - src/lib/nostr/index.ts
autonomous: true
requirements: [SPEC-02, SPEC-03]
must_haves:
  truths:
    - "Each new kind (37520 Article, 37521 Beacon, 37522 Sighting) has Factory+Cast scaffolding (helpers.ts/cast.ts/factory.ts/index.ts) following the official applesauce casting contract"
    - "Every scaffold consumes the shared tags.ts for bbox/g/L/l/t/c/a — no copy-pasted tag logic"
    - "Each is<Entity>() guard requires kind match AND a recognized modelVersion, so legacy/absent-discriminator events fail the guard without throwing"
    - "Each create() writes modelVersion into content and generates d only if absent; modify() preserves d (no lineage fork)"
  artifacts:
    - path: "src/lib/nostr/article/helpers.ts"
      provides: "isArticle guard + ArticleEvent type + content/tag getters"
      contains: "ARTICLE_KIND"
    - path: "src/lib/nostr/article/cast.ts"
      provides: "Article EventCast read view"
      contains: "extends EventCast"
    - path: "src/lib/nostr/article/factory.ts"
      provides: "ArticleFactory create()/modify() with modelVersion injection + d-lineage discipline"
      contains: "EventFactory"
    - path: "src/lib/nostr/live-beacon/cast.ts"
      provides: "LiveBeacon EventCast with expiresAt (NIP-40)"
      contains: "getExpirationTimestamp"
    - path: "src/lib/nostr/temporal-sighting/factory.ts"
      provides: "TemporalSightingFactory create()/modify()"
      contains: "TEMPORAL_SIGHTING_KIND"
  key_links:
    - from: "src/lib/nostr/article/factory.ts"
      to: "src/lib/nostr/modelVersion.ts (MODEL_VERSION)"
      via: "writes modelVersion into create() content"
      pattern: "MODEL_VERSION"
    - from: "src/lib/nostr/article/helpers.ts"
      to: "src/lib/nostr/tags.ts"
      via: "delegates bbox/g/t/L/l/c/a reads"
      pattern: "from ['\"].*tags['\"]"
    - from: "src/lib/nostr/article/cast.ts"
      to: "applesauce-core/casts (EventCast)"
      via: "extends EventCast; consumed via castEvent"
      pattern: "from ['\"]applesauce-core/casts['\"]"
---

<objective>
Scaffold the three net-new entity kinds (37520 Article, 37521 Live Beacon, 37522 Temporal Sighting) following the repo's universal Factory+Cast house pattern, each consuming the shared `tags.ts` (no copy-pasted tag logic, SPEC-02) and gating its `is<Entity>()` guard on the `modelVersion` discriminator (SPEC-03). This is scaffolding ONLY — no per-kind authoring UI, no hooks, no render (Phases 9–13 own those).

Purpose: Deliver the Factory+Cast event-class seams the four entity phases inherit, proving the shared `tags.ts` + `modelVersion` contracts hold across three consumers at once.
Output: `helpers.ts`/`cast.ts`/`factory.ts`/`index.ts` per kind (12 files) + barrel re-exports from `src/lib/nostr/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-v2-foundation/08-CONTEXT.md
@.planning/phases/08-spec-v2-foundation/08-PATTERNS.md

# The 4× proven template to mirror verbatim
@src/lib/nostr/map-context/helpers.ts
@src/lib/nostr/map-context/cast.ts
@src/lib/nostr/map-context/factory.ts
@src/lib/nostr/map-context/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Article (37520) scaffold — helpers/cast/factory/index</name>
  <files>src/lib/nostr/article/helpers.ts, src/lib/nostr/article/cast.ts, src/lib/nostr/article/factory.ts, src/lib/nostr/article/index.ts, src/lib/nostr/index.ts</files>
  <read_first>
    - `src/lib/nostr/map-context/helpers.ts:18` (`KnownEvent<typeof KIND>` type), `:67-69` (guard shape), `:75-85` (defensive `JSON.parse` content getter).
    - `src/lib/nostr/map-context/cast.ts:5-6` (`EventCast`/`CastRefEventStore` imports), `:23-27` (constructor throws via guard), `:46-90` (typed getters incl. `dTag`, `boundingBox`, `hashtags`, `contextReferences`, `referencedAddresses`).
    - `src/lib/nostr/map-context/factory.ts:28-47` (`create()`/`modify()` + `d`-lineage discipline), `:66-121` (tag setters — delegate to `tags.ts`).
    - `src/lib/nostr/map-context/index.ts` (barrel shape).
    - Plan 02 outputs: `tags.ts` (getBbox/getGeohash/getHashtags/getLabels/getContextRefs/getReferencedAddresses + setters), `modelVersion.ts` (`MODEL_VERSION`, `hasCurrentModelVersion`), `kinds.ts` (`ARTICLE_KIND`). `08-CONTEXT.md` D-01 (37520 = Article, NIP-23-style long-form).
    - Casting contract: https://applesauce.build/apps/casting/events.html (`EventCast` + `castEvent`); `08-RESEARCH.md` Pattern 1.
  </read_first>
  <behavior>
    - isArticle(): accepts a well-formed 37520 event (has d tag + content modelVersion === MODEL_VERSION) ⇒ true; wrong-kind ⇒ false; legacy/absent-modelVersion ⇒ false without throwing.
    - ArticleFactory.create(): template has a d tag and content carrying modelVersion; modify() reuses the existing d.
    - Article cast over a valid event exposes dTag and routes bbox/t/c/a reads through tags.ts.
  </behavior>
  <action>
    Create `src/lib/nostr/article/{helpers,cast,factory,index}.ts` mirroring `map-context/` verbatim, adapted for `ARTICLE_KIND`:
    - `helpers.ts`: `export type ArticleEvent = KnownEvent<typeof ARTICLE_KIND>`; an `ArticleContent` interface (NIP-23-style: title/summary/image/content markdown — keep minimal, scaffolding only); a defensive `getArticleContent` (try/catch returning a default, never throws); `isArticle(event)` requiring `event.kind === ARTICLE_KIND && getTagValue(event,'d') !== undefined && hasCurrentModelVersion(event)`. Tag READ getters (bbox/g/t/L/l/c/a) delegate to `tags.ts` — do NOT re-implement them.
    - `cast.ts`: `export class Article extends EventCast<ArticleEvent>`; constructor throws `'Event is not an Article (kind 37520)'` via `isArticle`; raw-event proxies + typed getters (`dTag`, `boundingBox`, `hashtags`, `labels`, `contextReferences`, `referencedAddresses`) delegating to `tags.ts`/helpers.
    - `factory.ts`: `export class ArticleFactory extends EventFactory<typeof ARTICLE_KIND>` with `static create(content)` that sets `tpl.content = JSON.stringify({ modelVersion: MODEL_VERSION, ...content })` (SPEC-03) and generates `d` via `generateShortDTag()` ONLY if absent; `static modify(event)` from `toEventTemplate(event)` (never regenerates `d`); tag setters delegate to `tags.ts` (`setBbox`/`setGeohash`/`setHashtags`/`setLabels`/`setContextRefs`/`setReferencedAddresses`).
    - `index.ts`: barrel re-export of cast/factory/helpers. Add the barrel to `src/lib/nostr/index.ts`.
  </action>
  <acceptance_criteria>
    - `bun test src/lib/nostr/article` GREEN (guard accept/reject, create() d+modelVersion, cast dTag).
    - `grep -q 'extends EventCast' src/lib/nostr/article/cast.ts` and `grep -q 'hasCurrentModelVersion' src/lib/nostr/article/helpers.ts`.
    - `grep -q 'MODEL_VERSION' src/lib/nostr/article/factory.ts` (modelVersion injected on create).
    - No copy-pasted bbox/`t`/`c`/`a` getter body in `article/helpers.ts` — it imports from `tags.ts` (`grep -q "from '@/lib/nostr/tags'" src/lib/nostr/article/helpers.ts` or the cast).
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/article && grep -q 'extends EventCast' src/lib/nostr/article/cast.ts && grep -q 'MODEL_VERSION' src/lib/nostr/article/factory.ts</automated>
  </verify>
  <done>The Article (37520) Factory+Cast scaffold exists, consumes `tags.ts`, gates on `modelVersion`, and its Wave-0 test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live Beacon (37521) + Temporal Sighting (37522) scaffolds</name>
  <files>src/lib/nostr/live-beacon/helpers.ts, src/lib/nostr/live-beacon/cast.ts, src/lib/nostr/live-beacon/factory.ts, src/lib/nostr/live-beacon/index.ts, src/lib/nostr/temporal-sighting/helpers.ts, src/lib/nostr/temporal-sighting/cast.ts, src/lib/nostr/temporal-sighting/factory.ts, src/lib/nostr/temporal-sighting/index.ts, src/lib/nostr/index.ts</files>
  <read_first>
    - Same `map-context/` analogs as Task 1 (`helpers.ts:18,67-85`, `cast.ts:5-27,46-90`, `factory.ts:28-47,66-121`, `index.ts`).
    - `08-PATTERNS.md` § cast.ts note: Beacon/Sighting casts add `get expiresAt() { return getExpirationTimestamp(this.event) }` (NIP-40). Import `getExpirationTimestamp` from `applesauce-core/helpers/expiration`.
    - Plan 02 outputs (`tags.ts`, `modelVersion.ts`) and `kinds.ts` (`LIVE_BEACON_KIND`, `TEMPORAL_SIGHTING_KIND`).
    - `08-CONTEXT.md` D-01 (37521 Beacon = replaceable presence/position w/ NIP-40; 37522 Sighting = NIP-52 time-bounded observation) and D-02 (37522 number reserved; representation confirmed in Phase 11 — scaffold the dedicated kind regardless).
  </read_first>
  <behavior>
    - isLiveBeacon / isTemporalSighting: accept well-formed (d + modelVersion) ⇒ true; wrong-kind / legacy ⇒ false, no throw.
    - LiveBeaconFactory.create() / TemporalSightingFactory.create(): d + modelVersion content; modify() preserves d.
    - LiveBeacon cast exposes expiresAt (getExpirationTimestamp); TemporalSighting cast exposes dTag + tag getters via tags.ts.
  </behavior>
  <action>
    Create `src/lib/nostr/live-beacon/{helpers,cast,factory,index}.ts` and `src/lib/nostr/temporal-sighting/{helpers,cast,factory,index}.ts`, each mirroring Task 1's Article scaffold but for `LIVE_BEACON_KIND` / `TEMPORAL_SIGHTING_KIND`:
    - Classes/guards: `isLiveBeacon`/`LiveBeacon`/`LiveBeaconFactory` (kind 37521); `isTemporalSighting`/`TemporalSighting`/`TemporalSightingFactory` (kind 37522). Constructor error strings name the kind number.
    - Each `helpers.ts` keeps a minimal content interface (Beacon: position/label placeholder; Sighting: title/description + NIP-52 `start`/optional `end` placeholder — scaffolding only, no UI), a defensive content getter (no-throw), and an `is<Entity>()` guard gating on `kind` + `d` + `hasCurrentModelVersion`.
    - Each `cast.ts` extends `EventCast`, throws via its guard, exposes `dTag` + tag getters via `tags.ts`. The LiveBeacon AND TemporalSighting casts add `get expiresAt() { return getExpirationTimestamp(this.event) }` (NIP-40, both kinds are expiry-bearing).
    - Each `factory.ts` injects `MODEL_VERSION` into `create()` content, generates `d` only if absent, `modify()` preserves `d`; tag setters delegate to `tags.ts`.
    - Add both barrels to `src/lib/nostr/index.ts`.
  </action>
  <acceptance_criteria>
    - `bun test src/lib/nostr/live-beacon src/lib/nostr/temporal-sighting` GREEN (guard accept/reject, create() d+modelVersion, cast dTag, beacon expiresAt).
    - `grep -q 'getExpirationTimestamp' src/lib/nostr/live-beacon/cast.ts`.
    - `grep -q 'TEMPORAL_SIGHTING_KIND' src/lib/nostr/temporal-sighting/helpers.ts` and `grep -q 'LIVE_BEACON_KIND' src/lib/nostr/live-beacon/helpers.ts`.
    - Both scaffolds import tag getters from `tags.ts` (no re-implemented bbox/`t`/`c`/`a` bodies).
  </acceptance_criteria>
  <verify>
    <automated>bun test src/lib/nostr/live-beacon src/lib/nostr/temporal-sighting && grep -q 'getExpirationTimestamp' src/lib/nostr/live-beacon/cast.ts</automated>
  </verify>
  <done>The Live Beacon (37521) and Temporal Sighting (37522) Factory+Cast scaffolds exist, consume `tags.ts`, gate on `modelVersion`, expose `expiresAt`, and their Wave-0 tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay/cache → guard | Each new-kind `is<Entity>()` guard parses untrusted event `content` for the `modelVersion` discriminator; a malformed/legacy event must not crash the read path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-SCAFFOLD | Tampering / Denial of Service (crash) | An `is<Entity>()` guard throwing on a malformed/legacy event during a `filter(guard)`/`map` over a mixed timeline | mitigate | Each guard delegates the content read to `hasCurrentModelVersion` (defensive `JSON.parse`, no-throw, from Plan 02); kind+`d`+`modelVersion` mismatch ⇒ `false`, never raised. Asserted by each per-kind test's reject rows (SPEC-03). |
| T-08-LINEAGE | Tampering | `create()` re-running `generateShortDTag()` on edit forks the entity; reusing a `d` overwrites another | mitigate | `create()` generates `d` only if absent; `modify()` reuses `toEventTemplate(event)` and never regenerates `d` (mirrors `map-context/factory.ts:34-46`, Pitfall 5). |
</threat_model>

<verification>
- `bun test src/lib/nostr/article src/lib/nostr/live-beacon src/lib/nostr/temporal-sighting` all GREEN.
- `bun test` full suite green.
- `bun run build` green.
- `bun run lint` clean.
- Each scaffold's `cast.ts` extends `EventCast` (official casting contract) and each `factory.ts` injects `MODEL_VERSION` + honors `d`-lineage.
</verification>

<success_criteria>
Three Factory+Cast scaffolds (37520/37521/37522) exist following the official applesauce casting contract, all consuming the shared `tags.ts` (SPEC-02 — no copy-paste) and gating on the `modelVersion` discriminator (SPEC-03 — no-throw guards), with all per-kind Wave-0 tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-spec-v2-foundation/08-04-SUMMARY.md` when done
</output>
