---
phase: 07-geometry-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/safeEditing/pendingDiffStore.ts
  - src/features/chat/safeEditing/gateBulkEdit.ts
  - src/features/chat/safeEditing/DatasetDiffDisclosure.tsx
  - src/features/chat/safeEditing/DatasetDiffDisclosure.test.tsx
autonomous: true
requirements: [GEO-02]
must_haves:
  truths:
    - "When the optimizer applies through gateBulkApply, the inline diff block renders a metrics-aware optimization headline (bytes / vertices / features / microgap joins) instead of the generic +N/~N/−N counts wall (D-04b)"
    - "Phase 5 and Phase 6 callers that pass NO headline still render the existing +N added · ~N changed · −N deleted / ~N restyled string unchanged (fully backward-compatible)"
  artifacts:
    - path: "src/features/chat/safeEditing/pendingDiffStore.ts"
      provides: "optional headline on EmitDiffBlockOptions + PendingDiffEntry"
      contains: "headline"
    - path: "src/features/chat/safeEditing/gateBulkEdit.ts"
      provides: "optional headline in GateBulkDeps threaded into every emitDiffBlock"
      contains: "headline"
    - path: "src/features/chat/safeEditing/DatasetDiffDisclosure.tsx"
      provides: "headline branch in buildDatasetDiffSummary / render path"
      contains: "headline"
  key_links:
    - from: "src/features/chat/safeEditing/gateBulkEdit.ts"
      to: "src/features/chat/safeEditing/pendingDiffStore.ts"
      via: "emitDiffBlock(diff, { headline })"
      pattern: "emitDiffBlock\\("
    - from: "src/features/chat/safeEditing/DatasetDiffDisclosure.tsx"
      to: "PendingDiffEntry.headline"
      via: "render headline when present"
      pattern: "headline"
---

<objective>
Thread an optional metrics-aware `headline` through the safe-editing diff plumbing
(`gateBulkApply` → `emitDiffBlock` → `PendingDiffEntry` → `DatasetDiffDisclosure`) so the
Phase-7 optimizer can surface a single before/after optimization summary in the existing
inline diff block (D-04b / GEO-02), rather than a wall of per-feature geometry edits.

Purpose: This is the ONE new piece of plumbing Phase 7 adds to the Phase 5/6 gate. It must
be strictly additive — every existing caller passes no headline and must render exactly as
before (Phase 5/6 diff tests stay green).
Output: optional `headline` on `EmitDiffBlockOptions`, `PendingDiffEntry`, `GateBulkDeps`,
and a headline branch in `DatasetDiffDisclosure`, plus a render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-geometry-optimization/07-CONTEXT.md
@.planning/phases/07-geometry-optimization/07-RESEARCH.md
@.planning/phases/07-geometry-optimization/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan threads (full phase inventory is in 07-04-PLAN.md):
- `EmitDiffBlockOptions.headline?: string`, `PendingDiffEntry.headline?: string` (pendingDiffStore.ts)
- `GateBulkDeps.headline?: string` (gateBulkEdit.ts)
- `DatasetDiffDisclosure` headline render branch + `buildDatasetDiffSummary` headline precedence
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry an optional headline through the pending-diff store and the gate</name>
  <files>src/features/chat/safeEditing/pendingDiffStore.ts, src/features/chat/safeEditing/gateBulkEdit.ts</files>
  <read_first>
    - `src/features/chat/safeEditing/pendingDiffStore.ts:32` (`PendingDiffEntry`), `:45` (`EmitDiffBlockOptions`), `:83` (`emitDiffBlock` body that builds the stored entry) — the exact additive insertion points
    - `src/features/chat/safeEditing/gateBulkEdit.ts:39` (`GateBulkDeps`), `:108,:119,:124` (the THREE `emitDiffBlock(diff, …)` call sites the headline must reach: no-op, immediate-apply, confirm)
    - `.planning/phases/07-geometry-optimization/07-RESEARCH.md` §"Architecture Pattern 4" + 07-PATTERNS.md §"Additive edits to the safe-editing layer" (the backward-compat requirement: Phase 5/6 callers pass no headline)
  </read_first>
  <behavior>
    - Test 1 (pendingDiffStore): `emitDiffBlock(diff, { headline: 'X' })` stores a `PendingDiffEntry` whose `headline === 'X'`; `emitDiffBlock(diff)` stores an entry with `headline` undefined.
    - Test 2 (gateBulkEdit): when `deps.headline` is supplied, the emitted pending entry carries it (asserted via a stubbed/spied `emitDiffBlock` or by reading `getPendingDiff` after a Level-1 gate); when `deps.headline` is omitted, the entry's headline is undefined and the existing flow is unchanged.
  </behavior>
  <action>
    In `pendingDiffStore.ts`: add an optional `headline?: string` to BOTH `EmitDiffBlockOptions` (around :45) and `PendingDiffEntry` (around :32); in `emitDiffBlock` (:83) set `headline: opts?.headline` on the stored entry. Keep `snapshotCache` invalidation untouched. In `gateBulkEdit.ts`: add an optional `headline?: string` to `GateBulkDeps` (:39) with a doc note that it is the metrics-aware optimization summary; pass `{ status: 'applied', headline: deps.headline }` / `{ headline: deps.headline }` into all THREE `emitDiffBlock` call sites (the no-op early return :108, the immediate-apply :119, and the confirm-path :124) so the headline reaches the disclosure regardless of safety level. Signatures stay additive — existing Phase 5/6 callers (`bulk-tools.ts`) compile and behave identically. Do NOT change classification, snapshot, or confirm logic.
  </action>
  <verify>
    <automated>bun test src/features/chat/safeEditing/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/features/chat/safeEditing/` passes (all existing Phase 5/6 tests green + the new headline behaviors)
    - `grep -c "headline" src/features/chat/safeEditing/pendingDiffStore.ts` ≥ 2 (option + entry); `grep -c "headline" src/features/chat/safeEditing/gateBulkEdit.ts` ≥ 4 (dep + 3 call sites)
    - `EmitDiffBlockOptions` and `PendingDiffEntry` both have an OPTIONAL `headline?: string` (omittable — existing callers untouched)
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>An optional headline flows from `GateBulkDeps` through `emitDiffBlock` into the stored `PendingDiffEntry`; existing callers are byte-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the metrics-aware headline in the diff disclosure</name>
  <files>src/features/chat/safeEditing/DatasetDiffDisclosure.tsx, src/features/chat/safeEditing/DatasetDiffDisclosure.test.tsx</files>
  <read_first>
    - `src/features/chat/safeEditing/DatasetDiffDisclosure.tsx:40` (`buildDatasetDiffSummary` — note it ALREADY special-cases `~N restyled`, the exact precedent for a per-kind headline branch), `:111` (props), `:119` (the `useMemo` summary), `:140` (where `summary` renders)
    - `src/features/chat/safeEditing/pendingDiffStore.ts` — `PendingDiffEntry.headline` from Task 1 (the source of the prop)
    - the existing `DatasetDiffDisclosure.test.tsx` (extend it — DO NOT replace; the `~N restyled` and counts tests must stay green)
  </read_first>
  <behavior>
    - Test 1: given an entry with `headline: '12.0MB → 0.9MB · 41k→3.2k pts · 312→18 features · 47 joins'`, the disclosure renders that headline string verbatim and NOT the generic `+N added · ~N changed · −N deleted`.
    - Test 2 (backward-compat): given NO headline and a pure-restyle diff, the disclosure still renders `~N restyled`; given a mixed diff with no headline, it renders the verbatim counts string.
  </behavior>
  <action>
    Add an optional `headline?: string` prop to `DatasetDiffDisclosureProps` and (where the transcript wiring constructs the disclosure from a `PendingDiffEntry`) pass `entry.headline` through. In the summary computation (`buildDatasetDiffSummary` and/or the `useMemo` at :119): when a `headline` is present, render it in place of the count string; otherwise fall through to the EXISTING `~N restyled` / counts logic verbatim (lines 45-54 stay the fallback). Keep `buildDatasetDiffSummary(diff)` as the no-headline pure function (so existing unit tests calling it directly stay green) and apply the headline precedence at the component/`useMemo` layer (or add an overload that takes an optional headline). Extend `DatasetDiffDisclosure.test.tsx` with the two behaviors above. Do NOT alter the Added/Changed/Deleted expandable sections — the per-row list stays available behind the toggle; only the collapsed headline changes.
  </action>
  <verify>
    <automated>bun test src/features/chat/safeEditing/DatasetDiffDisclosure.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/features/chat/safeEditing/DatasetDiffDisclosure.test.tsx` passes incl. the new metrics-headline test and the unchanged `~N restyled` / counts tests
    - With a `headline` prop present, the rendered collapsed summary equals the headline string (asserted via render query), and the generic counts string is NOT shown
    - With no `headline`, output is byte-identical to current behavior (restyle → `~N restyled`; else `+N added · ~N changed · −N deleted`)
    - `buildDatasetDiffSummary(diff)` (no-arg-headline form) remains exported and unchanged in behavior for existing direct unit tests
    - `bun run build` succeeds; `biome check` clean on touched files
  </acceptance_criteria>
  <done>The collapsed diff headline shows the optimization metrics when a headline is supplied, and the existing counts/restyle headline otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → transcript UI | The headline is a host-built display string (from worker-computed metrics), rendered as React text — no HTML injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | metrics headline string | accept | Headline is built host-side from the optimizer's numeric report (07-04), not from model text; rendered as plain React children (no `dangerouslySetInnerHTML`), so no XSS surface. |
| T-07-05 | Information Disclosure | additive diff plumbing | mitigate | `gateBulkEdit`/`pendingDiffStore`/`DatasetDiffDisclosure` are main-thread chat-UI modules — no secret reach; change is a string field only, no new import edges to Nostr/signer. |
| T-07-06 | Repudiation | backward-compat regression | mitigate | Additive optional field; Phase 5/6 callers unchanged; the full `bun test src/features/chat/safeEditing/` suite (existing diff tests) must stay green as the regression gate. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase; slopcheck N/A. |
</threat_model>

<verification>
- `bun test src/features/chat/safeEditing/` green (existing Phase 5/6 + new headline behaviors).
- `bun run build` succeeds.
- `biome check src/features/chat/safeEditing/` clean on touched files.
- Grep confirms `headline` is OPTIONAL everywhere (no required-field break for existing callers).
</verification>

<success_criteria>
- Optional `headline` threads gate → store → disclosure.
- Metrics headline renders when present; existing counts/restyle headline renders otherwise.
- Zero Phase 5/6 diff-test regressions.
</success_criteria>

<output>
Create `.planning/phases/07-geometry-optimization/07-02-SUMMARY.md` when done.
</output>
