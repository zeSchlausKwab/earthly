---
phase: 11-temporal-sighting
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/nostr/temporal-sighting/temporal-sighting.test.ts
  - src/lib/nostr/expiry.test.ts
  - src/lib/nostr/temporal-sighting/observationState.test.ts
  - src/lib/nostr/geo-comment/sightingComment.test.ts
autonomous: true
requirements: [SIGHT-01, SIGHT-02, SIGHT-03, SIGHT-04]
user_setup: []

must_haves:
  truths:
    - "A failing (RED) test asserts the 37522 content geometry round-trips and bbox/g derive from it (SIGHT-01)"
    - "A failing test asserts the defensive content getter returns geometry:undefined for a geometry-less 37522 (no throw)"
    - "A failing test asserts contextReferences() emits a `c` tag (SIGHT-02) and modify() preserves `d`"
    - "A failing test asserts dropExpired removes an expired Sighting and keeps a non-expired one at a fixed UTC clock (SIGHT-03)"
    - "A failing test asserts classifyObservationState returns live/upcoming/past from start/end/now"
    - "A failing test asserts GeoCommentFactory.root({kind:37522}) emits K/k = 37522 (SIGHT-04)"
  artifacts:
    - path: "src/lib/nostr/temporal-sighting/temporal-sighting.test.ts"
      provides: "Geometry round-trip + bbox/g derive + defensive-parse + c-emit + d-preserve RED stubs"
      contains: "geometry"
    - path: "src/lib/nostr/temporal-sighting/observationState.test.ts"
      provides: "Observation-state classifier RED stub (live/upcoming/past)"
      contains: "classifyObservationState"
    - path: "src/lib/nostr/expiry.test.ts"
      provides: "Per-read-path expiry coverage RED stub for Sightings"
      contains: "dropExpired"
    - path: "src/lib/nostr/geo-comment/sightingComment.test.ts"
      provides: "rootKind=37522 K/k emit RED stub"
      contains: "TEMPORAL_SIGHTING_KIND"
  key_links:
    - from: "temporal-sighting.test.ts"
      to: "src/lib/nostr/temporal-sighting"
      via: "imports the (not-yet-existing) geometry field + lifecycle derivation"
      pattern: "from '@/lib/nostr/temporal-sighting'"
    - from: "observationState.test.ts"
      to: "classifyObservationState helper (created in Plan 02)"
      via: "import of the not-yet-existing classifier"
      pattern: "classifyObservationState"
---

<objective>
Establish the Nyquist Wave-0 RED baseline for Phase 11. Create/extend the test stubs that pin every net-new and extended seam's contract BEFORE implementation: the 37522 `geometry` content field + bbox/g derivation, the defensive geometry-absent parse, the `c`-emit and `d`-preserve invariants, the per-read-path `dropExpired` coverage, the observation-state classifier, and the `rootKind=37522` comment emit. These tests MUST fail (RED) now and turn GREEN as Plans 02–04 land.

Purpose: The phase's two net-new logic concerns (geometry-on-content + observation-state marker) and its multi-site expiry invariant (SIGHT-03) are exactly where regressions hide. Pinning them as failing tests first forces the implementations to satisfy a fixed contract instead of drifting.
Output: 1 extended test file, 1 extended test file, 2 new test files — all RED against the current tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-temporal-sighting/11-CONTEXT.md
@.planning/phases/11-temporal-sighting/11-RESEARCH.md
@.planning/phases/11-temporal-sighting/11-PATTERNS.md
@.planning/phases/11-temporal-sighting/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the 37522 contract test — geometry round-trip, bbox/g derive, defensive parse, c-emit, d-preserve</name>
  <read_first>
    - src/lib/nostr/temporal-sighting/temporal-sighting.test.ts (the file being modified — current scaffold contract)
    - src/lib/nostr/temporal-sighting/helpers.ts (TemporalSightingContent, getTemporalSightingContent defensive getter)
    - src/lib/nostr/temporal-sighting/factory.ts (create/modify/sighting/expiration/bbox/geohash/contextReferences setters)
    - src/lib/nostr/geo-event/helpers.ts:154-179 (computeBboxFor/computeGeohashFor — the turf derivation analog the lifecycle will mirror)
    - src/lib/nostr/story/lifecycle.test.ts (the analog lifecycle-test shape, mock.module publish pattern)
    - scripts/seed-entities.ts:418-455 (factory-built 37522 fixture shape: title/desc/start/end/expiration/c-attach)
  </read_first>
  <behavior>
    - Test: publishing a Sighting with content.geometry = Point [lon,lat] derives a bbox tag and a geohash tag from that geometry (assert the signed event carries `bbox` + `g` tags whose values match the turf bbox/centroid of the geometry). This imports the Plan-02 `publishSighting` lifecycle (not-yet-existing) OR asserts a `deriveSightingTags(geometry)` helper — pick the lifecycle entry the implementation will expose; reference it by the name Plan 02 will create (`publishSighting` from `@/lib/nostr/temporal-sighting`).
    - Test: a round-trip — castEvent(signed, TemporalSighting) → `.sighting.geometry` deep-equals the input Point geometry (SIGHT-01).
    - Test: getTemporalSightingContent on a 37522 event whose content JSON has NO `geometry` returns `geometry: undefined` and does NOT throw (defensive parse; current seed events have no geometry).
    - Test: TemporalSightingFactory.create({...}).contextReferences(['37518:pubkey:dtag']) emits a `c` tag with that coordinate (SIGHT-02).
    - Test: TemporalSightingFactory.modify(existingEvent) preserves the original `d` tag value (no lineage fork — SIGHT-01 / SPEC §17.1).
  </behavior>
  <action>Extend src/lib/nostr/temporal-sighting/temporal-sighting.test.ts with five new test cases per the behavior block. Add a `geometry` field assertion against the Plan-02 lifecycle: import `publishSighting` from `@/lib/nostr/temporal-sighting` (mock the `publish` call via `mock.module('@/lib/nostr', ...)` exactly as story/lifecycle.test.ts does so no live relay is hit), assert the signed template carries `bbox` + `g` tags derived from a Point geometry. Add the geometry round-trip via `castEvent(signed, TemporalSighting, undefined as never)` → `.sighting.geometry` deep-equal. Add the defensive geometry-absent parse against `getTemporalSightingContent`. Add the `c`-emit assertion against `TemporalSightingFactory.create(...).contextReferences([coord]).sign(...)`. Add the `modify`-preserves-`d` assertion. These reference Plan-02 symbols (`publishSighting`, `TemporalSightingContent.geometry`) that do not exist yet — the file MUST be RED. Do NOT stub the implementations; only the tests.</action>
  <verify>
    <automated>bun test src/lib/nostr/temporal-sighting/temporal-sighting.test.ts 2>&1 | grep -qE 'fail|error|Cannot find|not a function' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `bun test src/lib/nostr/temporal-sighting/temporal-sighting.test.ts` reports failures (RED) attributable to the missing `geometry` field / `publishSighting` lifecycle, not to syntax errors.
    - source: the test file contains assertions referencing `geometry`, `publishSighting`, `contextReferences`, and `modify` d-preservation.
    - CLI: `biome check src/lib/nostr/temporal-sighting/temporal-sighting.test.ts` is clean.
  </acceptance_criteria>
  <done>Five RED test cases added; suite fails on the missing geometry/lifecycle seam, biome clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Observation-state classifier test + per-read-path expiry coverage + rootKind=37522 comment emit</name>
  <read_first>
    - src/lib/nostr/expiry.test.ts (the file being extended — isExpired/dropExpired existing cases)
    - src/lib/nostr/expiry.ts:22-30 (isExpired/dropExpired UTC-epoch-seconds semantics)
    - src/lib/nostr/temporal-sighting/cast.ts:57-60 (expiresAt getter via getExpirationTimestamp)
    - src/lib/nostr/geo-comment/factory.ts (GeoCommentFactory.root — rootKind is a runtime param emitting ['K', kindStr])
    - src/lib/nostr/kinds.ts:30 (TEMPORAL_SIGHTING_KIND = 37522)
    - .planning/phases/11-temporal-sighting/11-PATTERNS.md (Shared Patterns: Observation-state classifier — live/upcoming/past definitions)
  </read_first>
  <behavior>
    - Test (new observationState.test.ts): classifyObservationState(start, end, now) returns 'live' when now ∈ [start,end]; 'live' when start ≤ now and end is undefined within a freshness window; 'upcoming' when start > now; 'past' when end < now (or start < now past freshness). Cover all three branches + the no-end live case + the no-start undefined case (default).
    - Test (extend expiry.test.ts): dropExpired drops a 37522 event whose `expiration` tag is strictly before a fixed `now` and keeps one with expiration after `now` and one with no expiration tag (SIGHT-03, UTC epoch seconds — never Date.now() ms).
    - Test (new sightingComment.test.ts): GeoCommentFactory.root({ kind: TEMPORAL_SIGHTING_KIND, address, authorPubkey }) emits a `K` tag and a `k`-equivalent root-kind tag equal to '37522' (SIGHT-04 — proves no allowlist change is needed).
  </behavior>
  <action>Create src/lib/nostr/temporal-sighting/observationState.test.ts importing `classifyObservationState` from `@/lib/nostr/temporal-sighting` (the Plan-02 classifier, not-yet-existing) and asserting live/upcoming/past branches at a fixed `now`. Extend src/lib/nostr/expiry.test.ts with a Sighting-shaped describe block asserting dropExpired against a fixed UTC `now` for expired/non-expired/no-expiration 37522 events. Create src/lib/nostr/geo-comment/sightingComment.test.ts asserting `GeoCommentFactory.root` emits root-kind 37522 (read the existing factory to match its exact root-tag shape — `['K', '37522']`). The classifier import makes observationState.test.ts RED; expiry + comment tests should PASS already (they exercise shipped seams) and pin the SIGHT-03/SIGHT-04 contract so a later regression is caught.</action>
  <verify>
    <automated>bun test src/lib/nostr/expiry.test.ts src/lib/nostr/geo-comment/sightingComment.test.ts 2>&1 | grep -qE 'pass|✓' && bun test src/lib/nostr/temporal-sighting/observationState.test.ts 2>&1 | grep -qE 'fail|Cannot find' && echo BASELINE-OK</automated>
  </verify>
  <acceptance_criteria>
    - behavior: observationState.test.ts is RED (missing `classifyObservationState`); expiry.test.ts Sighting block and sightingComment.test.ts PASS.
    - source: expiry.test.ts asserts a UTC-epoch comparison (no `Date.now()`); sightingComment.test.ts asserts kind string '37522'.
    - CLI: `biome check` clean on the three files.
  </acceptance_criteria>
  <done>Classifier test RED; expiry per-read-path + comment root-kind tests GREEN and pinning the contract; biome clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → none | Wave-0 stubs touch only `*.test.ts` files; no runtime/production surface, no untrusted input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-DOC | Tampering | Test asserting wrong expiry units (ms vs s) | mitigate | expiry.test.ts asserts UTC epoch-seconds at a fixed `now`; explicitly never `Date.now()` — pins SIGHT-03's correct unit so a ms-bug is caught in Plans 02–04. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; zero new deps for the whole phase (RESEARCH § Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `bun test src/lib/nostr/temporal-sighting src/lib/nostr/expiry.test.ts src/lib/nostr/geo-comment/sightingComment.test.ts` runs; the geometry/lifecycle and classifier assertions are RED, the expiry + comment assertions GREEN.
- `biome check` clean on all four files.
- No production source modified.
</verification>

<success_criteria>
- temporal-sighting.test.ts extended with geometry round-trip / bbox-g derive / defensive parse / c-emit / d-preserve (RED on the missing seam).
- observationState.test.ts created (RED on the missing classifier).
- expiry.test.ts extended with Sighting per-read-path coverage (GREEN, pins SIGHT-03 units).
- sightingComment.test.ts created (GREEN, pins SIGHT-04 root-kind=37522).
- All four files biome-clean; `bun run build` unaffected.
</success_criteria>

## Artifacts this phase produces

This plan (Wave 0) produces test scaffolding only. The implementation symbols it references (created in later plans) are listed in 11-02/03/04 PLAN.md "## Artifacts this phase produces" sections. New files this plan creates:
- `src/lib/nostr/temporal-sighting/observationState.test.ts`
- `src/lib/nostr/geo-comment/sightingComment.test.ts`

<output>
Create `.planning/phases/11-temporal-sighting/11-01-SUMMARY.md` when done.
</output>
