---
phase: 06-ai-bulk-transform-data-driven-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/geo-editor/api/predicate.test.ts
  - src/features/geo-editor/api/dedup.test.ts
  - src/features/geo-editor/api/geometryValidation.test.ts
  - src/features/chat/tools/bulk-tools.test.ts
  - src/features/geo-editor/api/diff.test.ts
autonomous: true
requirements: [TOOLS-02, TOOLS-03, TOOLS-04, STYLE-01, STYLE-02]
must_haves:
  truths:
    - "Every Phase 6 production module has a RED (failing-because-absent) test file before any production code is written."
    - "The test files encode the exact requirement→behavior contract from 06-VALIDATION.md (predicate operators, dedup grouping, geometry validation, batch modes, style materialize, style-aware headline)."
  artifacts:
    - path: "src/features/geo-editor/api/predicate.test.ts"
      provides: "RED unit tests for matchesPredicate + selectByPredicate (every operator + missing/exists edge cases) — TOOLS-02 (D-06) + TOOLS-03 select"
    - path: "src/features/geo-editor/api/dedup.test.ts"
      provides: "RED unit tests for findDuplicateGroups (by geometry/attributes/both, keep-first survivor) — TOOLS-03 dedup"
    - path: "src/features/geo-editor/api/geometryValidation.test.ts"
      provides: "RED unit tests for validateGeometryFeatures (kinks/zero-area/ring-validity, read-only) — TOOLS-04"
    - path: "src/features/chat/tools/bulk-tools.test.ts"
      provides: "RED behavior tests for the five bulk tools (declarative/intelligence batch, select, dedup-delete, validate, style materialize, gate flow, STYLE-02 round-trip) — TOOLS-02/03/04/STYLE-01/STYLE-02"
    - path: "src/features/geo-editor/api/diff.test.ts"
      provides: "EXTENDED with RED tests for classifyModifyKind (style/properties/geometry) — STYLE-01 diff headline"
  key_links:
    - from: "test files"
      to: "06-VALIDATION.md Per-Task Verification Map"
      via: "one describe block per Req-ID row"
      pattern: "describe\\("
---

<objective>
Wave 0 — create the failing (RED) test scaffolds for every Phase 6 production module BEFORE any implementation, per 06-VALIDATION.md (Nyquist Dimension 8). Each later plan's tasks turn its red tests green; no Phase 6 task ships without an automated verify command that points at a file created here (or extended here for `diff.test.ts`).

Purpose: Lock the requirement→behavior contract as executable specs so the implementing plans cannot drift, and so `bun test` is the objective gate. The five bulk tools, the predicate engine, dedup, validation, and the style-aware diff headline all get their acceptance behavior written down first.

Output: Four new `*.test.ts` files (predicate, dedup, geometryValidation, bulk-tools) and an additive extension to the existing `diff.test.ts`. They are RED on landing because the production symbols they import do not exist yet — that is the intended Wave 0 state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-bulk-transform-data-driven-styling/06-VALIDATION.md
@.planning/phases/06-ai-bulk-transform-data-driven-styling/06-RESEARCH.md
@.planning/phases/06-ai-bulk-transform-data-driven-styling/06-PATTERNS.md

# Test backbone + analogs (read for idioms — copy structure, not content):
@src/features/geo-editor/api/diff.test.ts
@src/features/chat/tools/ingest-tools.test.ts
@src/features/geo-editor/core/test-harness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED unit-test scaffolds for the three pure api/ modules (predicate, dedup, geometryValidation)</name>
  <read_first>
    - src/features/geo-editor/api/diff.test.ts (the canonical pure-module test idiom: `import { describe, expect, it } from 'bun:test'`, a local `pointFeature(id, coords, props)` helper building an `EditorFeature`, no editor instance needed)
    - src/features/geo-editor/api/diff.ts (EditorFeature shape used by predicates; `deepEqual` semantics dedup mirrors)
    - src/features/geo-editor/core/types/index.ts:51-83 (EditorFeature: `id: string`, `properties: GeoJsonProperties & {...style keys...}`)
    - .planning/phases/06-ai-bulk-transform-data-driven-styling/06-VALIDATION.md (Per-Task Verification Map rows TOOLS-02 D-06, TOOLS-03 select, TOOLS-03 dedup, TOOLS-04)
    - .planning/phases/06-ai-bulk-transform-data-driven-styling/06-RESEARCH.md (Pattern 1 predicate surface; §TOOLS-03 dedup options; §TOOLS-04 validation checks)
  </read_first>
  <action>
    Create `src/features/geo-editor/api/predicate.test.ts` importing the not-yet-existent `matchesPredicate`, `selectByPredicate`, and types `Predicate`/`PredicateOp` from `./predicate`. Reuse the local `pointFeature(id, coords, props)` helper idiom from `diff.test.ts`. Cover each operator the engine must support: `eq`, `neq`, `exists`, `missing`, `contains` (substring on string props), `in` (value-in-set), and the numeric comparisons `lt`/`lte`/`gt`/`gte`. Include the A4 "missing" semantics edge cases (absent key, null, empty string `''`, whitespace-only string ALL count as missing; a present non-empty value counts as exists). Cover the flat `all: PredicateOp[]` AND-list (all clauses must pass). Cover `selectByPredicate` returning the full-set matches (TOOLS-03 select half) — assert it returns ALL matching features, not a capped subset.

    Create `src/features/geo-editor/api/dedup.test.ts` importing the not-yet-existent `findDuplicateGroups` (and any `DedupBy`/`DuplicateGroup` types) from `./dedup`. Cover `by: 'geometry'` (identical geometry → one group, keep-first survivor = first in input order), `by: 'attributes'` (identical chosen `keys` tuple → group), `by: 'both'` (geometry AND attributes), and the no-duplicates case (zero groups). Assert the group identifies the survivor id (keep-first) and the non-survivor ids to delete. Assert the function is PURE — it returns groups/ids only and mutates nothing (no editor reference exists in this module).

    Create `src/features/geo-editor/api/geometryValidation.test.ts` importing the not-yet-existent `validateGeometryFeatures` (and the report type, e.g. `GeometryValidationReport`) from `./geometryValidation`. Cover: a self-intersecting polygon → flagged `withSelfIntersections` (turf `kinks`); a near-zero-area sliver polygon → flagged `withZeroArea` (turf `area` below threshold); an invalid ring (too few points / unclosed) → flagged `invalidRings`; a clean polygon → no issues. Assert the aggregate shape `{ checked, withSelfIntersections, withZeroArea, invalidRings, issues: [{ featureId, issues: [...] }] }` and that the function performs NO mutation (read-only — TOOLS-04 contract). Mark cross-feature gap/sliver detection as out of scope in a comment (A3 deferred).

    Use `it.todo`-free RED: write real `expect(...)` assertions against the absent symbols so the import resolution itself fails (the intended Wave 0 RED). Do NOT create the production modules in this task — they are Plans 02 and 03.
  </action>
  <verify>
    <automated>bun test src/features/geo-editor/api/predicate.test.ts src/features/geo-editor/api/dedup.test.ts src/features/geo-editor/api/geometryValidation.test.ts 2>&1 | grep -Eq "Cannot find module|error: Could not resolve|matchesPredicate|findDuplicateGroups|validateGeometryFeatures" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - The three test files exist and each contains real `expect` assertions (not `it.todo`/`it.skip`) — assert via `grep -c "expect(" <file>` returning ≥ 8 per file.
    - Running them is RED because the imported production symbols do not exist yet (module-not-found / unresolved import), proven by the grep on stderr in `<verify>`.
    - `predicate.test.ts` references every operator name: `grep -Eo "eq|neq|exists|missing|contains|'in'|lt|lte|gt|gte" src/features/geo-editor/api/predicate.test.ts` finds all ten.
    - `dedup.test.ts` references all three `by` modes (`geometry`, `attributes`, `both`) and `keep-first` semantics.
    - `geometryValidation.test.ts` asserts the read-only aggregate shape keys (`checked`, `withSelfIntersections`, `withZeroArea`, `invalidRings`, `issues`).
  </acceptance_criteria>
  <done>Three RED pure-module test files exist, each with concrete assertions against absent symbols, encoding the TOOLS-02 (D-06 predicate + select), TOOLS-03 (dedup), and TOOLS-04 (validation) contracts from 06-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: RED behavior-test scaffold for the five bulk tools + EXTEND diff.test.ts for classifyModifyKind</name>
  <read_first>
    - src/features/chat/tools/ingest-tools.test.ts (behavior-test idiom: drives tools via the registry/dispatch against `createHeadlessEditor`; `useEditorStore.getState().editor?.getAllFeatures()` assertion pattern; `BATCH_GEOCODE_MAX_ROWS` skip-and-report precedent)
    - src/features/geo-editor/core/test-harness.ts (createHeadlessEditor — the headless backbone)
    - src/features/chat/safeEditing/gateRunCode.ts (gate flow tests will mirror: snapshot → classify → confirm → cancel-rolls-back-to-zero)
    - src/features/geo-editor/api/diff.test.ts (the file to EXTEND additively; existing classifyMutation tests must stay untouched)
    - src/features/geo-editor/api/styleOptions.ts (CANONICAL_STYLE_KEYS — style-only change asserts only these keys differ; InvalidStyleOptionError for the unknown-key case)
    - .planning/phases/06-ai-bulk-transform-data-driven-styling/06-VALIDATION.md (rows TOOLS-02 D-04a/D-04b/gate, STYLE-01, STYLE-01 diff, STYLE-02)
    - .planning/phases/06-ai-bulk-transform-data-driven-styling/06-RESEARCH.md (Pattern 2/3/4/5; BULK_EDIT_MAX_FEATURES=100; Pitfall 1 host-over-all-ids; Pitfall 6 dedup delete intent)
  </read_first>
  <action>
    Create `src/features/chat/tools/bulk-tools.test.ts` as the behavior-test scaffold for the five tools (`batch_edit_features`, `select_features`, `dedup_features`, `validate_geometry`, `style_by_attribute`). Drive each through the registry dispatch (the `ingest-tools.test.ts` idiom) OR through the exported `registerBulkTools` registrar against `createHeadlessEditor`, asserting against `useEditorStore.getState().editor?.getAllFeatures()`. Import the not-yet-existent `registerBulkTools` and `BULK_EDIT_MAX_FEATURES` from `./bulk-tools`. Write RED tests for:
    - Declarative batch (D-04a): seed MORE features than the model's compacted sample would show, run a declarative `set`/`fillIfMissing` rule, assert it modified ALL matching features by id INCLUDING out-of-sample ones (SAFE-05 / Pitfall 1) — and assert the schema has NO `features`/`featureIds` array param for declarative mode.
    - Declarative ops coverage: `set`, `copy`, `template` (`{propKey}` interpolation), `fillIfMissing` (only fires on A4-missing values).
    - Intelligence batch (D-04b/D-05): submit an `id→value` map larger than `BULK_EDIT_MAX_FEATURES`, assert the cap applied + a skip-and-report message naming the remainder (`Edited N of M; rerun ...`); submit unknown ids, assert skipped-and-counted (no crash).
    - Gate flow (TOOLS-02 gate): a bulk modify snapshots ONCE, classifies `modify`, and Cancel rolls back to zero net mutation (mirror gateRunCode cancel assertion).
    - select_features (TOOLS-03 select): read-only, returns matched ids/summary, NO editor mutation.
    - dedup_features (TOOLS-03 dedup): seeds geometry-duplicate features, asserts non-survivors deleted via `intent:'delete'` (so Level-2 confirms — Pitfall 6), survivor kept (keep-first).
    - validate_geometry (TOOLS-04): read-only, returns the report, NO editor mutation.
    - style_by_attribute (STYLE-01): materializes canonical style keys per matched bucket; unmatched features untouched; optional `fallback` only applies when supplied (D-03); an unknown style key throws/propagates `InvalidStyleOptionError` (Pitfall 3).
    - STYLE-02 round-trip: after a style apply, `JSON.stringify(featureCollection)` → re-parse preserves the materialized style props on `properties.*`.

    EXTEND `src/features/geo-editor/api/diff.test.ts` (do NOT rewrite it): add a new `describe('classifyModifyKind')` block importing the not-yet-existent `classifyModifyKind` from `./diff`. Cover: geometry change → `'geometry'`; only-CANONICAL_STYLE_KEYS change → `'style'`; a non-style property change → `'properties'`. Leave every existing `classifyMutation` test exactly as-is (backward-compat — Open Question 3).
  </action>
  <verify>
    <automated>bun test src/features/chat/tools/bulk-tools.test.ts src/features/geo-editor/api/diff.test.ts 2>&1 | grep -Eq "Cannot find module|Could not resolve|registerBulkTools|classifyModifyKind" && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/chat/tools/bulk-tools.test.ts` exists with concrete `expect` assertions for all five tools — `grep -c "expect(" src/features/chat/tools/bulk-tools.test.ts` ≥ 12.
    - The bulk-tools test references `BULK_EDIT_MAX_FEATURES`, the skip-and-report message contract, host-over-all-ids (out-of-sample modify), and `intent` `'delete'` for dedup.
    - `diff.test.ts` is EXTENDED, not replaced: the original `classifyMutation` describe block is unchanged (assert via `grep -c "classifyMutation" src/features/geo-editor/api/diff.test.ts` ≥ 1 AND a new `classifyModifyKind` describe block present).
    - Both runs are RED because `registerBulkTools` / `BULK_EDIT_MAX_FEATURES` / `classifyModifyKind` do not exist yet (proven by the grep in `<verify>`).
    - The existing Phase 5 suite is not broken by the diff.test.ts edit: `bun test src/features/geo-editor/api/diff.test.ts 2>&1` shows the ORIGINAL classifyMutation tests still pass (only the new classifyModifyKind block is red).
  </acceptance_criteria>
  <done>The bulk-tools behavior test exists (RED) covering all five tools + gate + caps + STYLE-02 round-trip, and diff.test.ts is additively extended (RED) for classifyModifyKind without disturbing the existing green classifyMutation tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI model → chat tool args | Untrusted tool-call arguments enter at the future tool handlers; these tests pin the validation/cap behavior that defends that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01a | Tampering | Wave-0 tests as the contract | mitigate | Tests assert host-over-all-ids (SAFE-05) and gate cancel-to-zero so later plans cannot ship a bypass and stay green. |
| T-06-01b | Denial of Service | Intelligence-batch cap | mitigate | bulk-tools.test.ts pins `BULK_EDIT_MAX_FEATURES` + skip-and-report so the DoS bound is regression-protected from inception. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (06-RESEARCH §Standard Stack: "this phase installs zero external packages"); no Package Legitimacy Gate required, no install task in any plan. |

No high-severity threats are introduced by this test-only plan; it is the regression net for the mitigations the later plans implement.
</threat_model>

<verification>
- `bun test src/features/geo-editor/api/predicate.test.ts src/features/geo-editor/api/dedup.test.ts src/features/geo-editor/api/geometryValidation.test.ts src/features/chat/tools/bulk-tools.test.ts` — all RED (absent-symbol import failures), the intended Wave 0 state.
- `bun test src/features/geo-editor/api/diff.test.ts` — original classifyMutation tests GREEN, new classifyModifyKind block RED.
- `bun run lint` on the five touched files is clean (Biome).
</verification>

<success_criteria>
- Four new RED test files + one additively-extended test file exist, encoding every 06-VALIDATION.md Req-ID row as executable assertions.
- No production code created (Plans 02-05 own that); the only green tests are the pre-existing classifyMutation ones.
- Every downstream Phase 6 task can name a `bun test src/...` command that points at a file created/extended here.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

New test files (no production symbols — those land in later plans):
- `src/features/geo-editor/api/predicate.test.ts`
- `src/features/geo-editor/api/dedup.test.ts`
- `src/features/geo-editor/api/geometryValidation.test.ts`
- `src/features/chat/tools/bulk-tools.test.ts`

Extended file:
- `src/features/geo-editor/api/diff.test.ts` (additive `classifyModifyKind` describe block)
</artifacts_produced>

<output>
Create `.planning/phases/06-ai-bulk-transform-data-driven-styling/06-01-SUMMARY.md` when done.
</output>
