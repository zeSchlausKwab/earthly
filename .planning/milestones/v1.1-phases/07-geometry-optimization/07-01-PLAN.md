---
phase: 07-geometry-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/geometry/fixture.ts
  - src/features/chat/geometry/optimize.test.ts
  - src/features/chat/geometry/optimizeClient.test.ts
  - src/features/chat/geometry/optimize.acceptance.test.ts
  - src/features/chat/tools/geometry-tools.test.ts
  - src/lib/workers/workerAssets.ts
autonomous: true
requirements: [GEO-01, GEO-02, GEO-03]
must_haves:
  truths:
    - "A deterministic synthetic-fixture generator produces an oversized (>1MB) messy LineString FeatureCollection with superfluous vertices, microgaps, and an identical-props subset — the West Pacific Trail surrogate"
    - "RED test files exist for optimize(), the RPC client, the acceptance bar, and the geometry tool — they reference production symbols that do not yet exist, so `bun test` fails on them (the objective contract Waves 2-3 turn green)"
    - "The `optimize` worker asset is registered in WORKER_ASSETS so the dev route and prod build can serve/emit it"
  artifacts:
    - path: "src/features/chat/geometry/fixture.ts"
      provides: "makeOversizedTrailFixture() deterministic generator"
      min_lines: 30
    - path: "src/features/chat/geometry/optimize.test.ts"
      provides: "RED tests for report/topology/merge/unreachable"
    - path: "src/features/chat/geometry/optimize.acceptance.test.ts"
      provides: "RED GEO-03 acceptance: oversized → under BLOSSOM_UPLOAD_THRESHOLD_BYTES"
    - path: "src/lib/workers/workerAssets.ts"
      provides: "optimize entry in WORKER_ASSETS"
      contains: "optimize.worker.js"
  key_links:
    - from: "src/features/chat/geometry/optimize.test.ts"
      to: "src/features/chat/geometry/optimize"
      via: "import { optimize } from './optimize'"
      pattern: "from './optimize'"
    - from: "src/lib/workers/workerAssets.ts"
      to: "src/features/chat/geometry/optimize.worker.ts"
      via: "WORKER_ASSETS.optimize.sourcePath"
      pattern: "optimize.worker"
---

<objective>
Lay the Wave-0 foundation for Phase 7: a deterministic synthetic acceptance fixture (the
West Pacific Trail surrogate), RED test scaffolds that encode the GEO-01/02/03 behavioral
contract, and the one-line `WORKER_ASSETS` registration that lets the off-thread optimize
worker be served (dev) and emitted (prod).

Purpose: `bun test` becomes the objective, reviewer-free contract that Waves 2 and 3 turn
green. The fixture generator is checked in (not a 12MB blob) so the acceptance test is
deterministic and repo-light.
Output: `src/features/chat/geometry/fixture.ts` + 4 RED test files + the `optimize`
WORKER_ASSETS entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-geometry-optimization/07-CONTEXT.md
@.planning/phases/07-geometry-optimization/07-RESEARCH.md
@.planning/phases/07-geometry-optimization/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates (full phase inventory is in 07-04-PLAN.md):
- `src/features/chat/geometry/fixture.ts` — `makeOversizedTrailFixture(opts?)` deterministic generator
- `src/features/chat/geometry/optimize.test.ts`, `optimizeClient.test.ts`, `optimize.acceptance.test.ts` (RED)
- `src/features/chat/tools/geometry-tools.test.ts` (RED)
- New `WORKER_ASSETS` key: `optimize` (servedName `optimize.worker.js`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic West Pacific Trail surrogate fixture generator</name>
  <files>src/features/chat/geometry/fixture.ts</files>
  <read_first>
    - `.planning/phases/07-geometry-optimization/07-RESEARCH.md` §"Synthetic acceptance fixture" (the exact fixture spec: N≈300 LineStrings, superfluous near-collinear vertices, microgaps within dissolve tolerance, identical-props subset, total > 1MB)
    - `src/features/geo-editor/constants.ts:26` — `BLOSSOM_UPLOAD_THRESHOLD_BYTES` (the >1MB target the generator must exceed)
    - `src/features/geo-editor/core/utils/featureHelpers.ts:105` — `normalizeLineCoordinates` (so the generated microgaps sit just OUTSIDE the snap tolerance pre-stitch, inside it post-stitch)
    - `src/features/geo-editor/components/toolbar/SimplifyDialog.tsx:36` — `estimateFeatureCollectionBytes` (the EXACT `TextEncoder().encode(JSON.stringify({type:'FeatureCollection',features})).length` measurement the fixture must exceed)
  </read_first>
  <action>
    Create `makeOversizedTrailFixture(opts?: { lineCount?: number; pointsPerLine?: number; seed?: number })` returning `{ type: 'FeatureCollection', features: EditorFeature[] }`. Generate ~300 LineString features tracing a coarse path across the West Pacific, each densified with near-collinear superfluous vertices (so turf.simplify can remove most), with deliberate microgaps between consecutive lines BELOW the `0.00001` dissolve tolerance (so stitch joins them), and a subset (~1/3) sharing byte-identical `properties` (name/description) so the lossless identical-props merge has work to do. Use a tiny deterministic PRNG seeded by `opts.seed` (default fixed) so the fixture is reproducible across runs. Each feature gets a stable string `id`. Size the densification so the serialized FeatureCollection (measured exactly as `new TextEncoder().encode(JSON.stringify({ type: 'FeatureCollection', features })).length`) exceeds `BLOSSOM_UPLOAD_THRESHOLD_BYTES`. Export a helper `fixtureBytes(fc)` using that same serialization. Import `EditorFeature` from `@/features/geo-editor/core/types` (type only). NO turf, NO editor, NO worker imports — pure data generation.
  </action>
  <verify>
    <automated>bun test src/features/chat/geometry/fixture.test.ts -t oversized 2>/dev/null || bun -e "import('./src/features/chat/geometry/fixture.ts').then(m=>{const fc=m.makeOversizedTrailFixture();const b=m.fixtureBytes(fc);if(b<=1048576)throw new Error('fixture too small: '+b);if(fc.features.length<100)throw new Error('too few features');console.log('OK bytes='+b+' features='+fc.features.length)})"</automated>
  </verify>
  <acceptance_criteria>
    - `makeOversizedTrailFixture()` returns a FeatureCollection whose `fixtureBytes(fc) > BLOSSOM_UPLOAD_THRESHOLD_BYTES` (1048576)
    - `fc.features.length >= 100`, all features are `LineString`, every feature has a unique string `id`
    - Calling the generator twice with the same (default) seed yields byte-identical output (deterministic)
    - A subset of features share byte-identical `properties` (verifiable: at least one duplicate `JSON.stringify(properties)` group of size ≥ 2)
    - File imports NO turf / editor / worker module (grep: `grep -E "from '@turf|geo-editor/core/GeoEditor|\.worker'" src/features/chat/geometry/fixture.ts` returns nothing)
  </acceptance_criteria>
  <done>The fixture generator deterministically produces an oversized, messy, partly-identical-props LineString collection exceeding the 1MB budget.</done>
</task>

<task type="auto">
  <name>Task 2: RED test scaffolds for optimize(), the RPC client, acceptance, and the tool</name>
  <files>src/features/chat/geometry/optimize.test.ts, src/features/chat/geometry/optimizeClient.test.ts, src/features/chat/geometry/optimize.acceptance.test.ts, src/features/chat/tools/geometry-tools.test.ts</files>
  <read_first>
    - `.planning/phases/07-geometry-optimization/07-RESEARCH.md` §"Validation Architecture" → "Phase Requirements → Test Map" (the exact test names/`-t` filters and which file owns each) and §"Synthetic acceptance fixture" (the assertion list)
    - `.planning/phases/06-ai-bulk-transform-data-driven-styling/06-CONTEXT.md` — the Wave-0 RED-scaffold idiom (06-01 used namespace imports so a missing named export does not crash the whole file's module load)
    - `src/features/chat/geometry/fixture.ts` — the generator from Task 1 (the tests import it)
    - `src/features/geo-editor/api/geometryValidation.ts:53` — `GeometryValidationReport` shape (the topology assertion reads `withSelfIntersections`/`withZeroArea`)
  </read_first>
  <action>
    Write four RED test files referencing production symbols that do not exist yet (turning green in Waves 2-3). Use a NAMESPACE import (`import * as optimizeMod from './optimize'`) so a missing named export is a runtime assertion failure, not a module-load crash that takes other files down (the 06-01 idiom). (1) `optimize.test.ts` — drive `optimizeMod.optimize(makeOversizedTrailFixture(), budget)`: a `-t report` block asserting `report` carries `bytesBefore/After`, `verticesBefore/After`, `featuresBefore/After`, `microgapJoins`; a `-t topology` block asserting a hand-built fixture where an aggressive tolerance would introduce a NEW self-intersection is rejected (result has no MORE self-intersections than baseline); a `-t merge` block asserting two identical-props same-type features merge to one Multi* while a differing-props pair stays separate, with every input property value still present on some output feature; a `-t unreachable` block asserting an impossibly-small budget returns `reachedBudget:false` WITHOUT throwing and still reduces bytes. (2) `optimizeClient.test.ts` — assert `runOptimize(fc, budget)` (or the chosen export) settles (resolves) in a non-worker environment via the sync fallback, never hangs. (3) `optimize.acceptance.test.ts` — the GEO-03 bar: `optimize(makeOversizedTrailFixture(), BLOSSOM_UPLOAD_THRESHOLD_BYTES)` yields `bytesAfter < BLOSSOM_UPLOAD_THRESHOLD_BYTES`, `verticesAfter < verticesBefore`, `featuresAfter <= featuresBefore`, `report.microgapJoins > 0`, no NEW topology problems vs baseline. (4) `geometry-tools.test.ts` — assert `registerGeometryTools` registers an `optimize_geometry` entry; assert the schema's only model-facing arg is an optional `targetBytes` (no feature/id array); assert (with a headless editor + mocked gate) it dispatches and gates as `modify`. Mark blocks `.skip`-free so they actively fail RED; it is acceptable for files to fail to import the missing `./optimize` — the suite must show these as failing, not passing.
  </action>
  <verify>
    <automated>bun test src/features/chat/geometry/ src/features/chat/tools/geometry-tools.test.ts 2>&1 | grep -Eq "fail|error|Cannot find module|is not a function|undefined" && echo "RED-OK: scaffolds fail as expected"</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist and FAIL when run (they reference `./optimize`, `runOptimize`/`optimizeClient`, and `registerGeometryTools` which do not exist yet) — `bun test src/features/chat/geometry/` exits non-zero
    - `optimize.test.ts` contains test blocks matching `-t report`, `-t topology`, `-t merge`, `-t unreachable` (grep: `grep -cE "report|topology|merge|unreachable" src/features/chat/geometry/optimize.test.ts` ≥ 4)
    - `optimize.acceptance.test.ts` asserts `bytesAfter` compared against `BLOSSOM_UPLOAD_THRESHOLD_BYTES` and `report.microgapJoins > 0`
    - `geometry-tools.test.ts` asserts the tool's only model-facing arg is `targetBytes` and that it gates as `'modify'`
    - Tests import the Task-1 fixture (grep: `grep -l "fixture" src/features/chat/geometry/*.test.ts` lists optimize.test.ts + optimize.acceptance.test.ts)
  </acceptance_criteria>
  <done>Four RED test files encode the GEO-01/02/03 contract and fail against absent production symbols.</done>
</task>

<task type="auto">
  <name>Task 3: Register the optimize worker asset</name>
  <files>src/lib/workers/workerAssets.ts</files>
  <read_first>
    - `src/lib/workers/workerAssets.ts:45` — the `WORKER_ASSETS` map (the `ingest` entry at :50 is the exact shape to mirror) and the docstring explaining WHY the `new Worker(new URL(...))` form is forbidden
    - `build.ts:270` — the `for (const id of Object.keys(WORKER_ASSETS))` loop that emits each worker to `dist/workers/<servedName>` (proves one map entry is all that is needed)
    - `.planning/debug/sandbox-worker-file-url-dev.md` — the file:// dev landmine + the pino-barrel secondary landmine this seam exists to avoid
  </read_first>
  <action>
    Add ONE entry to the `WORKER_ASSETS` object literal, mirroring the `ingest` entry: key `optimize`, `servedName: 'optimize.worker.js'`, `sourcePath: 'src/features/chat/geometry/optimize.worker.ts'`. Do not touch `workerUrl`, `WorkerId`, or the docstring beyond the new entry. The worker source file itself is created in 07-03; this entry pre-registers it so the dev `/workers/:name` route and the prod `build.ts` loop pick it up. The `satisfies Record<string, WorkerAsset>` constraint stays valid.
  </action>
  <verify>
    <automated>bun -e "import('./src/lib/workers/workerAssets.ts').then(m=>{if(m.workerUrl('optimize')!=='/workers/optimize.worker.js')throw new Error('bad url '+m.workerUrl('optimize'));console.log('OK '+m.workerUrl('optimize'))})"</automated>
  </verify>
  <acceptance_criteria>
    - `workerUrl('optimize')` returns `'/workers/optimize.worker.js'` (TypeScript accepts `'optimize'` as a `WorkerId`)
    - `WORKER_ASSETS.optimize.sourcePath === 'src/features/chat/geometry/optimize.worker.ts'`
    - `bun run build` is NOT required to pass yet (the worker source does not exist until 07-03); this task only adds the registry entry — verify by the import assertion above, not a full build
  </acceptance_criteria>
  <done>`optimize` is a registered WorkerId resolving to the stable served URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/fixture → suite | Generated fixture is in-repo deterministic data; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | `makeOversizedTrailFixture` size | accept | Fixture is bounded by `lineCount`/`pointsPerLine` (default ~300×~N); deterministic and only sized just over 1MB — not unbounded. Generated at test time only, never at runtime. |
| T-07-02 | Information Disclosure | fixture/test files reaching the worker bundle | mitigate | Test + fixture files are `*.test.ts`/`fixture.ts` — never imported by `optimize.worker.ts` (Wave-2 worker imports only `./optimize` + leaf helpers). No secret reachability. |
| T-07-03 | Tampering | new WORKER_ASSETS entry | accept | Additive config entry; the served worker source is created in 07-03 under the same leaf-import rule. No mutation surface here. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH §"Installation: None"); slopcheck N/A. |
</threat_model>

<verification>
- `bun test src/features/chat/geometry/` fails (RED scaffolds) — expected at end of this plan.
- The fixture generator and the WORKER_ASSETS entry both import-load cleanly.
- `biome check src/features/chat/geometry/ src/lib/workers/workerAssets.ts` is clean.
- Do NOT run a full `bun run build` gate here — the worker source lands in 07-03.
</verification>

<success_criteria>
- Deterministic oversized fixture > 1MB exists and is reproducible.
- Four RED test files exist and fail against absent Wave-2/3 symbols.
- `optimize` is a registered WorkerId.
- Biome clean on touched files.
</success_criteria>

<output>
Create `.planning/phases/07-geometry-optimization/07-01-SUMMARY.md` when done.
</output>
