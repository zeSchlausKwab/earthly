---
phase: 07-geometry-optimization
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/features/chat/tools/geometry-tools.ts
  - src/features/chat/tools/schemas.ts
  - src/features/chat/tools/registry.ts
autonomous: true
requirements: [GEO-01, GEO-02, GEO-03]
must_haves:
  truths:
    - "An optimize_geometry tool is registered in the typed registry; its ONLY model-facing arg is an optional targetBytes; it reads the FULL bound dataset via editor.getAllFeatures() (SAFE-05), never the model's compacted view (D-01/D-04)"
    - "The tool runs the off-thread optimize over all bound features, then applies the converged result through gateBulkApply as a single 'modify' snapshot with a metrics-aware headline — one undoable apply, one before/after diff block (D-04b)"
    - "The applied result routes through createAuthoring/the facade → runInterceptors; no raw editor.* mutation from chat/** (A3 boundary clean)"
    - "After optimize the user reviews before/after and publishes via the normal flow — NO auto-publish; an unreachable budget yields a best-effort honest report and the dataset can still take the existing Blossom external-upload path (D-07)"
    - "targetBytes is validated/clamped: finite positive number, absurd/NaN/negative values rejected or defaulted to BLOSSOM_UPLOAD_THRESHOLD_BYTES (V5)"
  artifacts:
    - path: "src/features/chat/tools/geometry-tools.ts"
      provides: "registerGeometryTools(register) + optimize_geometry handler + buildOptimizeHeadline + applyOptimizedCollection"
      exports: ["registerGeometryTools"]
      min_lines: 60
    - path: "src/features/chat/tools/schemas.ts"
      provides: "optimize_geometry OpenAI schema (optional targetBytes only)"
      contains: "optimize_geometry"
    - path: "src/features/chat/tools/registry.ts"
      provides: "registerGeometryTools(register) wired into bootstrapRegistry()"
      contains: "registerGeometryTools"
  key_links:
    - from: "src/features/chat/tools/geometry-tools.ts"
      to: "src/features/chat/geometry/optimizeClient.ts"
      via: "runOptimize over editor.getAllFeatures()"
      pattern: "runOptimize"
    - from: "src/features/chat/tools/geometry-tools.ts"
      to: "src/features/chat/safeEditing/gateBulkEdit.ts"
      via: "gateBulkApply(editor, { headline }, 'modify', apply)"
      pattern: "gateBulkApply\\("
    - from: "src/features/chat/tools/registry.ts"
      to: "src/features/chat/tools/geometry-tools.ts"
      via: "registerGeometryTools(register) in bootstrapRegistry()"
      pattern: "registerGeometryTools\\(register\\)"
---

<objective>
Wire the optimizer into the chat as the `optimize_geometry` tool: read the full bound
dataset (SAFE-05), run the off-thread `runOptimize`, apply the converged result through
`gateBulkApply` as ONE gated `modify` snapshot with a metrics-aware headline (D-04b), and
register it via the injected-`register` idiom. Close GEO-01/02/03 end-to-end and verify the
prod build emits the worker.

Purpose: turn the pure converger (07-03) + the headline plumbing (07-02) into a working AI
capability that drives the whole-dataset optimization through the Phase-5 gate, with no
auto-publish and an honest unreachable-budget path.
Output: `geometry-tools.ts`, the `optimize_geometry` schema, and the registry bootstrap call.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-geometry-optimization/07-CONTEXT.md
@.planning/phases/07-geometry-optimization/07-RESEARCH.md
@.planning/phases/07-geometry-optimization/07-PATTERNS.md
@src/features/chat/tools/bulk-tools.ts
</context>

<artifacts_this_phase_produces>
**Full Phase 7 artifact inventory** (this section is the canonical list for the phase):

New tool / symbol:
- `optimize_geometry` — the registered AI tool (`kind: 'authoring-primitive'`); ONLY model-facing arg = optional `targetBytes`
- `registerGeometryTools(register)` — injected-register tool registrar (this plan)
- `buildOptimizeHeadline(report)` — metrics-aware headline string builder (this plan)
- `applyOptimizedCollection(editor, result)` — facade-routed whole-collection replace apply (this plan)

New functions/types (07-03):
- `optimize(fc, targetBytes?) → { result, report }` — pure pipeline
- `runOptimize(fc, targetBytes?)` + `terminateOptimizeWorker()` — RPC client
- `OptimizeRequest` / `OptimizeResponse` / `OptimizeReport` — message + report shapes

New file paths:
- `src/features/chat/geometry/{types,optimize,optimize.worker,optimizeClient,fixture}.ts` (07-01 fixture, 07-03 the rest)
- `src/features/chat/tools/geometry-tools.ts` (this plan)
- test files: `src/features/chat/geometry/{optimize,optimizeClient,optimize.acceptance}.test.ts`, `src/features/chat/tools/geometry-tools.test.ts` (07-01)

New WORKER_ASSETS entry: `optimize` → served `optimize.worker.js` → emitted `dist/workers/optimize.worker.js` (07-01 entry, 07-03 source)

Gate-plumbing additions (07-02): optional `headline` on `EmitDiffBlockOptions`, `PendingDiffEntry`, `GateBulkDeps`, and the `DatasetDiffDisclosure` render branch

New `optimize_geometry` schema entry in `geoStaticToolSchemas` (this plan)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: optimize_geometry schema + registrar + gated apply + bootstrap wiring</name>
  <files>src/features/chat/tools/geometry-tools.ts, src/features/chat/tools/schemas.ts, src/features/chat/tools/registry.ts</files>
  <read_first>
    - `src/features/chat/tools/bulk-tools.ts` — the EXACT analog: the injected-`register` idiom (`:310`), `requireEditor()` (`:89-95`), `editor.getAllFeatures()` full-set read (`:319-321`), and the `gateBulkApply(editor, { getSafetyLevel, label }, 'modify', () => …)` call shape (`:376-383`)
    - `src/features/chat/safeEditing/gateBulkEdit.ts` — `gateBulkApply` signature incl. the new `headline` dep from 07-02 (`GateBulkDeps.headline`)
    - `src/features/chat/safeEditing/safetyAccess.ts` — `getSafetyLevel` (passed in `deps`)
    - `src/features/chat/geometry/optimizeClient.ts` — `runOptimize(fc, targetBytes?)` from 07-03
    - `src/features/geo-editor/api/authoring.ts:281,309` — `createAuthoring(editor)` + `writeGeoJSON(input, { replace: true })` (the facade-routed whole-collection replace apply — runs through `runInterceptors`); `:524` `deleteFeaturesById` is the precedent for keeping raw `editor.*` out of chat/**
    - `src/features/chat/tools/schemas.ts:17` (`schemaFor`), `:905` (`select_features` schema shape), end of `geoStaticToolSchemas` array (~`:1178` style_by_attribute is the last entry — append after it)
    - `src/features/chat/tools/registry.ts:29` (the `registerBulkTools` import line), `:1089` (`registerBulkTools(register)` inside `bootstrapRegistry()`) — mirror both for `registerGeometryTools`
    - `src/features/chat/tools/geometry-tools.test.ts` (07-01 RED) — the contract this turns green
    - `.planning/phases/07-geometry-optimization/07-PATTERNS.md` §`geometry-tools.ts` (the full gated-apply call shape) + §"Apply representation: approach (a) replace-in-place, headline is the truth"
  </read_first>
  <action>
    SCHEMA (`schemas.ts`): append one entry to `geoStaticToolSchemas` for `optimize_geometry`. Description: a host-driven tool that shrinks the ENTIRE bound dataset toward a byte budget via stitch→merge→simplify, off-thread, reporting before/after metrics; the host reads the full dataset itself — the model supplies NO features/ids. Parameters: a single OPTIONAL `targetBytes` (number, "target serialized byte budget; defaults to the 1MB publish limit"). `required: []`. No feature/id array param (D-04).
    REGISTRAR (`geometry-tools.ts`): new module using the INJECTED-`register` idiom — import ONLY `type ToolEntry` from `./registry` (never the value `register`; Pitfall 6 circular-init). Export `registerGeometryTools(register: (entry: ToolEntry) => void)`. Inside, `register({ name: 'optimize_geometry', kind: 'authoring-primitive', schema: schemaFor('optimize_geometry'), handler: async (args) => { … } })`. Handler: `requireEditor()` (mirror bulk-tools); `const all = editor.getAllFeatures()` (FULL id-keyed set, SAFE-05); V5-validate `targetBytes` — `const budget = (typeof args.targetBytes === 'number' && Number.isFinite(args.targetBytes) && args.targetBytes > 0) ? args.targetBytes : BLOSSOM_UPLOAD_THRESHOLD_BYTES` (reject NaN/negative/zero/absurd by defaulting; optionally clamp an absurd ceiling); `const { result, report } = await runOptimize({ type: 'FeatureCollection', features: all }, budget)`. Build `buildOptimizeHeadline(report)` → a compact string like `"12.0MB → 0.9MB · 41k→3.2k pts · 312→18 features · 47 joins"` (+ a "still over limit" note when `report.reachedBudget === false`). Apply via `gateBulkApply(editor, { getSafetyLevel, label: 'Optimize geometry', headline: buildOptimizeHeadline(report) }, 'modify', () => applyOptimizedCollection(editor, result))`. Define `applyOptimizedCollection(editor, result)` to route the whole-collection replace through the FACADE: `createAuthoring(editor).writeGeoJSON(result.features, { replace: true })` (replace-in-place; the headline is the user-facing truth per approach (a) — `classifyMutation` is id-keyed and merge mints new ids, so the per-row list is secondary). Return `{ cancelled: outcome.status === 'cancelled', ...report }` so the model sees the metrics + `reachedBudget`. NO auto-publish anywhere (D-07).
    BOOTSTRAP (`registry.ts`): add `import { registerGeometryTools } from './geometry-tools'` (mirror line 29) and call `registerGeometryTools(register)` in `bootstrapRegistry()` after the `registerBulkTools(register)` line (mirror :1089).
  </action>
  <verify>
    <automated>bun test src/features/chat/tools/geometry-tools.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/features/chat/tools/geometry-tools.test.ts` passes: `optimize_geometry` is registered; its schema's only model-facing arg is optional `targetBytes` with no feature/id array; the handler gates as `'modify'` and applies one snapshot
    - `geometry-tools.ts` imports ONLY `type ToolEntry` from `./registry` (grep: `grep -E "from './registry'" src/features/chat/tools/geometry-tools.ts` shows `import type`), never a value `register` import (no circular-init)
    - The apply routes through the facade, NOT a raw editor verb in chat/** (grep: `grep -E "editor\.(writeGeoJSON|setFeatures|addFeature|deleteFeatures|updateFeature)\(" src/features/chat/tools/geometry-tools.ts` returns nothing; `grep -c "createAuthoring" src/features/chat/tools/geometry-tools.ts` ≥ 1) — the A3 boundary scan stays green
    - `gateBulkApply(...'modify'...)` is the only apply path and receives a `headline` (grep: `grep -c "gateBulkApply" src/features/chat/tools/geometry-tools.ts` ≥ 1; `grep -c "headline" src/features/chat/tools/geometry-tools.ts` ≥ 1)
    - NO auto-publish (grep: `grep -E "publish|usePublishing|publishNew" src/features/chat/tools/geometry-tools.ts` returns nothing)
    - `registry.ts` calls `registerGeometryTools(register)` inside `bootstrapRegistry()` (grep ≥ 1)
    - `targetBytes` is V5-validated (grep: `grep -E "Number.isFinite|BLOSSOM_UPLOAD_THRESHOLD_BYTES" src/features/chat/tools/geometry-tools.ts` ≥ 1)
  </acceptance_criteria>
  <done>`optimize_geometry` reads the full dataset, optimizes off-thread, and applies through the gate as one metrics-headlined `modify` snapshot — no auto-publish, no raw editor mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite green, prod build emits the worker, A3 boundary clean</name>
  <files>src/features/chat/tools/geometry-tools.ts</files>
  <read_first>
    - the existing boundary/A3 test (the scan that forbids raw `editor.*` mutation tokens + secret imports in `chat/**`) — confirm `geometry-tools.ts` and the `chat/geometry/*` worker files pass it
    - `build.ts:270` — the worker emission loop (confirms `dist/workers/optimize.worker.js` is produced)
    - the tsc-baseline memory: gates are `bun test` + `bun run build` + `biome` — do NOT add a clean-`tsc --noEmit` criterion
  </read_first>
  <action>
    No new feature code — this is the phase-completion gate. Run the full `bun test` suite and confirm zero regressions (especially the Phase 5/6 diff tests after the 07-02 additive headline change, and the A3/boundary scan over `chat/**`). Run `bun run build` (dev) and `bun run build:production`, confirming `dist/workers/optimize.worker.js` is emitted and the optimize worker's bundle contains no Nostr/signer/pino imports (leaf-only). Run `biome check` on all touched files. If the boundary scan flags a literal `editor.*` token in `geometry-tools.ts`, move the mutation into the facade path (mirror how `bulk-tools.ts` uses `deleteFeaturesById`/`createAuthoring`). Fix any failure in this plan's files only; do not expand scope. Record the before/after metrics observed on the 07-01 fixture in the SUMMARY for the verify-work UAT (the live 12MB West Pacific Trail import → optimize → publish round-trip is the human-gated UAT, deferred to `/gsd-verify-work 7`).
  </action>
  <verify>
    <automated>bun test 2>&1 | tail -5 && bun run build:production 2>&1 | tail -3 && test -f dist/workers/optimize.worker.js && echo "WORKER-EMITTED"</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` (full suite) exits 0 — no Phase 5/6 regressions, all Phase 7 geometry/tool tests green
    - `bun run build` AND `bun run build:production` succeed; `dist/workers/optimize.worker.js` exists after the production build
    - The A3/boundary scan over `chat/**` passes (no raw `editor.*` mutation token, no secret import in `geometry-tools.ts` / `chat/geometry/*`)
    - `biome check` clean on all touched files
    - SUMMARY records the observed `bytesBefore→bytesAfter / verticesBefore→After / microgapJoins` on the synthetic fixture (the metrics the live UAT will reproduce)
    - NO clean-`tsc --noEmit` requirement asserted (baseline has ~305 pre-existing errors)
  </acceptance_criteria>
  <done>Full suite green, both builds succeed and emit the optimize worker, boundary scan clean — Phase 7 is implementation-complete and ready for `/gsd-verify-work 7`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → optimize_geometry | The model supplies ONLY an optional `targetBytes` number; never geometry/ids (D-04). |
| chat tool → editor | All mutation funnels through `gateBulkApply` → `createAuthoring` → `runInterceptors` (the Phase-5 gate). |
| host → publish | Optimization NEVER publishes; publishing stays the explicit `usePublishing` user action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Denial of Service | absurd/NaN `targetBytes` | mitigate | V5: validate finite positive; reject/clamp absurd values, default to `BLOSSOM_UPLOAD_THRESHOLD_BYTES`. The bounded binary search (07-03 `MAX_ITERS`) caps work regardless of the budget value; the off-thread RPC client's timeout guarantees settlement. |
| T-07-12 | Tampering | mutation bypassing the safe-editing gate | mitigate | The converged result applies ONLY via `gateBulkApply` → `createAuthoring` (facade) → `runInterceptors`; the no-raw-`editor.*` grep + the A3 boundary scan enforce it. One apply = one snapshot = one undo (D-04b / SAFE-06). |
| T-07-13 | Tampering / Repudiation | auto-publish of AI-optimized data | mitigate | NO publish call anywhere in `geometry-tools.ts` (grep-enforced). Under-limit result feeds `usePublishing.isOverSizeLimit` for the user's explicit publish; over-limit falls back to the existing `BlossomUploadDialog` escape hatch (D-07). `[CITED: REQUIREMENTS.md Out-of-Scope "Auto-publish of AI edits"]` |
| T-07-14 | Information Disclosure | secret reach via the new tool module | mitigate | `geometry-tools.ts` imports `runOptimize` (leaf RPC client), `gateBulkApply`, `createAuthoring`, `schemaFor` — no signer/wallet/Nostr; the worker bundle stays secret-free (07-03 boundary). Boundary scan over `chat/**` enforces it. |
| T-07-15 | Repudiation | silent over-budget result | mitigate | `report.reachedBudget:false` surfaces in the tool return AND the headline ("still over limit"), and the gate diff is shown before apply — no silent truncation (Phase 3/6 discipline). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase; slopcheck N/A. |
</threat_model>

<verification>
- `bun test` full suite green; `bun run build` + `bun run build:production` succeed and emit `dist/workers/optimize.worker.js`.
- A3/boundary scan over `chat/**` clean; no auto-publish; no raw `editor.*` mutation in chat tool code.
- `biome check` clean on touched files.
- Live 12MB West Pacific Trail import → optimize → publish round-trip is the human-gated `/gsd-verify-work 7` UAT (deferred; the mechanics are bun-test-proven here).
</verification>

<success_criteria>
- `optimize_geometry` registered with a target-bytes-only model surface, reading the full bound dataset (SAFE-05).
- Off-thread optimize → one gated `modify` apply with a metrics-aware headline → one undoable snapshot (D-04b).
- No auto-publish; honest unreachable-budget path; Blossom fallback intact (D-07).
- Full suite + both builds green; worker emitted; boundary clean.
</success_criteria>

<output>
Create `.planning/phases/07-geometry-optimization/07-04-SUMMARY.md` when done.
</output>
