/**
 * Whether list surfaces should let their containing sheet show through.
 * This is independent of embedded headers: desktop Browse embeds its lists too,
 * but keeps their opaque card surface. Selection and hover remain tinted overlays.
 */
export { PanelTranslucencyContext as EntityListTranslucencyContext } from '@/components/PanelTranslucencyContext'
